.header-left { display: flex; align-items: center; }
.header-right { display: flex; align-items: center; gap: var(--space-sm); -webkit-app-region: no-drag; }
.header-selector { padding: 3px 8px;
  background-color: var(--bg-tertiary);
  border: 1px solid var(--border-color);
  color: var(--text-secondary);
  font-size: var(--font-size-sm);
  border-radius: var(--radius-sm);
  max-width: 140px;
  cursor: pointer;
  outline: none;
  transition: all 0.12s ease; }
.header-selector:hover { border-color: var(--border-light); color: var(--text-primary); }
.header-selector:focus { border-color: var(--accent); }
.sidebar-btn { position: relative;
  display: flex;
  align-items: center;
  justify-content: center;
  width: 40px;
  height: 40px;
  border: none;
  border-radius: var(--radius-sm);
  background: transparent;
  color: var(--text-muted);
  cursor: pointer;
  transition: all 0.12s ease; }
.sidebar-btn:hover { background: var(--bg-tertiary); color: var(--text-primary); }
.sidebar-btn.active { background: var(--accent-dim);
  color: var(--accent);
  box-shadow: var(--shadow-glow); }
.sidebar-btn.active::before { content: '';
  position: absolute;
  left: -4px;
  top: 50%;
  transform: translateY(-50%);
  width: 2px;
  height: 20px;
  background: var(--accent);
  border-radius: 1px; }
.sidebar-btn svg { width: 18px; height: 18px; stroke-width: 1.5; }
.sidebar-divider { width: 24px; height: 1px;
  background: linear-gradient(90deg, transparent, var(--border-color), transparent);
  margin: var(--space-xs) 0; }
.tab-content { min-height: 300px; }
.tree-header { padding: 8px 10px; border-bottom: 1px solid var(--border-color); display: flex; justify-content: space-between; align-items: center; }
.tree-header span { font-size: var(--font-size-sm); color: var(--text-secondary); font-weight: 600; max-width: 120px; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
.tree-body { flex: 1; overflow-y: auto; padding: var(--space-xs); }
.tree-volume { margin: var(--space-xs) 0; }
.tree-volume-header { display: flex; align-items: center; padding: 4px 6px; cursor: pointer; border-radius: var(--radius-sm); font-size: var(--font-size-sm); color: var(--text-secondary); }
.tree-volume-header:hover { background: var(--bg-tertiary); }
.tree-volume-header .arrow { width: 14px; font-size: var(--font-size-xs); color: var(--text-muted); transition: transform .15s; }
.tree-volume-header .arrow.open { transform: rotate(90deg); }
.tree-chapters { padding-left: 14px; max-height: 0; overflow: hidden; transition: max-height 0.25s ease; }
.tree-chapters.open { max-height: 2000px; }
.tree-chapter { padding: 3px 8px; cursor: pointer; border-radius: var(--radius-sm); font-size: var(--font-size-sm); color: var(--text-secondary); display: flex; justify-content: space-between; align-items: center; }
.tree-chapter:hover { background: var(--bg-tertiary); color: var(--text-primary); }
.tree-chapter.active { background: var(--accent-dim); color: var(--accent); }
.tree-actions { display: flex; gap: var(--space-xs); margin-top: 2px; }
.tree-actions button { padding: 1px 4px; font-size: var(--font-size-xs); background: transparent; border: 1px solid var(--border-color); color: var(--text-muted); border-radius: 2px; cursor: pointer; }
.tree-actions button:hover { color: var(--text-primary); border-color: var(--border-light); }
.tree-add-btn { font-size: var(--font-size-xs); padding: 3px 8px; margin: 4px 6px; background: transparent; border: 1px dashed var(--border-color); color: var(--text-muted); border-radius: var(--radius-xs); cursor: pointer; width: calc(100% - 12px); text-align: left; }
.tree-add-btn:hover { color: var(--text-secondary); border-color: var(--border-light); }
.tree-outline-node { padding: 6px 8px; margin: 2px 0 6px; font-size: var(--font-size-sm); color: var(--text-secondary); background: var(--bg-tertiary); border-radius: var(--radius-sm); cursor: pointer; display: flex; align-items: center; gap: var(--gap); overflow: hidden; white-space: nowrap; text-overflow: ellipsis; }
.tree-outline-node:hover { color: var(--text-primary); background: var(--border-light); }
.tree-outline-node .outline-icon { font-size: var(--font-size-md); flex-shrink: 0; }
.editor-header { flex-wrap: nowrap; gap: var(--gap); overflow-x: auto; padding: 0 var(--space-md); height: 48px; border-bottom: 1px solid var(--border-color); display: flex; justify-content: space-between; align-items: center; background: var(--bg-secondary); flex-shrink: 0; }
.editor-header span { font-size: var(--font-size-sm); color: var(--text-secondary); }
.chat-header { padding: 6px 10px; border-bottom: 1px solid var(--border-color); display: flex; justify-content: space-between; align-items: center; font-size: var(--font-size-sm); color: var(--text-secondary); }
.chat-input-row { display: flex; gap: var(--gap); padding: 6px; border-top: 1px solid var(--border-color); }
.chat-input-row textarea { flex: 1; padding: 6px; background: var(--bg-input); border: 1px solid var(--border-color); color: var(--text-primary); border-radius: var(--radius-sm); font-size: var(--font-size-md); resize: none; outline: none; }
.editor-find-highlight.active { background: var(--warning-bright); }
.chat-empty { display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  height: 100%;
  color: var(--text-secondary);
  gap: var(--space-sm); }
.chat-empty .empty-icon { font-size: var(--font-size-lg); opacity: 0.5; }
.chat-empty p { margin: 0; font-size: var(--font-size-lg); }
.chat-empty .empty-sub { font-size: var(--font-size-sm); opacity: 0.6; }
.breadcrumb-home { color: var(--text-secondary); cursor: pointer; padding: 2px 6px; border-radius: var(--radius-xs); white-space: nowrap; }
.breadcrumb-home:hover { background: var(--bg-tertiary); color: var(--text-primary); }
.breadcrumb-sep { color: var(--text-muted); margin: 0 2px; }
.breadcrumb-item { display: flex; align-items: center; gap: var(--gap);
  padding: 2px 8px; border-radius: var(--radius-xs); cursor: pointer; white-space: nowrap;
  background: var(--bg-tertiary); color: var(--text-secondary); }
.breadcrumb-item:hover { background: var(--accent-dim); color: var(--accent); }
.breadcrumb-item .bc-close { font-size: var(--font-size-md); line-height: 1; opacity: 0.5; }
.breadcrumb-item:hover .bc-close { opacity: 1; }
.chat-input-row { display: flex; gap: var(--gap); padding: 6px 8px;
  border-top: 1px solid var(--border-color); }
.chat-input-row textarea { flex: 1; background: var(--bg-primary); color: var(--text-primary);
  border: 1px solid var(--border-color); border-radius: var(--radius-sm);
  padding: var(--space-xs) var(--space-sm); font-size: var(--font-size-sm); resize: none;
  font-family: inherit; }
.tab-item.active { color: var(--accent); border-bottom-color: var(--accent); }
.editor-header { gap: var(--space-xs); }
.header-selector { max-width: 100px !important; }
.sidebar-btn { width: 48px; height: 48px; }
.editor-header { gap: var(--space-sm); }
.editor-header button { padding: 2px 4px; font-size: var(--font-size-xs); }
.header-selector, .agent-selector { max-width: 80px !important; }
.tree-volume-header { position: relative; }
.tree-volume-header:hover .tree-vol-gen { display: inline-block; }
.tree-vol-gen { display: none;
  position: absolute;
  right: 4px;
  top: 50%;
  transform: translateY(-50%);
  font-size: var(--font-size-xs);
  padding: 1px 4px;
  background: var(--accent, var(--info));
  color: var(--text-on-accent);
  border: none;
  border-radius: var(--radius-xs);
  cursor: pointer; }
.tree-gen-btn { display: block; width: 100%; padding: var(--space-xs) var(--space-sm); margin-top: 2px;
  background: var(--accent-soft, var(--accent-dim)); color: var(--text-primary);
  border: 1px solid var(--border-color); border-radius: var(--radius-xs); font-size: var(--font-size-xs); cursor: pointer;
  transition: background 0.15s; }
.tree-gen-btn:hover { background: var(--accent-primary, var(--accent)); color: var(--text-on-accent); }
.tree-actions { display: flex; gap: var(--gap); padding: var(--space-xs) var(--space-md); background: var(--bg-tertiary);
  border-bottom: 1px solid var(--border-color); font-size: var(--font-size-xs); color: var(--text-secondary);
  align-items: center; overflow: hidden; white-space: nowrap; }
.tree-actions button[data-a="gen-body"] { background: none; border: none; color: var(--accent-primary, var(--accent)); cursor: pointer;
  font-size: var(--font-size-sm); padding: 0 2px; margin-right: 2px; }
.tree-actions button[data-a="gen-body"]:hover { color: var(--accent-strong, var(--accent-active)); }
.breadcrumb-item { transition: all 0.2s ease; }
.breadcrumb-item:hover { box-shadow: var(--shadow-xs); }
.sidebar-divider { background: linear-gradient(90deg, transparent, var(--border-light), transparent); }
.chat-input-row textarea { border-radius: var(--radius-sm);
  transition: all 0.2s ease; }
.chat-input-row textarea:focus { border-color: var(--accent);
  box-shadow: 0 0 0 2px var(--accent-dim); }
.editor-toolbar { display: flex; gap: var(--space-sm); align-items: center; flex-wrap: nowrap; overflow-x: auto; }
.editor-toolbar-group { display: flex; gap: var(--space-xs); align-items: center; flex-shrink: 0; }
.editor-toolbar-sep { width: 1px; height: 18px; background: var(--border-light); margin: 0 var(--space-xs);
  flex-shrink: 0; opacity: 0.5; }
.editor-toolbar-group .btn-sm { white-space: nowrap; }
.tab-hidden { display: none; }
.editor-toolbar-group-inline { display: flex; gap: var(--gap); }
.tree-node { padding: var(--space-xs) var(--space-sm); border-radius: var(--radius-sm);
  transition: all 0.2s ease; cursor: pointer; }
.tree-node:hover { background: var(--bg-tertiary); }
.tree-node.active { background: var(--accent-dim); }
.tree-node-actions { opacity: 0; transition: opacity 0.2s; }
.tree-node:hover .tree-node-actions { opacity: 1; }
.sidebar-btn:not(:disabled):active, .btn-sm:not(:disabled):active { transform: scale(0.97); }
.sidebar-btn:focus-visible, input:focus-visible, select:focus-visible, textarea:focus-visible { outline: 2px solid var(--accent); outline-offset: 2px; }
.sidebar-btn:not(:disabled):hover { background: var(--bg-hover); color: var(--accent); }
.sidebar-btn:not(:disabled):active { transform: scale(0.92); }
.sidebar-btn.active { color: var(--accent); background: var(--accent-dim); }
.sidebar-btn svg { transition: transform var(--transition); }
.sidebar-btn:hover svg { transform: scale(1.1); }
.sidebar-btn.active::before { content: ''; position: absolute; left: 0; top: 20%; bottom: 20%; width: 3px; border-radius: 0 2px 2px 0; background: var(--accent); }
.editor-toolbar-sep { width: 1px; height: 18px; background: var(--border-color); margin: 0 var(--space-xs); flex-shrink: 0; }
.editor-toolbar { flex-wrap: wrap; }
.sidebar-btn { width: 36px; height: 36px; }
.header-selector { max-width: 100px; }
.sidebar-btn { width: 32px; height: 32px; }
.tree-node { border-radius: var(--radius-sm); margin: 1px var(--space-xs); padding: 4px var(--space-sm); transition: background 0.12s ease, color 0.12s ease; position: relative; }
.tree-node:hover { background: var(--bg-hover); }
.tree-node.active { background: var(--accent-dim); color: var(--accent); font-weight: 500; }
.tree-node.active::before { content: ''; position: absolute; left: 0; top: 4px; bottom: 4px; width: 2px; border-radius: 1px; background: var(--accent); }
.tree-node-content { display: flex; align-items: center; gap: var(--gap); overflow: hidden; }
.tree-node-label { overflow: hidden; text-overflow: ellipsis; white-space: nowrap; font-size: var(--font-size-sm); }
.tree-toggle { width: var(--space-md); height: var(--space-md); display: inline-flex; align-items: center; justify-content: center; cursor: pointer; color: var(--text-muted); transition: transform var(--transition-fast); font-size: var(--font-size-xs); flex-shrink: 0; }
.tree-toggle.expanded { transform: rotate(90deg); }
.tree-children { margin-left: 12px; border-left: 1px solid var(--border-color); }
.editor-toolbar { background: var(--bg-secondary); border-bottom: 1px solid var(--border-color); padding: var(--space-xs) var(--space-sm); display: flex; align-items: center; gap: var(--space-xs); flex-shrink: 0; min-height: 36px; }
.editor-toolbar button { width: 28px; height: 28px; display: flex; align-items: center; justify-content: center; background: transparent; border: none; color: var(--text-secondary); cursor: pointer; border-radius: var(--radius-xs); transition: all 0.12s ease; font-size: var(--font-size-md); }
.editor-toolbar button:hover { background: var(--bg-hover); color: var(--accent); }
.editor-toolbar button:active { transform: scale(0.92); }
.editor-toolbar button.active { background: var(--accent-dim); color: var(--accent); }
.chat-msg { max-width: 85%; padding: var(--space-sm) var(--space-md); border-radius: var(--radius-lg); word-wrap: break-word; word-break: break-word; line-height: 1.6; font-size: var(--font-size-sm); animation: fadeIn 0.2s ease; }
.chat-msg.user { align-self: flex-end; background: var(--user-bubble); color: var(--text-primary); }
.chat-msg.ai { align-self: flex-start; background: var(--ai-bubble); color: var(--text-primary); border: 1px solid var(--border-color); }
.chat-msg.system { align-self: center; background: transparent; color: var(--text-muted); font-size: var(--font-size-xs); padding: var(--space-xs) var(--space-md); }
.editor-toolbar-group { display: flex;
  align-items: center;
  gap: var(--space-1); }
.editor-toolbar-sep { width: 1px;
  align-self: stretch;
  background: var(--border-color);
  margin: 0 var(--space-2);
  flex-shrink: 0; }
.editor-toolbar button { height: var(--btn-sm-height);
  width: var(--btn-sm-height);
  white-space: nowrap;
  flex-shrink: 0; }
.editor-toolbar button:not(:disabled):active { transform: var(--tf-press); }
.tree-body,
#chat-messages,
.pl-body,
.sc-items-area { overscroll-behavior: contain; }
.tree-body .tree-item,
.sc-items-list .sc-item,
.mem-list .mem-item { content-visibility: auto;
  contain-intrinsic-size: 40px; }
.editor-header { height: 48px;
  display: flex;
  align-items: center;
  gap: var(--space-2, 4px);
  padding: 0 var(--space-4, 8px);
  border-bottom: 1px solid var(--border-color); }
.editor-header button { height: 28px;
  min-width: 28px;
  padding: 0 var(--space-2, 4px);
  border-radius: var(--radius-sm, 6px);
  user-select: none;
  white-space: nowrap;
  display: inline-flex;
  align-items: center;
  justify-content: center; }
.sidebar-btn { width: 36px;
  height: 36px;
  border-radius: var(--radius-sm, 6px);
  display: flex;
  align-items: center;
  justify-content: center;
  margin: 0 var(--space-1, 2px);
  transition: background var(--t-normal, 0.15s ease), color var(--t-normal, 0.15s ease);
  position: relative;
  user-select: none;
  cursor: pointer;
  border: none;
  background: transparent;
  color: var(--text-secondary); }
.sidebar-btn:hover { background: var(--bg-hover);
  color: var(--accent); }
.sidebar-btn.active { background: var(--accent-dim);
  color: var(--accent); }
.sidebar-btn.active::before { content: '';
  position: absolute;
  left: 0;
  top: 6px;
  bottom: 6px;
  width: 2px;
  background: var(--accent);
  border-radius: 0 2px 2px 0; }
.sidebar-btn svg { width: 20px;
  height: 20px;
  flex-shrink: 0; }
.sidebar-divider { height: 1px;
  background: var(--border-color);
  margin: var(--space-1, 2px) var(--space-2, 4px); }
.tree-header { display: flex;
  align-items: center;
  gap: var(--space-2, 4px);
  padding: var(--space-2, 4px) var(--space-3, 6px);
  border-bottom: 1px solid var(--border-color);
  height: 36px; }
.tree-body { flex: 1;
  overflow-y: auto;
  overscroll-behavior: contain; }
.tree-item, .tree-node { padding: var(--space-1, 2px) var(--space-2, 4px);
  border-radius: var(--radius-xs, 4px);
  display: flex;
  align-items: center;
  gap: var(--space-1, 2px);
  cursor: pointer;
  transition: background var(--t-normal, 0.15s ease);
  user-select: none; }
.tree-item:hover, .tree-node:hover { background: var(--bg-hover); }
.tree-item.active, .tree-node.active { background: var(--accent-dim);
  color: var(--accent); }
.tree-toggle { transition: transform var(--t-normal, 0.15s ease); }
.tree-item.expanded > .tree-toggle,
.tree-node.expanded > .tree-toggle { transform: rotate(90deg); }
.chat-msg { display: flex;
  gap: var(--space-2, 4px);
  margin-bottom: var(--space-3, 6px);
  align-items: flex-start; }
.breadcrumb-home { cursor: pointer;
  color: var(--text-secondary);
  transition: color var(--t-normal, 0.15s ease); }
.breadcrumb-home:hover { color: var(--accent); }
.breadcrumb-sep { color: var(--text-muted);
  margin: 0 var(--space-1, 2px); }
.breadcrumb-item { color: var(--text-primary);
  cursor: pointer; }
.toolbar) { display: inline-flex;
  align-items: center;
  flex-wrap: wrap; }
.toolbar).is-horizontal { flex-direction: row;
  gap: var(--btn-group-gap-standard, 8px); }
.toolbar).is-compact { gap: var(--btn-group-gap-compact, 4px); }
.toolbar).is-spacious { gap: var(--btn-group-gap-spacious, 12px); }
.toolbar).is-vertical { flex-direction: column;
  gap: var(--btn-group-gap-v-normal, 8px); }
.toolbar:not(.is-vertical)) { flex-direction: row;
  gap: var(--btn-group-gap-standard, 8px); }
.toolbar).is-vertical.is-compact { gap: var(--btn-group-gap-v-tight, 4px); }
.toolbar) > :is(.btn-divider, .divider, [role="separator"]) { width: var(--btn-divider-width, 1px);
  align-self: center;
  border: none;
  border-left: var(--btn-divider-width, 1px) solid var(--border-color);
  margin: 0 var(--btn-divider-margin, 4px); }
.toolbar).is-vertical > :is(.btn-divider, .divider, [role="separator"]) { width: auto;
  height: var(--btn-divider-width, 1px);
  border-left: none;
  border-top: var(--btn-divider-width, 1px) solid var(--border-color);
  margin: var(--btn-divider-margin, 4px) 0; }
.toolbar).is-connected { gap: 0; }
.toolbar).is-connected > :is(.btn, button):not(:first-child) { border-top-left-radius: 0;
  border-bottom-left-radius: 0; }
.toolbar).is-connected > :is(.btn, button):not(:last-child) { border-top-right-radius: 0;
  border-bottom-right-radius: 0; }
.editor-header, .editor-toolbar) { gap: var(--toolbar-gap, 2px); }
.editor-header, .editor-toolbar) button,
:is(.editor-header, .editor-toolbar) .btn { height: 28px;
  min-width: 28px;
  padding: 0 var(--space-2, 4px);
  border-radius: var(--radius-sm, 6px);
  margin: 0;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  transition: filter 0.12s ease, transform 0.12s ease, background 0.12s ease; }
.editor-header, .editor-toolbar).is-connected button,
:is(.editor-header, .editor-toolbar).is-connected .btn { border-right: 1px solid var(--border-color);
  border-radius: 0; }
.editor-header, .editor-toolbar).is-connected button:last-child,
:is(.editor-header, .editor-toolbar).is-connected .btn:last-child { border-right: none; }
.editor-header, .editor-toolbar) button:hover,
:is(.editor-header, .editor-toolbar) .btn:hover { filter: brightness(1.1);
  transform: translateY(-1px); }
.editor-header, .editor-toolbar) button:active,
:is(.editor-header, .editor-toolbar) .btn:active { filter: brightness(0.95);
  transform: translateY(0);
  box-shadow: inset 0 1px 3px var(--bg-glass); }
.sidebar-btn, #app-sidebar button) { width: 36px;
  height: 36px;
  min-width: 36px;
  padding: 0;
  margin: 0 var(--toolbar-gap, 2px);
  border-radius: var(--radius-sm, 6px);
  display: flex;
  align-items: center;
  justify-content: center;
  position: relative;
  border: none;
  background: transparent;
  color: var(--text-secondary);
  transition: background 0.12s ease, color 0.12s ease; }
.sidebar-btn, #app-sidebar button):hover { background: var(--bg-hover);
  color: var(--accent);
  transform: none; }
.sidebar-btn, #app-sidebar button).active { background: var(--accent-dim);
  color: var(--accent); }
.sidebar-btn, #app-sidebar button).active::before { content: '';
  position: absolute;
  left: 0;
  top: 6px;
  bottom: 6px;
  width: 3px;
  background: var(--accent);
  border-radius: 0 3px 3px 0; }
.sidebar-btn, #app-sidebar button) svg { width: 20px;
  height: 20px;
  flex-shrink: 0; }
.sidebar-btn, #app-sidebar button, .editor-header button, .editor-toolbar button))):hover { filter: brightness(1.1);
  transform: translateY(-1px); }
.editor-header { height: 48px; display: flex; align-items: center; gap: var(--gap); padding: 0 var(--space-md); border-bottom: 1px solid var(--border-color); background: var(--bg-secondary); overflow-x: auto; flex-shrink: 0; }
.editor-header button { height: 28px; min-width: 28px; padding: 0 6px; border-radius: var(--radius-xs); font-size: var(--font-size-sm); border: none; background: transparent; color: var(--text-secondary); cursor: pointer; display: inline-flex; align-items: center; justify-content: center; transition: all 0.12s ease-out; flex-shrink: 0; }
.editor-header button:hover { background: var(--bg-tertiary); color: var(--text-primary); transform: translateY(-1px); }
.editor-header button:active { transform: translateY(0); box-shadow: inset 0 1px 2px var(--bg-glass); }
.editor-header .toolbar-divider { width: 1px; height: 20px; background: var(--border-color); margin: 0 var(--space-xs); flex-shrink: 0; }
.sidebar-btn { width: 36px; height: 36px; border-radius: var(--radius); margin: 2px auto; border: none; background: transparent; color: var(--text-muted); cursor: pointer; display: flex; align-items: center; justify-content: center; font-size: var(--font-size-md); position: relative; transition: all 0.15s ease-out; flex-shrink: 0; }
.sidebar-btn:hover { background: var(--bg-tertiary); color: var(--text-primary); }
.sidebar-btn.active { background: var(--accent-dim); color: var(--accent); }
.sidebar-btn.active::before { content: ""; position: absolute; left: -2px; top: 6px; bottom: 6px; width: 3px; border-radius: 2px; background: var(--accent); }
.sidebar-btn::after { content: attr(data-tooltip); position: absolute; left: calc(100% + 8px); top: 50%; transform: translateY(-50%); background: var(--bg-tertiary); border: 1px solid var(--border-color); padding: var(--space-xs) var(--space-sm); border-radius: var(--radius-xs); font-size: var(--font-size-sm); white-space: nowrap; opacity: 0; pointer-events: none; transition: opacity 0.15s; z-index: 100; color: var(--text-primary); }
.sidebar-btn:hover::after { opacity: 1; }
.tree-node { padding: var(--space-xs) var(--space-sm); border-radius: var(--radius-xs); margin-bottom: 1px; cursor: pointer; font-size: var(--font-size-sm); color: var(--text-secondary); display: flex; align-items: center; gap: var(--gap); transition: background 0.12s ease-out; }
.tree-node:hover { background: var(--bg-secondary); }
.tree-node.active, .tree-node.selected { background: var(--accent-dim); color: var(--accent); font-weight: 500; }
.tree-node .toggle-arrow { width: 12px; height: 12px; display: inline-flex; align-items: center; justify-content: center; transition: transform 0.15s ease-out; flex-shrink: 0; font-size: var(--font-size-xs); color: var(--text-muted); }
.tree-node.collapsed .toggle-arrow { transform: rotate(-90deg); }
.tree-node.volume-node { font-weight: 600; color: var(--text-primary); }
.tree-node.chapter-node { font-weight: 400; padding-left: 24px; }
.tree-children { overflow: hidden; transition: max-height 0.2s ease-out; }
.chat-bubble { max-width: 80%; padding: 8px 14px; font-size: var(--font-size-sm); line-height: 1.6; word-break: break-word; animation: bubbleIn 0.2s ease-out; }
.chat-bubble.user { align-self: flex-end; background: var(--accent); color: var(--text-on-accent); border-radius: var(--radius-lg) 12px 2px 12px; }
.chat-bubble.ai { align-self: flex-start; background: var(--bg-secondary); color: var(--text-primary); border-radius: var(--radius-lg) 12px 12px 2px; border: 1px solid var(--border-color); }
.chat-bubble.ai pre, .chat-bubble.ai code { background: var(--bg-tertiary); border-radius: var(--radius-xs); padding: var(--space-sm); font-family: Consolas, Monaco, monospace; font-size: var(--font-size-sm); overflow-x: auto; margin: var(--space-xs) 0; }
.chat-bubble .timestamp { font-size: var(--font-size-xs); color: var(--text-muted); margin-top: 4px; opacity: 0.7; }
.breadcrumb { height: 36px; border-bottom: 1px solid var(--border-color); padding: 0 var(--space-md); display: flex; align-items: center; gap: var(--gap); font-size: var(--font-size-sm); flex-shrink: 0; background: var(--bg-secondary); }
.breadcrumb .crumb { color: var(--accent); cursor: pointer; transition: opacity 0.12s; }
.breadcrumb .crumb:hover { text-decoration: underline; opacity: 0.8; }
.breadcrumb .crumb.current { color: var(--text-primary); font-weight: 500; cursor: default; }
.breadcrumb .separator { color: var(--text-muted); margin: 0 2px; }
.editor-header,
 .editor-toolbar { display: flex;
   align-items: center;
   gap: var(--space-1, 2px);
   padding: 0 var(--space-4, 8px);
   height: 48px;
   min-height: 48px;
   border-bottom: 1px solid var(--border-color);
   background: var(--bg-secondary, var(--bg-secondary)); }
.editor-header button,
 .editor-toolbar button { height: 28px;
   min-width: 28px;
   padding: 0 var(--space-2, 4px);
   border-radius: var(--radius-sm, 6px);
   border: none;
   background: transparent;
   color: var(--text-secondary);
   cursor: pointer;
   display: inline-flex;
   align-items: center;
   justify-content: center;
   transition: background var(--transition-fast, 0.12s ease), color var(--transition-fast, 0.12s ease);
   user-select: none;
   white-space: nowrap;
   font-size: var(--font-size-xs, 12px); }
.editor-header button:hover,
 .editor-toolbar button:hover { background: var(--bg-hover, var(--accent-dim));
   color: var(--accent); }
.editor-header button:active,
 .editor-toolbar button:active { background: var(--accent-dim, var(--accent-dim));
   transform: var(--tf-press, scale(0.97)); }
.tree-node,
 .tree-item { padding: var(--space-1, 2px) var(--space-2, 4px) var(--space-1, 2px) var(--space-4, 8px);
   border-radius: var(--radius-sm, 6px);
   cursor: pointer;
   display: flex;
   align-items: center;
   gap: var(--space-1, 2px);
   transition: background var(--transition-fast, 0.12s ease);
   position: relative;
   user-select: none; }
.tree-node:hover,
 .tree-item:hover { background: var(--bg-hover, var(--accent-dim)); }
.tree-node.active,
 .tree-item.active,
 .tree-node.active::before,
 .tree-item.active::before { background: var(--accent-dim, var(--accent-dim)); }
.tree-node.active::before,
 .tree-item.active::before { content: '';
   position: absolute;
   left: 0;
   top: 4px;
   bottom: 4px;
   width: 2px;
   border-radius: 1px;
   background: var(--accent); }
.tree-node .arrow,
 .tree-item .arrow,
 .tree-arrow { transition: transform var(--transition-fast, 0.12s ease);
   cursor: pointer;
   opacity: 0.6; }
.tree-node .arrow:hover,
 .tree-item .arrow:hover,
 .tree-arrow:hover { opacity: 1; }
.tree-actions { opacity: 0;
   transition: opacity var(--transition-fast, 0.12s ease); }
.tree-node:hover .tree-actions,
 .tree-item:hover .tree-actions { opacity: 1; }
.tree-actions button { width: 20px;
   height: 20px;
   border-radius: var(--radius-xs, 4px);
   border: none;
   background: transparent;
   color: var(--text-muted);
   cursor: pointer;
   display: inline-flex;
   align-items: center;
   justify-content: center;
   font-size: var(--font-size-xs, 12px);
   transition: background var(--transition-fast, 0.12s ease), color var(--transition-fast, 0.12s ease); }
.tree-actions button:hover { background: var(--bg-tertiary, var(--bg-tertiary));
   color: var(--accent); }
.chat-message { animation: bubbleIn var(--transition, 0.2s ease) ease-out; }
.chat-input-area,
 .chat-input,
 #chat-input-container { position: sticky;
   bottom: 0;
   background: var(--bg-glass, var(--bg-glass));
   backdrop-filter: var(--blur, blur(8px));
   -webkit-backdrop-filter: var(--blur, blur(8px));
   padding: var(--space-2, 4px) var(--space-4, 8px);
   border-top: 1px solid var(--border-color);
   z-index: 2; }
.chat-send-btn,
 #chat-send,
 .btn-send { border-radius: var(--radius-sm, 6px);
   border: none;
   background: var(--accent-gradient, linear-gradient(135deg, var(--accent) 0%, var(--accent-lighter) 100%));
   color: var(--text-on-accent);
   cursor: pointer;
   min-width: 36px;
   height: 36px;
   display: inline-flex;
   align-items: center;
   justify-content: center;
   transition: transform var(--transition-fast, 0.12s ease), box-shadow var(--transition-fast, 0.12s ease); }
.chat-send-btn:hover,
 #chat-send:hover,
 .btn-send:hover { transform: var(--tf-lift, translateY(-1px));
   box-shadow: var(--shadow-accent-sm, 0 2px 8px var(--accent-glow)); }
.chat-send-btn:active,
 #chat-send:active,
 .btn-send:active { transform: var(--tf-press, scale(0.97)); }
.tree-header { height: 40px;
   min-height: 40px;
   display: flex;
   align-items: center;
   padding: 0 var(--space-md, 16px);
   border-bottom: 1px solid var(--border-color);
   background: var(--bg-secondary, var(--bg-secondary));
   font-size: var(--font-size-sm, 13px);
   font-weight: var(--fw-semibold, 600);
   color: var(--text-secondary);
   user-select: none; }
.breadcrumb { display: flex;
  align-items: center;
  gap: var(--space-1, 2px);
  font-size: var(--font-size-xs, 12px);
  color: var(--text-muted); }
.breadcrumb-item { cursor: pointer;
  padding: 2px var(--space-sm, 8px);
  border-radius: var(--radius-xs, 4px);
  transition: background var(--transition-fast, 0.12s ease), color var(--transition-fast, 0.12s ease); }
.breadcrumb-item:hover { background: var(--bg-hover, var(--accent-dim));
  color: var(--accent); }
.breadcrumb-item.current { color: var(--text-primary);
  cursor: default; }
.breadcrumb-item.current:hover { background: none; }
.breadcrumb-separator { color: var(--text-muted);
  opacity: 0.5; }
.tree-node,
.card-item { content-visibility: auto;
  contain-intrinsic-size: 40px; }
.chat-messages { overscroll-behavior: contain; }
.chat-header > span { display: inline-flex !important;
  align-items: center !important;
  height: 32px !important;
  font-size: 13px !important;
  font-weight: 500 !important;
  color: var(--text-primary) !important;
  white-space: nowrap !important; }
.chat-header { height: 48px !important;
  max-height: 48px !important;
  flex-wrap: nowrap !important; }
.editor-toolbar { gap: var(--space-xs);
  padding: var(--space-xs) var(--space-sm);
  border-bottom: 1px solid var(--border-color);
  background: var(--bg-elevated); }
.editor-toolbar button,
.editor-toolbar .toolbar-btn { border-radius: var(--radius-sm);
  transition: all 0.12s ease;
  position: relative; }
.editor-toolbar button:hover,
.editor-toolbar .toolbar-btn:hover { background: var(--accent-dim);
  color: var(--accent);
  transform: translateY(-1px); }
.editor-toolbar button:active,
.editor-toolbar .toolbar-btn:active { transform: translateY(0); }
.editor-divider { width: 1px;
  height: 20px;
  background: var(--border-color);
  margin: 0 var(--space-xs);
  flex-shrink: 0; }
.chat-messages .msg-ai,
.chat-messages .msg-user,
.chat-messages .message-ai,
.chat-messages .message-user { animation: bubbleIn 0.3s cubic-bezier(0.16, 1, 0.3, 1);
  max-width: 85%;
  word-wrap: break-word; }
.chat-messages .msg-ai,
.chat-messages .message-ai { background: var(--bg-elevated);
  border: 1px solid var(--border-color);
  border-radius: var(--radius-md) var(--radius-md) var(--radius-md) 2px; }
.chat-messages .msg-user,
.chat-messages .message-user { background: var(--accent-dim);
  color: var(--text-primary);
  border-radius: var(--radius-md) var(--radius-md) 2px var(--radius-md); }
.chat-messages .msg-system,
.chat-messages .message-system { font-size: var(--font-size-xs);
  color: var(--text-muted);
  text-align: center;
  padding: 4px 0; }
.tree-node { transition: background 0.12s ease, border-left-color 0.12s ease;
  border-left: 2px solid transparent; }
.tree-node:hover { background: var(--bg-hover);
  border-left-color: var(--accent-dim); }
.tree-node.active,
.tree-node.selected { background: var(--accent-dim);
  border-left-color: var(--accent);
  color: var(--text-primary); }
.tree-node .tree-toggle { transition: transform var(--transition-fast);
  display: inline-flex;
  align-items: center;
  justify-content: center;
  width: var(--space-md);
  height: var(--space-md);
  flex-shrink: 0; }
.tree-node.collapsed .tree-toggle { transform: rotate(-90deg); }
.tree-children { overflow: hidden;
  transition: max-height var(--transition-slow); }
.tree-actions { opacity: 0;
  transition: opacity var(--transition-fast); }
.tree-node:hover .tree-actions { opacity: 1; }
.tab-bar,
.tabs { display: flex;
  gap: 0;
  border-bottom: 1px solid var(--border-color);
  background: var(--bg-secondary);
  overflow-x: auto;
  scrollbar-width: thin; }
.tab-bar .tab,
.tabs .tab { padding: var(--space-xs) var(--space-md);
  cursor: pointer;
  color: var(--text-secondary);
  font-size: var(--font-size-sm);
  border-bottom: 2px solid transparent;
  transition: all 0.12s ease;
  white-space: nowrap;
  flex-shrink: 0; }
.tab-bar .tab:hover,
.tabs .tab:hover { color: var(--text-primary);
  background: var(--bg-hover); }
.tab-bar .tab.active,
.tabs .tab.active { color: var(--accent);
  border-bottom-color: var(--accent);
  font-weight: var(--fw-semibold); }
.sidebar-nav .nav-item { transition: all 0.12s ease;
  border-left: 2px solid transparent;
  border-radius: 0 var(--radius-sm) var(--radius-sm) 0; }
.sidebar-nav .nav-item:hover { background: var(--bg-hover);
  border-left-color: var(--accent-dim); }
.sidebar-nav .nav-item.active { background: var(--accent-dim);
  border-left-color: var(--accent);
  color: var(--text-primary); }
.tree-add-btn,
.tree-gen-btn,
.provider-card-edit,
.tree-actions button { border-radius: var(--radius-sm) !important;
  transition: all 0.12s ease; }
.tree-add-btn:hover { border-color: var(--accent) !important;
  color: var(--accent) !important;
  background: var(--accent-dim) !important; }
.tree-gen-btn:hover { border-color: var(--accent) !important;
  box-shadow: var(--shadow-accent-xs); }
.tree-actions button { width: 20px;
  height: 20px;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  font-size: var(--font-size-xs); }
.tree-actions button:hover { background: var(--accent-dim);
  color: var(--accent); }
.sidebar,
.left-sidebar { min-width: 200px;
  max-width: 280px;
  transition: width var(--transition-slow), min-width var(--transition-slow); }
.breadcrumb-home,
.breadcrumb-item,
.breadcrumb-sep,
.bc-close { display: inline-flex;
  align-items: center;
  white-space: nowrap; }
.breadcrumb-home { gap: var(--space-xs);
  padding: var(--space-xs) var(--space-sm);
  border: none;
  border-radius: var(--radius-sm);
  background: transparent;
  color: var(--text-secondary);
  font-size: var(--font-size-sm);
  font-weight: var(--fw-medium);
  cursor: pointer;
  transition: background 0.12s ease, color 0.12s ease; }
.breadcrumb-home:hover { background: var(--accent-dim);
  color: var(--accent); }
.breadcrumb-home:active { transform: var(--tf-press); }
.breadcrumb-home:focus-visible { outline: 2px solid var(--accent);
  outline-offset: 1px; }
.breadcrumb-item { padding: var(--space-xs) var(--space-sm);
  border: none;
  border-radius: var(--radius-sm);
  background: transparent;
  color: var(--text-secondary);
  font-size: var(--font-size-sm);
  cursor: pointer;
  transition: background 0.12s ease, color 0.12s ease; }
.breadcrumb-item:hover { background: var(--accent-dim);
  color: var(--text-primary); }
.breadcrumb-item:active { transform: var(--tf-press); }
.breadcrumb-item:focus-visible { outline: 2px solid var(--accent);
  outline-offset: 1px; }
.breadcrumb-sep { color: var(--text-muted);
  font-size: var(--font-size-xs);
  user-select: none;
  padding: 0 var(--space-xs); }
.tree-actions { display: flex;
  align-items: center;
  gap: var(--space-xs);
  flex-wrap: nowrap; }
.tree-actions button { display: inline-flex;
  align-items: center;
  justify-content: center;
  padding: var(--space-xs) var(--space-sm);
  min-height: var(--btn-xs-height);
  font-size: var(--font-size-xs);
  color: var(--text-muted);
  background: transparent;
  border: 1px solid transparent;
  border-radius: var(--radius-sm);
  cursor: pointer;
  transition: background 0.12s ease, color 0.12s ease,
              border-color 0.12s ease; }
.tree-actions button:hover { color: var(--text-primary);
  background: var(--bg-hover);
  border-color: var(--border-light); }
.tree-actions button.btn-delete:hover,
.tree-actions button[data-action="delete"]:hover { color: var(--danger);
  background: var(--danger-dim);
  border-color: transparent; }
.tree-add-btn { display: inline-flex;
  align-items: center;
  gap: var(--space-xs);
  padding: var(--space-xs) var(--space-sm);
  margin: var(--space-xs) var(--space-sm);
  font-size: var(--font-size-xs);
  color: var(--text-muted);
  background: transparent;
  border: 1px dashed var(--border-color);
  border-radius: var(--radius-sm);
  cursor: pointer;
  transition: background 0.12s ease, color 0.12s ease,
              border-color 0.12s ease; }
.tree-add-btn:hover { color: var(--accent);
  background: var(--accent-dim);
  border-color: var(--accent); }
.tree-gen-btn { display: inline-flex;
  align-items: center;
  gap: var(--space-xs);
  padding: var(--space-xs) var(--space-sm);
  font-size: var(--font-size-xs);
  font-weight: var(--fw-medium);
  color: var(--accent);
  background: var(--accent-soft, var(--accent-dim));
  border: 1px solid var(--border-color);
  border-radius: var(--radius-sm);
  cursor: pointer;
  transition: background 0.12s ease, color 0.12s ease,
              border-color 0.12s ease, box-shadow 0.12s ease,
              transform 0.12s ease; }
.tree-gen-btn:hover { color: var(--text-on-accent);
  background: var(--accent);
  border-color: var(--accent);
  box-shadow: var(--shadow-accent-xs); }
.tree-gen-btn:active { transform: var(--tf-press); }
.tree-actions button,
  .tree-add-btn,
  .tree-gen-btn,
  .modal-tab,
  .sc-item-form .form-group input,
  .sc-item-form .form-group textarea,
  .sc-item-form .form-group select,
  .mem-form .form-group input,
  .mem-form .form-group textarea,
  .mem-form .form-group select { transition: none; }
.tree-gen-btn:active { transform: none; }
.tree-add-btn { color: var(--text-secondary, var(--text-secondary)) !important; }
.tree-add-btn:hover { color: var(--accent) !important; }
.tree-gen-btn { color: var(--text-primary, var(--text-primary)) !important; }
.tree-gen-btn:hover { color: var(--text-on-accent) !important; }
.tree-gen-btn { color: var(--text-primary, var(--text-primary)) !important;
  background: var(--bg-tertiary, var(--bg-elevated)) !important;
  border: 1px solid var(--accent-dim, var(--accent-dim)) !important;
  font-weight: var(--fw-medium, 500); }
.tree-gen-btn:hover { color: var(--text-on-accent) !important;
  background: var(--accent, var(--accent)) !important; }
.tree-add-btn, .tree-gen-btn { min-height: 28px !important;
  padding: var(--space-xs, 4px) var(--space-sm, 8px) !important;
  border-radius: var(--radius-btn, 6px) !important;
  font-size: var(--font-size-xs, 12px) !important;
  transition: background 0.12s ease, color 0.12s ease !important; }
.tree-add-btn { border: 1px solid var(--border-color, var(--bg-hover)) !important;
  color: var(--text-secondary, var(--text-secondary)) !important;
  background: var(--bg-tertiary, var(--bg-elevated)) !important; }
.tree-add-btn:hover { background: var(--bg-hover, var(--border-light)) !important;
  color: var(--text-primary, var(--text-primary)) !important;
  border-color: var(--accent-dim, var(--accent-glow)) !important; }
.tree-gen-btn { background: var(--bg-tertiary, var(--bg-elevated)) !important;
  border: 1px solid var(--accent-dim, var(--accent-dim)) !important;
  color: var(--text-primary, var(--text-primary)) !important;
  font-weight: var(--fw-medium, 500) !important; }
.tree-gen-btn:hover { background: var(--accent, var(--accent)) !important;
  color: var(--text-on-accent) !important;
  border-color: var(--accent, var(--accent)) !important; }
.editor-header { padding: var(--space-sm, 8px) var(--space-md, 12px) !important;
  gap: var(--space-sm, 8px) !important;
  border-bottom: 1px solid var(--border-color, var(--bg-hover)) !important; }
.editor-toolbar { gap: var(--space-xs, 4px) !important;
  padding: var(--space-xs, 4px) var(--space-sm, 8px) !important;
  border-radius: var(--radius-md, 8px) !important;
  background: var(--bg-tertiary, var(--bg-elevated)) !important;
  flex-shrink: 1 !important;
  max-width: 60% !important; }
.editor-toolbar-group { gap: var(--space-xxs, 2px) !important;
  display: flex !important;
  align-items: center !important; }
.editor-toolbar-sep { width: 1px !important;
  height: 20px !important;
  background: var(--border-color, var(--bg-hover)) !important;
  margin: 0 var(--space-xs, 4px) !important; }
.breadcrumb-home, .breadcrumb-item { padding: var(--space-xxs, 2px) var(--space-xs, 4px) !important;
  border-radius: var(--radius-xs, 4px) !important;
  transition: background 0.12s ease, color 0.12s ease !important;
  cursor: pointer !important; }
.breadcrumb-item:hover { background: var(--bg-hover, var(--border-light)) !important;
  color: var(--text-primary, var(--text-primary)) !important; }
.breadcrumb-sep { color: var(--text-muted, var(--text-muted)) !important;
  user-select: none !important; }
.chat-header { padding: var(--space-sm, 8px) var(--space-md, 12px) !important;
  background: var(--bg-secondary, var(--bg-input)) !important;
  border-bottom: 1px solid var(--border-color, var(--bg-hover)) !important;
  gap: var(--space-sm, 8px) !important;
  align-items: center !important; }
.tree-add-btn, .tree-gen-btn, .pl-step-status, .pl-step-label { font-size: var(--font-size-xs, 12px) !important; }
.sidebar-btn { position: relative; }
.sidebar-btn::after { content: attr(data-tooltip); position: absolute; left: 100%; top: 50%; transform: translateY(-50%); background: var(--bg-elevated); color: var(--text-primary); padding: 4px 10px; border-radius: var(--radius-sm); font-size: var(--font-size-xs); white-space: nowrap; opacity: 0; pointer-events: none; transition: opacity 0.15s; z-index: var(--z-tooltip); margin-left: 8px; border: 1px solid var(--border-color); box-shadow: var(--shadow-sm); }
.sidebar-btn:hover::after { opacity: 1; }
.tree-node { padding: var(--space-xs) var(--space-sm); margin: 1px 4px; border-radius: var(--radius-sm); transition: background 0.1s; cursor: pointer; }
.tree-node:hover { background: var(--bg-hover); }
.tree-node.active { background: var(--accent-dim); color: var(--accent); }
.tree-actions { display: flex; gap: var(--gap); opacity: 0; transition: opacity 0.15s; }
.tree-node:hover .tree-actions { opacity: 1; }
.tree-actions button { width: auto; min-width: 24px; height: 24px; padding: 2px 6px; font-size: var(--font-size-xs); background: transparent; border: 1px solid var(--border-color); color: var(--text-muted); border-radius: var(--radius-xs); cursor: pointer; transition: all 0.1s; }
.tree-actions button:hover { color: var(--text-primary); border-color: var(--border-light); background: var(--bg-tertiary); }
.chat-messages { flex: 1; overflow-y: auto; padding: var(--space-md); }
.chat-msg { margin-bottom: 12px; padding: 10px 14px; border-radius: var(--radius-md); max-width: 90%; word-wrap: break-word; }
.chat-msg.user { background: var(--user-bubble); margin-left: auto; border-bottom-right-radius: 4px; }
.chat-msg.ai { background: var(--ai-bubble); margin-right: auto; border-bottom-left-radius: 4px; }
.chat-input-area { padding: var(--space-sm) var(--space-md); border-top: 1px solid var(--border-color); display: flex; gap: var(--space-sm); align-items: flex-end; }
.header-selector { height: 32px; padding: 0 var(--space-sm); background-color: var(--bg-input); border: 1px solid var(--border-color); border-radius: var(--radius-sm); color: var(--text-primary); font-size: var(--font-size-sm); cursor: pointer; }
.sidebar-btn { width: 36px; height: 36px; display: flex; align-items: center; justify-content: center; border-radius: var(--radius-sm); border: none; background: transparent; color: var(--text-muted); cursor: pointer; transition: all 0.12s ease; }
.sidebar-btn:hover { background: var(--bg-hover); color: var(--accent); }
.sidebar-btn.active { background: var(--accent-dim); color: var(--accent); }
.sidebar-btn svg { width: 20px; height: 20px; }
.tab-content { display: none; padding: var(--space-md); }
.tab-content.active { display: block; }
.sidebar-btn { transition: all 0.2s ease !important; border-radius: var(--radius) !important; }
.sidebar-btn:hover { background: var(--accent-dim) !important; color: var(--accent-light) !important; transform: translateY(-2px) !important; }
.sidebar-btn.active { background: var(--accent-dim) !important; color: var(--accent) !important; box-shadow: inset 3px 0 0 var(--accent) !important; }
.editor-header { border-bottom: 1px solid var(--border-color) !important;
  background: linear-gradient(180deg, var(--bg-secondary) 0%, var(--bg-tertiary) 100%) !important; }
.tree-node { transition: all 0.15s ease !important; border-radius: var(--radius-sm) !important; }
.tree-node:hover { background: var(--bg-hover) !important; transform: translateX(2px) !important; }
.tree-node.active { background: var(--accent-dim) !important; border-left: 2px solid var(--accent) !important; }
.chat-msg { transition: all 0.2s ease !important; border-radius: var(--radius-md) !important; }
.chat-msg.user { border-left: 3px solid var(--accent) !important; }
.chat-msg.ai { border-left: 3px solid var(--success, var(--success)) !important; }
.editor-header { border-bottom: 1px solid var(--border-color) !important; }
.editor-toolbar-group button:first-child, .editor-toolbar-group button:nth-child(2) { width: 32px !important; min-width: 32px !important; padding: 0 !important; text-align: center !important;
  background: transparent !important; border: 1px solid transparent !important; }
.editor-toolbar-group button:first-child:hover, .editor-toolbar-group button:nth-child(2):hover { background: var(--bg-hover) !important; border-color: var(--border-color) !important; }
.editor-header button[id=btn-generate], .editor-header button:has-text("鐢熸垚") { background: var(--accent) !important; color: var(--text-on-accent) !important; font-weight: 600 !important;
  border: none !important; box-shadow: 0 2px 8px var(--accent-dim) !important; }
.editor-header button:has-text(".md"), .editor-header button:has-text(".txt"), .editor-header button:has-text(".epub") { background: transparent !important; border: 1px solid var(--border-color) !important; color: var(--text-primary) !important; }
.editor-header button:has-text(".md"):hover, .editor-header button:has-text(".txt"):hover, .editor-header button:has-text(".epub"):hover { border-color: var(--accent) !important; color: var(--accent-light) !important; }
.chat-header { border-bottom: 1px solid var(--border-color) !important; background: var(--bg-secondary) !important; }
.chat-input-area { background: var(--bg-secondary) !important; border-top: 1px solid var(--border-color) !important; padding: 10px 12px !important; }
.chat-input-area textarea { background: var(--bg-input) !important; border: 1px solid var(--border-color) !important; border-radius: var(--radius) !important; padding: var(--space-sm) var(--space-md) !important; }
.breadcrumb-item { padding: 2px 8px !important; border-radius: var(--radius-sm) !important; transition: all 0.15s ease !important; }
.breadcrumb-item:hover { background: var(--bg-hover) !important; color: var(--accent-light) !important; }
.breadcrumb-sep { color: var(--text-muted) !important; margin: 0 2px !important; }
.chat-msg.user { background: var(--accent-dim) !important; margin-left: 24px !important; border-left: 3px solid var(--accent) !important; }
.chat-msg.ai { background: var(--bg-tertiary) !important; margin-right: 24px !important; border-left: 3px solid var(--success, var(--success)) !important; }
.tree-node { padding: 4px 8px !important; margin: 1px 4px !important; border-radius: var(--radius-sm) !important; font-size: var(--font-size-sm) !important; transition: all 0.15s ease !important; }
.editor-toolbar button:hover { background: var(--bg-hover) !important; }
.tree-actions had opacity:0 but hover selector used .tree-node instead of .tree-chapter */
.tree-actions { display: flex !important;
  gap: var(--space-xs) !important;
  opacity: 0.5 !important;
  padding: 0 !important;
  background: transparent !important;
  border: none !important;
  transition: opacity 0.15s !important; }
.tree-chapter:hover .tree-actions,
.tree-volume-header:hover .tree-actions,
.tree-item:hover .tree-actions,
.tree-node:hover .tree-actions { opacity: 1 !important; }
.tree-actions button { width: 22px !important;
  height: 22px !important;
  min-width: 22px !important;
  padding: 0 !important;
  font-size: var(--font-size-xs) !important;
  background: transparent !important;
  border: 1px solid transparent !important;
  color: var(--text-muted) !important;
  border-radius: var(--radius-xs) !important;
  cursor: pointer !important;
  transition: all 0.1s !important;
  display: flex !important;
  align-items: center !important;
  justify-content: center !important; }
.tree-actions button:hover { color: var(--text-primary) !important;
  border-color: var(--border-color) !important;
  background: var(--bg-tertiary) !important; }
.tree-actions button[data-a="gen-body"]:hover { color: var(--accent, var(--accent)) !important;
  border-color: var(--accent-glow, var(--accent-glow)) !important;
  background: var(--accent-dim, var(--accent-dim)) !important; }
.tree-actions button[data-a="del-ch"]:hover { color: var(--danger, var(--danger)) !important;
  border-color: var(--danger-dim) !important;
  background: var(--danger-dim) !important; }
.sidebar-btn):not(.btn-close):not(.btn-xs):not(.btn-sm):not(.btn-md):not(.btn-lg):not(.btn-icon):not(.modal-tab):not(.pl-step):not(.tree-actions button):not(.editor-toolbar button):not(.btn-primary):not(.btn-secondary):not(.btn-danger):not(.btn-back-sm):not(.btn-toggle):not(.pl-nav-btn):not(.pl-nav-button):not(.sc-cat-btn):not(.mem-cat-btn):not(.provider-card-edit) { min-height: var(--btn-sm-height) !important;
  padding: 0 14px !important;
  border-radius: var(--radius-sm, 6px) !important;
  font-size: var(--font-size-sm, 13px) !important;
  display: inline-flex !important;
  align-items: center !important;
  justify-content: center !important;
  gap: var(--gap) !important; }
.sidebar-btn { overflow: hidden !important; }
.sidebar-btn svg { width: 20px !important;
  height: 20px !important;
  flex-shrink: 0 !important; }
.tab-content label { font-size: 13px !important;
  font-weight: 500 !important;
  color: var(--text-secondary, var(--text-secondary)) !important;
  margin-bottom: 4px !important;
  display: block !important; }
.tab-content input, .tab-content textarea, .tab-content select { background-color: var(--bg-tertiary, var(--bg-secondary)) !important;
  border: 1px solid var(--accent-dim) !important;
  border-radius: 8px !important;
  padding: var(--space-sm) var(--space-md) !important;
  font-size: 13px !important;
  transition: border-color 0.15s ease !important; }
.tab-content input:focus, .tab-content textarea:focus, .tab-content select:focus { border-color: var(--accent, var(--accent)) !important;
  outline: none !important;
  box-shadow: 0 0 0 2px var(--accent-dim) !important; }
.chat-header { padding: var(--space-sm) var(--space-md) !important;
  border-bottom: 1px solid var(--border-color) !important;
  background: transparent !important; }
.chat-messages { flex: 1 !important;
  overflow-y: auto !important;
  padding: var(--space-md) !important; }
.chat-message { max-width: 85% !important;
  padding: 10px 14px !important;
  border-radius: 12px !important;
  margin-bottom: 8px !important;
  font-size: 13px !important;
  line-height: 1.6 !important;
  word-break: break-word !important; }
.chat-message.user { background: var(--accent-dim) !important;
  color: var(--text-primary, var(--text-primary)) !important;
  margin-left: auto !important;
  border-bottom-right-radius: 4px !important; }
.chat-message.assistant { background: var(--bg-tertiary, var(--bg-secondary)) !important;
  color: var(--text-primary, var(--text-primary)) !important;
  border: 1px solid var(--accent-dim) !important;
  border-bottom-left-radius: 4px !important; }
.chat-input-area { padding: var(--space-sm) var(--space-md) !important;
  border-top: 1px solid var(--accent-dim) !important;
  display: flex !important;
  gap: var(--space-sm) !important;
  align-items: flex-end !important; }
.chat-input-area textarea { flex: 1 !important;
  border: 1px solid var(--accent-dim) !important;
  border-radius: 8px !important;
  padding: var(--space-sm) var(--space-md) !important;
  font-size: 13px !important;
  resize: none !important;
  max-height: 120px !important;
  transition: border-color 0.15s ease !important; }
.chat-input-area textarea:focus { border-color: var(--accent, var(--accent)) !important;
  outline: none !important; }
.editor-toolbar button { min-width: 32px !important;
  height: 28px !important;
  padding: 0 var(--space-sm) !important;
  border-radius: 6px !important;
  font-size: 13px !important;
  display: inline-flex !important;
  align-items: center !important;
  justify-content: center !important;
  gap: var(--gap) !important;
  transition: all 0.15s ease !important; }
.editor-toolbar button:hover { background: var(--accent-dim) !important; }
.tree-header { padding: var(--space-sm) var(--space-md) !important;
  border-bottom: 1px solid var(--accent-dim) !important; }
.tree-item, .tree-volume-header { padding: 6px 12px !important;
  border-radius: 6px !important;
  transition: background 0.15s ease !important; }
.tree-item:hover, .tree-volume-header:hover { background: var(--accent-dim) !important; }
.tree-item.active { background: var(--accent-dim) !important;
  color: var(--accent, var(--accent)) !important; }
.tree-actions { opacity: 0.5 !important;
  transition: opacity 0.15s ease !important; }
.tree-volume-header:hover .tree-actions,
.tree-chapter:hover .tree-actions,
.tree-item:hover .tree-actions { opacity: 1 !important; }
.editor-toolbar { display: flex !important; align-items: center !important; gap: var(--gap) !important; flex-wrap: wrap !important; }
.editor-toolbar-group { display: flex !important; align-items: center !important; gap: var(--gap) !important; padding: 0 4px !important; }
.chat-message.user { background: var(--accent-dim) !important; border: 1px solid var(--accent-glow) !important; margin-left: auto !important; border-bottom-right-radius: 4px !important; }
.chat-message.assistant { background: var(--bg-hover) !important; border: 1px solid var(--bg-hover) !important; margin-right: auto !important; border-bottom-left-radius: 4px !important; }
.chat-input-area { padding: 10px 14px !important; border-top: 1px solid var(--bg-hover) !important; display: flex !important; gap: var(--space-sm) !important; align-items: flex-end !important; }
.chat-input-area textarea { flex: 1 !important; min-height: 36px !important; max-height: 120px !important; padding: var(--space-sm) var(--space-md) !important; background: rgba(0,0,0,0.3) !important; border: 1px solid var(--bg-hover) !important; border-radius: 8px !important; color: var(--text-primary) !important; font-size: 13px !important; resize: none !important; }
.chat-input-area textarea:focus { border-color: var(--border-focus) !important; outline: none !important; box-shadow: 0 0 0 2px var(--accent-dim) !important; }
.chat-input-area button { height: 36px !important; min-width: 36px !important; padding: 0 var(--space-md) !important; border-radius: 8px !important; display: inline-flex !important; align-items: center !important; justify-content: center !important; cursor: pointer !important; border: 1px solid var(--bg-hover) !important; background: var(--accent-glow) !important; color: var(--text-primary) !important; transition: all 0.15s ease !important; }
.chat-input-area button:hover { background: var(--accent-glow) !important; }
.tree-item { display: flex !important; align-items: center !important; gap: var(--space-sm) !important; padding: 6px 10px !important; border-radius: 6px !important; cursor: pointer !important; transition: all 0.12s ease !important; font-size: 13px !important; color: var(--text-primary) !important; }
.tree-item:hover { background: var(--bg-hover) !important; color: var(--text-primary) !important; }
.tree-item.active { background: var(--accent-dim) !important; color: var(--text-primary) !important; }
.tree-actions { display: flex !important; gap: var(--space-xs) !important; margin-left: auto !important; opacity: 0 !important; transition: opacity 0.15s ease !important; }
.tree-item:hover .tree-actions { opacity: 1 !important; }
.tree-actions button { width: 24px !important; height: 24px !important; display: inline-flex !important; align-items: center !important; justify-content: center !important; border: none !important; background: transparent !important; color: var(--text-secondary) !important; cursor: pointer !important; border-radius: var(--radius-sm) !important; font-size: 12px !important; }
.tree-actions button:hover { background: var(--bg-hover) !important; color: var(--text-primary) !important; }
.chat-messages::-webkit-scrollbar-thumb { background: var(--bg-hover) !important; border-radius: 3px !important; }
.editor-toolbar button,
#editor-panel .editor-tools button { height: var(--btn-sm-height) !important;
  min-height: var(--btn-sm-height) !important;
  line-height: var(--btn-sm-height) !important;
  border-radius: var(--radius-btn) !important;
  padding: 0 var(--space-sm) !important;
  font-size: 13px !important;
  white-space: nowrap !important; }
.editor-ai-tool,
.ai-assist-btn { height: var(--btn-sm-height) !important;
  min-height: var(--btn-sm-height) !important;
  line-height: var(--btn-sm-height) !important;
  border-radius: var(--radius-btn) !important;
  padding: 0 var(--space-sm) !important;
  font-size: 12px !important;
  white-space: nowrap !important;
  display: inline-flex !important;
  align-items: center !important;
  gap: var(--gap) !important; }
.tree-btn,
#chapter-tree .tree-action-btn { height: 28px !important;
  min-height: 28px !important;
  line-height: 28px !important;
  padding: 0 var(--space-sm) !important;
  font-size: 12px !important;
  border-radius: var(--radius-btn) !important;
  white-space: nowrap !important; }
.tree-node,
#chapter-tree .tree-chapter,
#chapter-tree .tree-volume { border-radius: 6px !important;
  padding: 4px 8px !important;
  transition: background 0.15s ease !important; }
.editor-toolbar { padding: 6px 12px !important;
  gap: var(--gap) !important;
  border-bottom: 1px solid var(--border-color) !important; }
.chat-message.user { background: var(--accent-dim) !important;
  border: 1px solid var(--accent-glow) !important; }
.chat-message.assistant { background: var(--bg-card, var(--bg-input)) !important;
  border: 1px solid var(--border-color) !important; }
.tree-chapter:hover,
.tree-volume:hover { background: var(--bg-hover); }
.tree-chapter.active,
.tree-volume.active { background: var(--accent-dim);
  color: var(--accent, var(--accent)); }
.editor-toolbar button,
#editor-toolbar button { margin-right: 2px;
  border-radius: var(--radius-xs);
  transition: all 0.15s ease; }
.editor-toolbar button:hover,
#editor-toolbar button:hover { background: var(--bg-hover); }
.chat-message { border-radius: 12px;
  padding: 10px 14px;
  margin-bottom: 8px;
  max-width: 85%; }
.chat-message.user { background: var(--accent-dim);
  margin-left: auto;
  border-bottom-right-radius: 4px; }
.chat-message.assistant { background: var(--bg-card, var(--bg-input));
  margin-right: auto;
  border-bottom-left-radius: 4px;
  border: 1px solid var(--border-color); }
.tree-chapter, .tree-node { border-radius: 6px !important;
  padding: var(--space-sm) var(--space-md) !important;
  cursor: pointer !important;
  transition: all 0.2s ease !important;
  border: 1px solid transparent !important;
  margin-bottom: 2px !important; }
.tree-chapter:hover, .tree-node:hover { background: var(--accent-dim) !important;
  border-color: var(--accent-glow) !important; }
.tree-chapter.active, .tree-node.active { background: var(--accent-dim) !important;
  border-color: var(--accent) !important;
  color: var(--text-bright) !important; }
.tree-chapter-title, .tree-chapter .chapter-title { font-size: var(--font-size-sm) !important;
  color: var(--text-primary) !important;
  white-space: nowrap !important;
  overflow: hidden !important;
  text-overflow: ellipsis !important; }
.tree-volume-header { font-size: var(--font-size) !important;
  font-weight: 600 !important;
  color: var(--text-bright) !important;
  padding: 6px 8px !important;
  border-radius: 6px !important;
  cursor: pointer !important;
  transition: all 0.2s ease !important; }
.tree-volume-header:hover { background: var(--bg-hover) !important; }
.chat-message.user-message, .chat-message.user { background: var(--accent) !important;
  color: var(--text-on-accent) !important;
  margin-left: auto !important;
  border-bottom-right-radius: 4px !important; }
.chat-message.ai-message, .chat-message.assistant { background: var(--bg-card) !important;
  border: 1px solid var(--border-color) !important;
  color: var(--text-primary) !important;
  margin-right: auto !important;
  border-bottom-left-radius: 4px !important; }
.chat-input-area textarea { background: var(--bg-input) !important;
  border: 1px solid var(--border-light) !important;
  border-radius: 8px !important;
  padding: var(--space-sm) var(--space-md) !important;
  font-size: var(--font-size-sm) !important;
  color: var(--text-primary) !important;
  resize: none !important;
  outline: none !important;
  transition: border-color var(--transition) !important; }
.chat-input-area textarea:focus { border-color: var(--accent) !important;
  box-shadow: 0 0 0 2px var(--accent-dim) !important; }
.editor-toolbar, #editor-toolbar { display: flex !important;
  align-items: center !important;
  gap: var(--gap) !important;
  padding: 4px 8px !important;
  background: var(--bg-tertiary) !important;
  border-bottom: 1px solid var(--border-color) !important;
  min-height: 40px !important; }
.editor-toolbar button, #editor-toolbar button { padding: 4px 8px !important;
  border-radius: var(--radius-sm) !important;
  font-size: var(--font-size-sm) !important;
  color: var(--text-secondary) !important;
  background: transparent !important;
  border: none !important;
  cursor: pointer !important;
  transition: all 0.2s ease !important;
  white-space: nowrap !important; }
.editor-toolbar button:hover, #editor-toolbar button:hover { background: var(--bg-hover) !important;
  color: var(--text-bright) !important; }
.editor-toolbar button:active, #editor-toolbar button:active { background: var(--accent-glow) !important; }
.editor-toolbar button { padding: 4px 8px !important;
  margin: 0 !important;
  border-radius: var(--radius-sm) !important;
  font-size: 13px !important;
  color: var(--text-secondary) !important;
  background: transparent !important;
  border: none !important;
  cursor: pointer !important;
  transition: all 0.15s ease !important;
  white-space: nowrap !important;
  min-height: 28px !important;
  display: inline-flex !important;
  align-items: center !important;
  justify-content: center !important; }
.editor-toolbar button:hover { background: var(--accent-dim) !important;
  color: var(--text-bright) !important; }
.editor-toolbar button:active { background: var(--accent-glow) !important; }
.editor-toolbar button + button { margin-left: 0 !important; }
.sidebar-btn.active { background: var(--accent-dim) !important;
  color: var(--accent) !important; }
.sidebar-btn.active::after { content: "" !important;
  position: absolute !important;
  left: 0 !important;
  top: 4px !important;
  bottom: 4px !important;
  width: 3px !important;
  background: var(--accent) !important;
  border-radius: 0 2px 2px 0 !important; }
.breadcrumb-bar { height: 32px !important;
  min-height: 32px !important;
  padding: 0 var(--space-md) !important;
  background: var(--bg-tertiary) !important;
  border-bottom: 1px solid var(--border-color) !important;
  font-size: 13px !important;
  color: var(--text-primary) !important;
  overflow-x: auto !important;
  overflow-y: hidden !important;
  white-space: nowrap !important;
  display: flex !important;
  align-items: center !important;
  gap: var(--gap) !important; }
.breadcrumb-item { cursor: pointer !important;
  transition: color 0.15s !important; }
.breadcrumb-item:hover { color: var(--accent) !important; }
.breadcrumb-sep { color: var(--text-secondary) !important;
  user-select: none !important; }
.chat-messages-area { flex: 1 !important;
  overflow-y: auto !important;
  padding: var(--space-md) !important; }
.chat-bubble.user, .chat-msg.user, .msg-user { background: var(--accent) !important;
  color: var(--text-on-accent) !important;
  margin-left: auto !important;
  border-bottom-right-radius: 4px !important; }
.chat-bubble.assistant, .chat-msg.assistant, .msg-ai { background: var(--bg-card) !important;
  border: 1px solid var(--border-color) !important;
  color: var(--text-primary) !important;
  margin-right: auto !important;
  border-bottom-left-radius: 4px !important; }
.chat-msg .msg-time, .chat-bubble .timestamp { font-size: 10px !important;
  color: var(--text-secondary) !important;
  opacity: 0.7 !important;
  margin-top: 4px !important; }
.chat-input-row { display: flex !important; gap: var(--space-sm) !important; padding: 8px 0 0 0 !important; border-top: 1px solid var(--border-color) !important; margin-top: 8px !important; }
.tree-add-btn { border-radius: 6px !important;
  padding: 4px 10px !important;
  font-size: 11px !important;
  border: 1px dashed var(--border-light) !important;
  background: transparent !important;
  color: var(--text-secondary) !important;
  cursor: pointer !important;
  transition: all 0.15s !important;
  text-align: center !important; }
.tree-add-btn:hover { border-color: var(--accent) !important;
  color: var(--accent) !important;
  background: var(--accent-dim) !important; }
.header-right .btn-icon { width: 32px !important;
  height: 28px !important;
  display: inline-flex !important;
  align-items: center !important;
  justify-content: center !important;
  border-radius: 6px !important;
  background: transparent !important;
  border: none !important;
  color: var(--text-secondary) !important;
  cursor: pointer !important;
  transition: all 0.15s !important; }
.header-right .btn-icon:hover { background: var(--bg-hover) !important;
  color: var(--text-bright) !important; }
.editor-toolbar { width: 100% !important;
  max-width: 100% !important;
  box-sizing: border-box !important;
  padding: 4px 12px !important; }
