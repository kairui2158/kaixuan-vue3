# 设定层宽度纠偏经验

## 错误

只测量子组件宽度，没有递归测量流水线外框、内容区、步骤面板和设定工作区，导致把外层 `32px` 右内边距误认为设定框自身太窄。

## 改正

遇到 UI 空白或尺寸异常时，必须沿父级到子级读取真实 DOM 的矩形、padding、width、max-width 和 flex 属性，再改最接近根因的唯一规则。

## 硬约束

不通过给子组件叠加第三套覆盖样式解决外层空间问题；先定位外层约束，再删除或修改旧规格。改动后必须构建、杀进程、使用 `start-electron.bat` 重启，并用 CDP 复测实际矩形。
