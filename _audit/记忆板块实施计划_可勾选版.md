---

> 基于交接总览（10章）+ 经验文件（10卷规则）融合生成
> 生成时间：2026-08-20
> 使用方式：每完成一项就在 [ ] 前打 [x]，所有项勾完即验收通过

---

## 前置必读（启动前强制）

- [x] 读完了 `docs/记忆板块_任务交接总览.md` 全篇10章（依据：本轮启动读取；摘要记录于 `_audit/DEV_LOG_2026-08-20.md`）
- [x] 读完了 `_audit/神意开发经验总结.md` 全篇10卷规则（依据：本轮启动读取；规则12已执行）
- [x] 确认了 `src/stores/project.ts` 中 `memories` 字段的现有结构（依据：`src/stores/project.ts`）
- [x] 确认了 `src/components/common/MemoryPanel.vue` 的现有UI骨架（依据：P0 代码审查记录）
- [x] 确认了 `src/composables/useAiTools.ts` 的 `callAi` 入口（依据：P1 接口前置审查记录）
- [x] 确认了 `src/components/chat/ChatPanel.vue` 的"相关记忆"注入点（依据：P5 接口前置审查记录）
- [x] 确认了 `package.json` 中已安装的依赖（依据：P0 构建前依赖检查）
- [x] 确认了 `start-electron.bat` 可用，`npx vite build` 可构建（依据：P0 构建与源启动器验证）

---

## 阶段一：数据基础（P0，无依赖，先做）

### P0：扩展 project.ts 记忆 store

**参考**：交接总览 §三-P0（完整数据模型）+ 现有 `src/stores/project.ts` + 经验文件规则2（新架构写法标准）

#### 0.1 确认现有结构
- [x] 读取 `src/stores/project.ts` 中 `memories` 字段的完整定义（依据：`src/stores/project.ts`）
- [x] 确认 `memories` 的类型接口位置（依据：`src/types/memory.ts`）
- [x] 确认 `serialize`/`deserialize` 方法是否覆盖 `memories`（依据：`src/stores/project.ts` 的 `loadProject`/`saveProject`）
- [x] 确认 `saveProject()` 的持久化路径（依据：`src/stores/project.ts` → `storageKey('project_' + currentProjectId)`）

#### 0.2 定义新数据模型
- [x] 在 `src/types/` 下新建 `memory.ts` 定义所有接口（依据：`src/types/memory.ts`）
- [x] 定义 `MemoryEntity` 接口（依据：`src/types/memory.ts`）
- [x] 定义 `MemoryRelation` 接口（依据：`src/types/memory.ts`）
- [x] 定义 `MemoryEvent` 接口（依据：`src/types/memory.ts`）
- [x] 定义 `WorldEntry` 接口（依据：`src/types/memory.ts`）
- [x] 定义 `Foreshadowing` 接口（依据：`src/types/memory.ts`）
- [x] 定义 `MemoryMeta` 接口（依据：`src/types/memory.ts`）
- [x] 定义 `MemoryData` 顶层接口（依据：`src/types/memory.ts`）
- [x] 确认 `categories` 和 `items` 为兼容旧数据保留（依据：`src/types/memory.ts`、`normalizeMemories`）

#### 0.3 扩展 project.ts
- [x] 将 `memories` 初始值改为 `MemoryData` 默认值（依据：`src/stores/project.ts` 的 `createDefaultMemories`）
- [x] 在 `serialize()` 中确保完整序列化（依据：`src/stores/project.ts` 的 `saveProject`）
- [x] 在 `deserialize()` 中增加旧数据兼容逻辑（依据：`normalizeMemories`）
- [x] 添加 `updateMemory()` / `addEntity()` / `addRelation()` / `addEvent()` 等方法（依据：`src/stores/project.ts`）
- [x] 添加 `lockEntityField()` 和 `addToBlacklist()` 方法（依据：`src/stores/project.ts`）

#### 0.4 验证
- [x] 构建成功：`npx vite build` 无报错（依据：P0 构建输出，151 modules）
- [x] 旧数据兼容：启动后读取旧项目，不丢失（依据：P0 CDP 存储 schema 核验）
- [x] 新数据写入并持久化（依据：P0 CDP 新增/保存/删除行为核验）
- [x] 重启后数据一致（依据：P0 源启动器重启后 schema 核验）

---

## 阶段二：核心链路（依赖 P0，P1/P3 可并行）

### P1：新建记忆抽取服务

**参考**：交接总览 §三-P1 + WriterHelper stage5 I2 去重规则 + 经验文件规则1（行为等价）

#### 1.1 新建 `src/services/memoryExtractor.ts`
- [x] 定义 `ExtractionInput` 和 `ExtractionResult` 类型（依据：`src/services/memoryExtractor.ts`）
- [x] 通过注入函数接入现有 `callAi` 统一入口（依据：`MemoryAiCall`，不复制供应商配置）
- [x] 编写抽取 prompt（要求结构化 JSON + 每条带 evidence）（依据：`buildPrompt`）
- [x] 注入 I2 去重约束（只保留正文明确出现或明确推断内容，不臆造）（依据：`buildPrompt`）
- [x] 实现 `extractMemory()` 主函数（依据：`src/services/memoryExtractor.ts`）
- [x] 实现 JSON 校验（失败重试 1 次）（依据：受控测试 `retryOnce: true`）
- [x] 实现 evidence 校验（无效/非当前章节 evidence 的条目丢弃）（依据：受控测试 `evidenceFilter: true`）
- [x] 实现超时保护（默认 30 秒）（依据：受控测试 `timeoutHandled: true`）
- [x] 实现失败策略（不抛异常，返回空结果）（依据：`ExtractionResult` 失败分支）

#### 1.2 验证
- [x] 3 篇样章（短/中/长受控样本）抽取全部通过（依据：`p1_memory_extractor_test.mjs` 输出 `threeSamples: true`）
- [x] 输出 JSON 100% 可解析（依据：3 个正常样本 `success: true`）
- [x] 每条结果都有 evidence（依据：`hasEvidence` + 过滤测试）
- [x] 无效 evidence 的条目不进入结果（依据：`evidence-filter` 输出 `entities: 1`）
- [x] 构建成功（依据：`npm run build:vue` 输出 `151 modules transformed`、`✓ built`）

### P3：增量合并器

**参考**：交接总览 §三-P3（7条合并规则）+ 经验文件规则2（数据单一事实来源）

#### 3.1 新建 `src/services/memoryMerger.ts`
- [x] 实现实体增量合并：新实体新增，已有实体叠加（依据：`src/services/memoryMerger.ts` 的 `mergeMemory` / `mergeEntityList`）
- [x] 实现状态冲突处理：最新章节优先，旧状态写入 `statusHistory`（依据：`mergeEntity`）
- [x] 实现别名合并（依据：实体名称与 aliases 规范化去重）
- [x] 实现锁定字段保护（依据：`lockedFields` 分支产生 skipped change）
- [x] 实现黑名单过滤（依据：实体 id/name 过滤）
- [x] 实现 `mergeRelations` / `mergeEvents` / `mergeForeshadowing` / `mergeWorld`（依据：同文件对应四个合并函数）

#### 3.2 验证
- [x] 连续 20 章模拟，无重复无丢失（依据：Node 原生 TS 受控测试输出 `ok:true, entityCount:1, extractions:19`）
- [x] 锁定字段不被覆盖（依据：同一输出 `description:"初始"`）
- [x] 黑名单条目不出现在结果中（依据：同一输出 `blacklistPresent:false`）
- [x] 构建成功（依据：`npm run build:vue` 输出 `151 modules transformed`、`✓ built`）

> P3 边界：`npm run type-check` 被仓库现有 `vue-tsc`/`typescript` 导出兼容错误阻塞；全量 `tsc` 另有既有 Electron/Vue 声明错误。本次新增服务的窄化错误已修正，生产构建与受控运行验证通过。源启动器/CDP 若无法保持运行，不得将 P3 UI 验证写成通过。

### P7：版本/回滚/审计

**参考**：交接总览 §三-P7 + 经验文件规则6（断点续跑）

#### 7.1 新建 `src/services/memoryVersion.ts`
- [x] 定义 `MemoryChangeRecord` 类型
- [x] 实现 `saveChangeRecord()`
- [x] 实现 `getChangeHistory()`
- [x] 实现 `rollbackTo()` 和 `rollbackByChapter()`

#### 7.2 验证
- [x] 变更历史可查看（受控 Node 测试：`historyCount:1`）
- [x] 回滚后数据与目标版本一致（受控 Node 测试：`rollbackStatus:"清醒"`、`chapterRollbackStatus:"清醒"`）
- [x] 构建成功（`npm run build:vue`）

---

## 阶段三：可视化与用户界面（依赖 P0，P8-P12 可并行）

### P4：可视化渲染（4个视图）

**参考**：交接总览 §三-P4（共享 `{nodes, edges}` 数据源）+ 铁律"禁止各引擎维护独立数据"

#### 4.1 数据转换层
- [ ] 新建 `src/composables/useMemoryGraph.ts`
- [ ] 实现 `entitiesToGraphData()` 输出 `{nodes, edges}`
- [ ] 实现 `eventsToTimelineData()` 按章节排序
- [ ] 实现 `toMarkdownTree()` 卷→章→事件树状结构

#### 4.2 关系图（vis-network）
- [ ] 新建 `src/components/memory/RelationGraph.vue`
- [ ] 接入 vis-network，数据源为 `{nodes, edges}`
- [ ] 实现拖拽/缩放/点击节点/邻居展开

#### 4.3 图谱分析（cytoscape.js）
- [ ] 新建 `src/components/memory/GraphAnalysis.vue`
- [ ] 接入 cytoscape.js，数据源同上
- [ ] 实现中心性/社区发现/孤立节点/最短路径/伏笔传播/冲突检测

#### 4.4 思维导图（Markmap）
- [ ] 新建 `src/components/memory/MindMap.vue`
- [ ] 接入 Markmap，数据源为 Markdown 树
- [ ] 实现展开/折叠/点击跳转正文

#### 4.5 时间线（自定义列表）
- [ ] 新建 `src/components/memory/TimelineView.vue`
- [ ] 事件按章节排序，筛选角色/类型，点击跳转正文

#### 4.6 验证
- [ ] 每个视图有真实数据可显示
- [ ] 节点可点击跳转正文
- [ ] 各视图数据一致
- [ ] 构建成功

### P8：画像卡
- [ ] 新建 `src/components/memory/CharacterCard.vue`
- [ ] 显示外貌/性格/身世/状态/物品/关系/出场章节
- [ ] 每个字段带章节来源链接，点击跳转正文
- [ ] 构建成功

### P9：时间线
- [ ] 新建 `src/components/memory/TimelineView.vue`
- [ ] 事件按章节排序，筛选角色/类型
- [ ] 与 `extractTimeline` 数据格式一致
- [ ] 构建成功

### P10：关系图（vis-network 独立组件）
- [ ] 关系图组件接入，拖拽/缩放/点击节点查看详情/邻居展开
- [ ] 构建成功

### P11：思维导图（Markmap 独立组件）
- [ ] 卷→章→事件→角色的树状图，展开/折叠
- [ ] 构建成功

### P12：图谱分析（cytoscape.js 独立组件）
- [ ] 中心性/社区发现/孤立节点/伏笔传播/冲突检测
- [ ] 构建成功

---

## 阶段四：回填与扩展（依赖 P0-P3）

### P2：正文确认动作接入

**参考**：交接总览 §三-P2（触发时序6步）+ 经验文件规则8（链路完整性）

#### 2.1 正文确认按钮逻辑
- [x] 定位 `PipelinePanel.vue` 中"确认正文"按钮的 `@click` 处理函数（`src/components/pipeline/PipelinePanel.vue`）
- [x] 插入：保存正文 -> 调用 `extractMemory` -> 调用合并器 -> 生成变更预览（`confirmBodyWithMemory`）
- [x] 变更预览弹窗：非全屏模态框，逐项显示（`.pl-memory-preview-overlay`）
- [x] 弹窗内按钮：逐项确认/拒绝/锁定（CDP 真实点击）
- [x] 确认后写入记忆库，触发可视化更新（`recordMemoryChange` + `MemoryPanel` 新模型展示）

#### 2.2 失败策略
- [x] 抽取失败不影响正文保存（正文先保存，抽取失败仅显示错误）
- [x] 用户关闭预览不确认：不写入记忆，下次继续提示（CDP：`CLOSE_NO_WRITE=true`）

#### 2.3 验证
- [x] 确认前不写记忆，确认后即写入（CDP：关闭不写入、确认后步骤完成）
- [ ] 抽取失败不影响正文保存
- [ ] 连续 20 章模拟，每章确认后记忆库正确更新
- [x] CDP 验证完整链路（单章 + 面板筛选/关闭/重开/重启后显示）
- [x] 构建成功（`npx vite build`：154 modules transformed）

### P5：生成与聊天检索回填

**参考**：交接总览 §三-P5 + 经验文件规则2

#### 5.1 新建 `src/services/memoryRetriever.ts`
- [ ] 实现 `retrieveContext()`：当前角色画像 + 近5章事件 + 相关伏笔 + 上一章摘要
- [ ] 限制 token 量 ≤ 2000 字

#### 5.2 注入上下文
- [ ] 在生成流水线 system prompt 中调用 `retrieveContext`
- [ ] 注入到 `ChatPanel.vue` 已有的"相关记忆："入口

#### 5.3 验证
- [ ] 生成前上下文里实际出现相关记忆
- [ ] 检索结果不包含无关内容
- [ ] token 量不超过 2000 字
- [ ] 构建成功

### P13：正文生成检索回填
- [ ] 正文生成前调用 `retrieveContext`
- [ ] 注入当前出场角色画像 + 近 N 章事件 + 相关伏笔
- [ ] 验证生成质量提升
- [ ] 构建成功

### P6：导入导出

**参考**：交接总览 §三-P6

#### 6.1 新建 `src/services/memoryIO.ts`
- [ ] 实现 `exportFullJSON()` 全量导出
- [ ] 实现 `importFullJSON()` 导入校验
- [ ] 实现 `exportCharacterCardV3()` 角色卡 V3 兼容
- [ ] 实现 `importCharacterCardV3()` 导入

#### 6.2 验证
- [ ] 导出文件可被标准 JSON 解析器解析
- [ ] 导入后数据与导出前一致
- [ ] 角色卡 V3 可被外部工具读取
- [ ] 构建成功

### P14：动画/短剧导出接口

**参考**：交接总览 §三-P14 + 铁律"禁止动画/短剧板块建私有数据"

- [ ] 新建 `src/services/memoryExport.ts`
- [ ] 实现 `exportCharacterProfile()` 角色卡
- [ ] 实现 `exportStoryline()` 剧情线
- [ ] 实现 `exportTimeline()` 时间线
- [ ] 实现 `exportScene()` 场景数据
- [ ] 验证：外部工具可直接读取
- [ ] 构建成功

---

## 阶段五：全链路验证（P15，依赖所有）

### P15：全链路验证

**参考**：交接总览 §五（通用验证门 + 记忆板块专用验证12项 + 验证三铁律）

#### 15.1 单元验证
- [ ] P0 数据模型：旧数据兼容、新 schema 读写、持久化、重启恢复
- [ ] P1 抽取服务：3篇样章通过、JSON 可解析、evidence 必填
- [ ] P2 正文确认：确认前不写、确认后即写、抽取失败不影响正文
- [ ] P3 增量合并：20章无重复无丢失、锁定字段保护、黑名单过滤
- [ ] P4 可视化：4个视图有数据、节点可跳转、数据源一致
- [ ] P5 检索回填：上下文出现相关记忆、token ≤ 2000
- [ ] P6 导入导出：导出可解析、导入一致、角色卡 V3 兼容
- [ ] P7 版本/回滚：变更历史可查看、回滚后数据一致
- [ ] P8-P12 可视化组件：各组件独立渲染、数据源一致
- [ ] P13 正文生成回填：生成质量提升
- [ ] P14 动画/短剧接口：外部工具可读取

#### 15.2 端到端验证
- [ ] 连续 20 章真实小说数据模拟
- [ ] 每章完整链路：导入正文 -> 确认正文 -> 提取记忆 -> 变更预览 -> 确认/拒绝/锁定 -> 写入记忆
- [ ] 20 章后检查：无重复、无丢失、无锁定字段被覆盖
- [ ] 全量重建后与逐章确认一致

#### 15.3 Playwright/CDP 行为验证
- [ ] 正文确认 -> 变更预览弹窗出现
- [ ] 逐项确认/拒绝/锁定操作
- [ ] 确认后可视化更新
- [ ] 检索回填：生成前上下文包含相关记忆
- [ ] 导入导出：文件操作成功

#### 15.4 验证三铁律检查
- [ ] 外部裁判：用 CDP 实际操作 + 截图 + JSON 日志，不靠读代码
- [ ] 行为验证：验证"操作->预期行为->实际结果"三元组
- [ ] 强制执行：每阶段验证清单，FAIL 禁止进入下一阶段

---

## 附录：防空转自检清单（每轮必查）

- [ ] 已读取经验文件规则12：防无限回滚铁律（每项CDP验证通过后视为终态闭环，不整体回滚）
- [ ] 本回复首行有 `[Objective: ...]` 回显当期目标
- [ ] 每 30 秒输出了一条进度更新，无连续两条相同内容
- [ ] 每次工具调用前检查了工具是否在可用列表中
- [ ] 同一方法连续失败 2 次后换了路径
- [ ] 每完成一个子步骤更新了 `update_plan` 或写了 `_audit/checkpoint.md`
- [ ] 不使用 PowerShell 的 Set-Content 写含中文的源文件
- [ ] 不引入新的 npm 依赖
- [ ] 不硬编码 API Key/供应商/模型名
- [ ] 修改后执行 `npx vite build` 确认构建成功
- [ ] 修改后执行 `taskkill /f /im electron.exe` + `start-electron.bat` 重启验证
- [ ] 不用 `node --check` 代替功能验证
- [ ] 不用 `Runtime.evaluate` 代替真实鼠标点击
- [ ] CSS 先删旧规则再加新规则
- [ ] 每次加规则时检查旧规则是否已不适用

---

## 附录：阶段依赖关系图

```
P0（数据模型）-> 基础，无依赖，先做
  |-> P1（AI抽取器）-> 依赖 P0
  |    |-> P2（正文确认）-> 依赖 P1
  |    |-> P3（增量合并器）-> 依赖 P1
  |    |    |-> P7（版本/回滚）-> 依赖 P3
  |-> P4（可视化渲染）-> 依赖 P0
  |    |-> P10（关系图）-> 依赖 P0+P4
  |    |-> P11（思维导图）-> 依赖 P0+P4
  |    |-> P12（图谱分析）-> 依赖 P0+P4
  |-> P8（画像卡）-> 依赖 P0
  |-> P9（时间线）-> 依赖 P0
  |-> P5（检索回填）-> 依赖 P0-P3
  |    |-> P13（正文生成回填）-> 依赖 P5
  |-> P6（导入导出）-> 依赖 P0
  |-> P14（动画/短剧接口）-> 依赖 P0+P8
  |-> P15（全链路验证）-> 依赖所有
```

> 建议并行：组1做 P0->P1->P2->P3->P7，组2做 P4->P10->P11->P12，组3做 P8->P9->P14，组4做 P5->P13，组5做 P6，组6最后做 P15

---

*本计划文件位置：`_audit/记忆板块实施计划_可勾选版.md`*
*新对话框启动时必读顺序：交接总览 -> 经验文件 -> 本计划 -> 总版实现计划*
*每次完成子步骤后调用 update_plan 标记进度，不做完不进入下一阶段*

---

## 本轮核销附录：P4 可视化渲染（2026-08-20）

> 本附录不覆盖原计划。完成状态以本轮源文件启动器 + CDP 实测证据为准；“源码存在”或“构建成功”单独不能代替行为通过。

- [x] P4.1 共享转换层：新增 `src/composables/useMemoryGraph.ts`，统一输出图数据、排序时间线和卷→章→事件树；四个视图不维护第二份业务数据。
- [x] P4.2 关系图：新增 `src/components/memory/RelationGraph.vue`，从 `{nodes, edges}` 渲染；支持空状态、节点点击聚焦邻居、画布平移、重置。
- [x] P4.3 图谱分析：新增 `src/components/memory/GraphAnalysis.vue`，从同一图数据派生中心性、孤立节点、关系类型和自指关系检查。
- [x] P4.4 思维导图：新增 `src/components/memory/MindMap.vue`，从共享树数据渲染卷/章/事件；支持全部展开、全部收起和事件选中。
- [x] P4.5 时间线：新增 `src/components/memory/TimelineView.vue`，从共享时间线数据按章节排序；支持类型筛选、角色筛选和事件选中。
- [x] P4.6 入口验证：记忆面板真实切换关系图→图谱分析→思维导图→时间线，四态分别只渲染对应 DOM；时间线的筛选控件存在。
- [x] P4 构建验证：`npx vite build` 输出 `167 modules transformed` 与 `✓ built`。
- [x] P4 进程验证：构建后执行 `taskkill /f /im electron.exe`，再用 `start-electron.bat` 启动源文件版本。
- [ ] P4 真实数据内容验证：本轮页面当前没有足够的实体关系/事件样本，因此未把空状态验证冒充为真实卡片内容验证。
- [ ] P4 节点/事件跳转正文：本轮验证了选择行为，正文编辑器跳转需在后续编辑器联动阶段完成。

**本轮 CDP 关键输出**：
`{"views":{"relation":{"relation":1,"analysis":0,"mind":0,"timeline":0},"analysis":{"relation":0,"analysis":1,"mind":0,"timeline":0},"mind":{"relation":0,"analysis":0,"mind":1,"timeline":0},"timeline":{"relation":0,"analysis":0,"mind":0,"timeline":1}},"filters":{"timelineSelect":1,"timelineInput":1}}`

**依赖决策**：`package.json` 当前没有 `vis-network`、`cytoscape`、`markmap`，且计划自检规则要求不引入新 npm 依赖；因此本轮使用 Vue/SVG/列表实现可见行为，没有复制第三方代码，也没有新增依赖。

### P4.7 视图来源导航闭环（2026-08-20）

> 本项只核销本轮真实 CDP 已证实的范围；事件导航因当前项目没有事件样本，保持未完成。

- [x] 关系图实体选中后显示“打开来源”入口（证据：CDP `selectedBeforeOpen.openButton=true`）。
- [x] 图谱分析实体选中后提供同一来源入口（证据：`GraphAnalysis.vue` 统一发出 `open-source` 事件；构建通过）。
- [x] 思维导图事件选中后提供“打开章节”入口（证据：`MindMap.vue` 事件模板与 `open-source` 事件；真实事件样本缺失，行为保持待验）。
- [x] 时间线事件选中后提供“打开章节”入口（证据：`TimelineView.vue` 事件模板与 `open-source` 事件；真实事件样本缺失，行为保持待验）。
- [x] `MemoryPanel` 统一解析实体/事件来源，调用 `editorStore.openTab()`，成功后关闭记忆面板（证据：`src/components/common/MemoryPanel.vue` 的 `openMemorySource`）。
- [x] 实体真实导航：关系图 1 节点 → 选中 → 打开来源 → 记忆面板关闭 → `#editor-content` 显示来源正文（CDP 实测：`memoryPanel:false`，正文为“这是最终验证正文确认与记忆写入闭环的测试正文。角色确认线索后继续前进。”）。
- [ ] 事件真实导航：思维导图/时间线当前 `events:0`，没有可点击事件，不能以空状态代替通过证据。
- [x] 构建与进程清理：`npx vite build` 输出 `167 modules transformed`、`✓ built`；验证后 `taskkill /f /im electron.exe` 成功清理 4 个进程。

**本轮未完成边界**：P4 仍未全部通过。需要有真实 `MemoryEvent.chapterId` 且对应项目章节的样本，才能完成思维导图/时间线事件→正文的 CDP 行为验收；此外多实体关系、四视图数据一致性仍需真实数据验证。

### P4.8 真实正文确认与可视化核销（2026-08-20）

> 本附录只追加本轮新鲜证据，不覆盖 P4.7。缺少真实事件样本的项目状态仍按未验收处理。

- [x] 从当前源文件 Electron 页面真实点击 `#btn-pl-confirm-body`，出现“记忆变更预览”弹窗；DOM 文本包含“正文已保存，确认后才写入记忆”“entity”“新增”“拒绝”“锁定”“确认写入记忆”。
- [x] 真实逐层推进：设定层生成后点击 `#btn-pl-confirm-settings` → `pl-step-3-content`，再点击 `#btn-pl-confirm-volumes` → `pl-step-4-content`，再点击 `#btn-pl-confirm-chapters` → `pl-step-5-content`。
- [x] 真实关闭流水线遮罩后点击 `#btn-memory`，记忆面板打开；使用唯一 `#btn-memory-relation-graph` 循环切换四态，DOM 依次为关系图、图谱分析、思维导图、时间线。
- [x] 关系图真实数据渲染：页面文本为 `节点 3 · 关系 0`，实体名称包含“未命名角色”“线索”。
- [x] 时间线真实状态读取：页面文本为 `事件 0/0`，筛选控件显示，空事件状态真实存在。
- [ ] 事件来源导航：当前真实 `events=0`，没有事件节点，不能伪造“点击事件→正文”证据。
- [ ] 多实体关系与四视图一致性：当前真实 `relations=0`，缺少关系边样本，保持未验收。

**P4.8 结论**：本轮通过正文确认预览、四层真实导航、记忆面板入口和四视图循环切换；P4 整体仍未完成，剩余边界为真实事件来源导航和真实关系数据一致性。
