# P6 全功能回归与遗留边界报告

日期：2026-08-22  
范围：神意助手写作板块、记忆板块及其项目持久化整合回归  
启动方式：`start-electron.bat` 源文件启动器  
结论：**部分完成，不得标记为整合验收全部通过**

## 一、执行边界

本轮按真实用户路径核验：

`项目 → 大纲导入/编辑/锁定 → 流水线五层 → 正文/编辑器 → 记忆 → 保存 → 重启恢复 → 错误和导入导出边界`

代码构建、DOM 存在、Pinia 状态、Electron 项目存储、原生文件窗口和供应商实际成功响应分别记账，不能互相替代。

## 二、已通过

| 编号 | 闭环 | 结果 | 证据 |
|---|---|---|---|
| P6-1 | 源文件构建、启动和运行基线 | 通过 | `npm run build:vue` 原始输出：`175 modules transformed`、`built in 1.11s`；CDP 页面 `file:///D:/codex/novel-workshop-vue3/dist-renderer/index.html`，标题“神意助手” |
| P6-2 | 项目新建、加载、删除和存储清理 | 通过 | `wa_project_<id>`、`wa_lastProjectId` 实际读写；项目删除后键清除且重启不复活占位项目 |
| P6-3 | DOCX 大纲导入、编辑、锁定、字数传递 | 通过 | `test_doc.docx` 实际导入；编辑器内容、`outlineLocked=true`、`bookWordCount=660000` 和设定层“66 万字”一致 |
| P6-4 | 流水线五层导航与结构 | 通过 | 实际 DOM 顺序：大纲 → 设定 → 卷纲 → 章节 → 正文；五层入口和正文操作按钮存在 |
| P6-5 | 正文确认前的记忆预览链路 | 通过（代码和入口） | `PipelinePanel.vue`：正文先保存，再抽取，进入预览，逐条拒绝/锁定，确认写入记忆 |
| P6-6 | 主编辑器直接编辑正文的记忆入口 | 通过（代码和入口） | `EditorPanel.vue` 的“提取记忆”按钮调用 `useMemoryExtraction`；正文先保存，再审核和确认写入 |
| P6-7 | 记忆四视图与共享项目数据源 | 通过（入口和同源） | 关系图、图谱分析、思维导图、时间线均可见；记忆与章节位于同一 `wa_project_<id>` 项目对象 |
| P6-8 | 关闭、杀 Electron、源启动器重启恢复 | 通过 | 重启前后项目名、大纲锁定、总字数、大纲长度、记忆数、卷数一致 |
| P6-9 | 记忆导入策略入口 | 通过（策略和入口） | `MemoryPanel.vue` 提供“导入 JSON（合并）”和“覆盖导入 JSON”两个独立入口；`memoryIO.ts` 有格式校验和重复项跳过 |

## 三、部分通过或未核销

| 编号 | 边界 | 状态 | 事实 |
|---|---|---|---|
| P6-10 | 流水线用户可见取消生成 | 未通过 | 最终 CDP 按钮文本中没有“取消生成/取消”；源码只有生成状态禁用和 AI 生成中提示，未形成统一取消入口 |
| P6-11 | chain 失败后的断点续跑 | 未核销 | 断点对象可读，包含 `volumeIndex/chapterCount/total`；本轮没有伪造供应商失败，也没有取得“失败→保存→重启→从断点继续且不从头”的真实证据 |
| P6-12 | 真实正文 API 生成到编辑器 | 部分通过 | 历史 P8 有真实生成和编辑器联动证据；本轮只核验层结构和按钮，未重新依赖供应商完整响应作为本轮通过依据 |
| P6-13 | 原生 JSON 导入/导出实际落盘 | 未核销 | 页面入口和 Electron 原生窗口已验证；真实选择文件、确认保存、读回目标文件和状态前后对比未完成。原生窗口自动化曾出现 `coordinate input geometry is unavailable` |
| P6-14 | 损坏 JSON、断网、超时、重试、取消 | 部分通过 | 源码存在统一错误类型、AbortSignal、重试和 JSON 解析路径；本轮没有取得供应商真实失败/重试/取消三层行为证据 |
| P6-15 | 记忆真实新增后四视图内容变化 | 未核销 | 已核验同源和视图入口；本轮没有用真实正文抽取结果完成“审核→写入→四视图出现新增→重启恢复”的完整数据证据 |

## 四、未接入项

动画/短剧板块当前没有发现真实读取记忆的业务接入点。记忆服务和检索函数存在，不等于动画/短剧已经使用记忆；本项不阻塞“写作板块 + 记忆板块”基础验收，但不得写成已完成。

## 五、代码与工具状态

- `npm run build:vue`：通过。
- `npm run type-check`：仍有历史类型债务，不能标记为通过；详见 `lessons/ERROR_LOG.md`。
- 本轮发现一次验证器路径错误：实际记忆面板为 `src/components/common/MemoryPanel.vue`，已记录并修正，不涉及业务代码。
- Electron 结束前必须执行 `taskkill /F /IM electron.exe /T`；本轮启动验证后按该规则清理。

## 六、最终判定

写作板块与记忆板块已经具备代码级连接，并且项目持久化、重启恢复、主编辑器记忆入口和流水线记忆预览已有可核验基础；但是取消生成、断点续跑、原生文件实际读写、真实供应商错误恢复、记忆新增后的全视图数据变化仍未全部核销。因此本轮结论是：

**代码链路基本形成，真实用户整合验收部分完成；不得交付为“全部功能已验收通过”。**

## 七、P6 收尾状态表

| 子项 | 最终状态 | 处理结论 |
|---|---|---|
| P6-1 至 P6-9 | 已核销 | 保留已有构建、Electron、DOM、存储和重启证据，不重复回滚重测 |
| P6-10 | 未通过 | 后续需补用户可见取消入口，并验证真实请求中止、空占位清理和再次生成 |
| P6-11 | 未核销 | 后续需用可控失败完成保存断点、重试/重启续跑、成功清除四段证据 |
| P6-12 | 部分通过 | 后续需重新取得真实正文 API 响应到编辑器的本轮证据 |
| P6-13 | 未核销 | 后续需用桌面级操作完成原生 JSON 选择、落盘、读回和状态对比 |
| P6-14 | 部分通过 | 后续需分别验证真实断网、超时、重试、取消，不以源码路径替代行为证据 |
| P6-15 | 未核销 | 后续需完成记忆写入后四视图内容变化及重启恢复对比 |
| 动画/短剧记忆读取 | 未接入 | 不得写成已完成；作为后续板块接入任务 |

## 八、收尾与证据清理

- 本轮临时截图已备份到 `BACKUP/cleanup_20260822-P6/test_evidence/`，并从 `test_evidence/` 删除。
- `_audit/p6_*.cjs` 临时探针和 `tests/reports/p6_*` 中间报告已清理；正式报告、开发日志和经验文件保留。
- 本报告只保留结论和证据边界，不把历史证据扩大为本轮新证据。

## 九、下一阶段计划（仅列出，不在 P6 收尾执行）

1. **P7-1 取消生成闭环**：先补用户可见取消按钮，再验证请求中止、状态收束、空气泡清理和再次生成。
2. **P7-2 chain 断点续跑闭环**：构造可控失败，保存每步中间状态，失败后从正确步骤恢复，成功后清除断点。
3. **P7-3 原生 JSON 文件闭环**：用桌面自动化完成导入/导出真实文件，并比较文件内容与项目状态。
4. **P7-4 真实 API 错误恢复闭环**：分别验证网络错误、超时、重试和取消，记录请求次数与最终状态。
5. **P7-5 记忆四视图数据闭环**：正文抽取、审核、确认写入后，逐一确认四视图新增、持久化和重启恢复。
6. **P7-6 重新构建与全链路回归**：只在 P7-1 至 P7-5 各自取得证据后执行，不回跳已核销的 P6-1 至 P6-9。
