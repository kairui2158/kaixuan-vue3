# P7-9 真实 AI 记忆抽取与审核闭环

日期：2026-08-23
状态：未通过；入口可用，真实抽取未收束到审核结果

## 本轮目标

在 P7-8 隔离项目 `p1787414333932` 上，通过真实正文编辑器执行“保存正文 → 提取记忆 → 审核预览 → 确认写入”，核对 UI、项目存储和四视图，不使用脚本伪造 AI 输出。

## 结果勾选

- [x] 读取 P7-8 日志和经验文件。
- [x] 使用 `start-electron.bat` 启动源文件 Electron，页面为 `file:///D:/codex/novel-workshop-vue3/dist-renderer/index.html`，CDP 为 `127.0.0.1:9227`。
- [x] 通过章节树打开隔离项目正文，并通过编辑器保存正文。
- [x] 保存后的项目正文可从 `wa_project_p1787414333932` 读取。
- [x] 真实点击“提取记忆”，打开“记忆变更预览”面板。
- [x] 等待超过 37 秒后检查，项目记忆没有被提前写入，`items=1`、`entities=0`、`history=0`、`extractionCount=0`，空结果保护成立。
- [x] 关闭抽取预览后，入口恢复为“提取记忆”，没有残留生成中按钮状态。
- [ ] 真实 AI 返回抽取结果：未通过。37 秒后仍为“抽取中...”，没有进入可审核变更列表。
- [ ] 逐条拒绝/恢复/锁定/解锁：未核销，因为没有真实变更条目。
- [ ] 确认写入记忆并核对四视图更新：未核销。
- [ ] 真实供应商错误提示、重试和断点恢复：不属于本轮抽取成功闭环，未核销。

## 关键证据

```text
TAB 林舟在雨夜发现一枚旧钥匙。 MODE 正文层
SAVED { body: '林舟在暴雨夜里发现一枚刻着北辰纹章的旧钥匙。他把钥匙收进外套，决定天亮后去城北的旧钟楼寻找锁孔。', items: 1 }
STARTED 抽取中...
PREVIEW_VISIBLE 1
PROJECT_AFTER_TIMEOUT { items: 1, entities: 0, history: 0, extractionCount: 0 }
CLOSED 0 BUTTON 提取记忆
```

## 结论

本轮证明了“正文编辑 → 保存 → 真实抽取入口 → 审核预览打开 → 未完成时不落盘 → 关闭后入口恢复”。本轮没有证明 AI 返回、抽取结果审核和确认写入，因此 P7-9 不能标记为通过。失败现象暂按供应商响应/网络等待边界记录，不能直接判定应用业务逻辑缺陷；但抽取流程缺少用户可见的取消/等待进度核销仍是后续风险。

## 收尾

- [x] 未修改业务源码。
- [x] 未直接写入项目或记忆存储。
- [x] 未保留临时验证脚本、截图或中间文件。
- [x] Electron 进程已清理。

## 下一阶段计划（仅供审核，不执行）

P7-9-R：在客户提供可响应的记忆抽取供应商或受控测试接口后，重新核验真实 JSON 返回、审核拒绝/恢复/锁定/解锁、确认写入、存储计数和四视图更新；完成后再进入 P7-10 原生 JSON 文件闭环。
