# 设定层 P7：视觉层级和边框降噪

## 证据

- CDP `P7_BORDER_LAYOUT`：工作区 `1576x480`，内容框 `1576x443`，内部编辑区无边框；列表 `1558x190`，当前项编辑区 `1558x135`，分类操作区 `1558x41`。
- 内容框是唯一包围工作区的边框；列表行使用轻量单行边框，未形成框套框。
- 所有关键区域在 `y=458..901` 内连续排列，未发生相互覆盖。

## 结论

- P7：PASS。进入 P8 功能行为闭环回归。
