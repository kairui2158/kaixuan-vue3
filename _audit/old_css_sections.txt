
=== chat at 28665 ===
p(150px, 25vh, 300px); background: var(--bg-input); font-size: var(--font-size-sm);
}
#ow-chat-area .chat-input-row {
  display: flex; gap: var(--gap); padding: var(--btn-tight-padding);
  border-top: 1px solid var(--border-color);
}
#ow-chat-area .chat-input-row textarea {
  flex: 1; background: var(--bg-primary); color: var(--text-primary);
  border: 1px solid var(--border-color); border-radius: var(--radius-sm);
  padding: var(--space-xs) var(--space-sm); font-size: var(--font-size-sm); resize: none;
  font-family: inherit;
}

/* === Outline Editor Drag Area === */

/* === Self-check: Supplement Missing CSS Classes === */
.checkbox-label { display: flex; align-items: center; gap: var(--space-sm); font-size: var(--font-size-sm); cursor: pointer; }
.ctx-item {
  padding: var(--form-padding); font-size: var(--font-size-sm); cursor: pointer; color: var(--text-primary);
}
.ctx-item:hover { background: var(--accent); color: var(--text-on-accent); }
.sbm-close {
  background: none; border: none; color: var(--text-secondary); font-size: var(--font-size-lg);
  cursor: pointer; padding: var(--btn-xxs-padding); border-radius: var(--radius-xs);
}
.sbm-close:hover { background: var(--bg-tertiary); color: var(--text-primary); }

/* === Animation Keyframes === */
@keyframes wh-spin { to { transform: rotate(360deg); } }
@keyframes wh-toast-slide-in { from { transform: translateX(120%); opacity: 0; } to { transform: translateX(0); opacity: 1; } }
@keyframes wh-toast-slide-out { to { transform: translateX(120%); opacity: 0; } }
@keyframes msg-fade-in { from { opacity: 0; transform: translateY(8px); } to { opacity: 1; transform: translateY(0); } }
@keyframes panel-fade-in { from { opacity: 0; } to { opacity: 1; } }

/* === Unified Button System === */




/* === Unified Input System === */

/* === Select Customization === */

/* === FormField === */

/* === Modal Size System === */

/* === Modal Animation === */

/* === Toast Notification System === */
#toast-container {
  position: fixed; top: 16px; right: 16px; z-index: var(--z-toast);
  display: flex; flex-direction: column; gap: var(--space-sm); pointer-events: none;
}

/* === Loading Spinner (standalone) === */

/* === Progress Bar === */

/* === Message Fade In ===

=== chat2 at 187703 ===
rollbar-thumb, .mem-content::-webkit-scrollbar-thumb, #pl-context-summary::-webkit-scrollbar-thumb, .chat-messages::-webkit-scrollbar-thumb { background: var(--bg-hover); border-radius: 3px; }
#pl-ch-empty-hint, .pl-empty-hint, .empty-hint { text-align: center; padding: var(--space-lg) var(--space-md); color: var(--text-muted); font-size: var(--font-size-sm); border: 1px dashed var(--bg-hover); border-radius: var(--radius-lg); margin: var(--space-sm) 0; }
.pl-steps { display: flex ; align-items: center ; gap: var(--gap) ; padding: var(--space-md) var(--space-md) ; border-bottom: 1px solid var(--bg-hover) ; overflow-x: auto ; }
.pl-step { white-space: nowrap; }
.pl-step:hover { background: var(--bg-hover); color: var(--text-primary); }
.pl-step.active { background: var(--accent-dim); color: var(--text-primary); }
.pl-step.completed { color: var(--success); }
.pl-step .pl-step-num { display: inline-flex; align-items: center; justify-content: center; width: 28px; height: 28px; border-radius: 50%; font-size: var(--font-size-sm); font-weight: 600; background: var(--bg-hover); }
.pl-step.active .pl-step-num { background: var(--accent-glow); color: var(--text-primary); }
.pl-step.completed .pl-step-num { background: var(--success-dim); color: var(--success); }
.pl-header { display: flex; align-items: center; justify-content: space-between; padding: var(--space-md) var(--space-md); border-bottom: 1px solid var(--bg-hover); }
.pl-agent-bar { display: flex; align-items: center; gap: var(--space-sm); padding: 10px 16px; border-bottom: 1px solid var(--bg-hover); background: var(--bg-overlay-soft); }
.pl-agent-bar select { height: var(--btn-sm-height); min-width: 140px; padding: 0 var(--space-sm); background-color: var(--bg-input); border: 1px solid var(--border-color); border-radius: 6px; color: var(--text-primary); font-size: var(--font-size-sm); cursor: pointer; }
/* ===== Round 9: 深层按钮尺寸统一 ===== */

/* --- 流水线设定步骤按钮统一 --- */
#pl-step-2-content .btn-primary,
#pl-step-2-content .btn-secondary {
  height: 32px;
  min-height: 32px;
  line-height: 32px;
  padding: 0 var(--space-md);
  border-radius: var(--radius-sm);
  font-size: var(--font-size-sm);
  white-space: nowrap;
}

#pl-step-2-content .pl-bound-toggle {
  height: var(--btn-sm-height);


=== chat3 at 222565 ===
ace textarea:focus { border-color: var(--accent); box-shadow: var(--shadow-focus); }
#ow-chat-area .chat-input-row { display: flex; gap: var(--space-sm); padding: 8px 0 0 0; border-top: 1px solid var(--border-color); margin-top: 8px; }
#ow-chat-area .btn-send { min-width: 56px ; padding: 6px 14px ; border-radius: 6px ; background: var(--accent) ; color: var(--text-on-accent) ; border: none ; cursor: pointer ; font-size: var(--font-size-sm) ; transition: var(--transition-input) ; }
#ow-chat-area .btn-send:hover { background: var(--accent-hover) ; }

/* ==================== Round 15: Memory Panel ==================== */

/* ==================== Round 15: Pipeline Toggle + Card Deep ==================== */
.pl-toggle-btn, .pl-enable-btn { padding: 4px 12px; font-size: var(--font-size-xs); border-radius: 12px; border: none; cursor: pointer; transition: var(--transition-card); white-space: nowrap; font-weight: 500; }
.pl-toggle-btn.enabled, .pl-enable-btn.enabled { background: var(--success); color: var(--text-on-accent); }
.pl-toggle-btn.enabled:hover { background: var(--success); }
.pl-toggle-btn.disabled, .pl-enable-btn.disabled { background: var(--border-color); color: var(--text-secondary); }
.pl-toggle-btn.disabled:hover { background: var(--border-light); color: var(--text-primary); }
.pl-vol-card .vol-actions, .pl-vol-card .card-actions, .pl-volume-card .vol-actions, .pl-volume-card .card-actions { display: flex; gap: var(--space-sm); margin-top: 10px; padding-top: 10px; border-top: 1px solid var(--border-color); }
:is(.pl-nav-btn, .pl-nav-button):disabled { cursor: not-allowed ; opacity: 0.45 ; }
.pl-nav-btn.btn-primary { background: var(--accent) ; color: var(--text-on-accent) ; }
.pl-nav-btn.btn-primary:hover { background: var(--accent-hover) ; }
.pl-nav-btn.btn-secondary { background: var(--border-color) ; color: var(--text-primary) ; }
.pl-nav-btn.btn-secondary:hover { background: var(--border-light) ; }

/* ==================== Round 15: Dashboard + Overview + Confirm + Diff + Agent Test ==================== */
.dashboard-card, .stat-card { background: var(--bg-card); border: 1px solid var(--border-color); border-radius: var(--radius-lg); padding: var(--space-md); box-shadow: var(--shadow-sm); transition: var(--transition-button); }

=== deai at 243674 ===
font-weight: 600; }\n#btn-de-ai:hover { background: #fff3e0; }\n
/* === de-AI settings panel === */
.deai-config-section { padding: 12px 0; }
.deai-skill-bar { display: flex; gap: 8px; margin-bottom: 8px; }
.deai-skill-bar select { flex: 1; }
.deai-skills-list { margin-bottom: 12px; }
.deai-skill-chip { display: inline-flex; align-items: center; gap: 6px; background: var(--accent-dim); border: 1px solid var(--accent-glow); border-radius: var(--radius-lg); padding: var(--btn-xs-padding); margin: 2px; font-size: var(--font-size-sm); color: var(--accent-lighter); }
.deai-skill-chip span { color: var(--accent-lighter); font-weight: var(--fw-medium); }
.deai-skill-remove { background: none; border: none; color: var(--danger,#e74c3c); cursor: pointer; font-size: 16px; padding: 0 2px; line-height: 1; }
.deai-skill-remove:hover { color: #c0392b; }
.deai-hardrule-toggle { margin: 8px 0; }
.deai-hardrule-toggle .toggle-label { display: flex; align-items: center; gap: 8px; cursor: pointer; font-size: 14px; }
.deai-hardrules-list { margin-top: 8px; padding-left: 16px; }
.deai-hardrule-item { display: flex; align-items: center; gap: 6px; padding: 4px 0; font-size: 13px; color: var(--text-primary,#333); cursor: pointer; }
.deai-hardrule-item input[type=checkbox] { margin: 0; cursor: pointer; }
.deai-hardrule-item span { user-select: none; }

.deai-progress-bar { width: 100%; height: 8px; background: var(--bg-tertiary); border-radius: var(--radius-sm); overflow: hidden; margin-bottom: var(--space-sm); }
.deai-progress-fill { height: 100%; width: 0%; background: var(--accent-gradient); border-radius: var(--radius-sm); transition: width 0.3s ease; }
.deai-progress-info { display: flex; justify-content: space-between; align-items: center; margin-bottom: var(--space-md); }
#deai-progress-percent { font-size: var(--font-size-lg); font-weight: var(--fw-semibold); color: var(--accent); }
#deai-progress-step { font-size: var(--font-size-sm); color: var(--text-secondary); }
.deai-step-list { display: flex; flex-direction: column; gap: var(--space-xs); }
.deai-step-item { display: flex; align-items: center; gap: var(--space-sm); padding: var(--space-xs) var(--space-sm); border-radius: var(--radius-sm); font-size: var(--font-size-sm); }
.deai-step-item .deai-step-dot { width: 10px; height: 10px; border-radius: 50%; flex-shrink: 0; border: 2px solid var(--border-color); background: transparent; }
.deai-step-item .deai-step-label { flex: 1; color: var(--text-secondary); }
.deai-step-item .deai-step-status { font-size: var(--font-size-xs); color: var(--text-muted); }
.deai-step-item.pending .deai-step-dot { border-color: var(--border-color); background: transparent; }
.deai-step-item.active .deai-step-dot { border-color: var(--accent); background: var(--accent); animation: deai-pulse 1s ease-in-out infinite; }
.deai-step-item.active .deai-step-label { color: var(--text-primary); font-weight: var(--fw-medium); }
.deai-step-item.active .deai-step-status { color: var(--accent); }
.deai-step-item.done .deai-step-dot { border-color: var(--success); background: var(--success); }
.deai-step-item.done .deai-step-label { color: var(--text-muted); text-decoration: line-through; }
.deai-step-item.done .deai-step-status { color: var(--success); }
.deai-step-item.failed .deai-step-

=== pipeline at 212637 ===
= */


/* ==================== Pipeline Card Beautification ==================== */

.pl-card, .pipeline-card {
  background: var(--bg-card);
  border: 1px solid var(--border-color);
  border-radius: var(--radius-lg);
  padding: var(--space-md);
  margin-bottom: 12px;
  box-shadow: var(--shadow-sm);
  transition: var(--transition);
}
.pl-card:hover, .pipeline-card:hover {
  border-color: var(--border-light);
  box-shadow: var(--shadow-lg);
}
.pl-card-title, .pipeline-card .card-title {
  font-size: var(--font-size);
  font-weight: 600;
  color: var(--text-bright);
  margin-bottom: 8px;
}
.pl-card-content, .pipeline-card .card-content {
  font-size: var(--font-size-sm);
  color: var(--text-secondary);
  line-height: 1.6;
}

/* ==================== Chat Panel Beautification ==================== */


/* ==================== Editor Panel Beautification ==================== */


/* ==================== Dropdown Beautification ==================== */


/* ==================== Card Grid ==================== */

.item-list .card-grid-item, .item-list > div {
  background: var(--bg-card);
  border: 1px solid var(--border-color);
  border-radius: var(--radius-lg);
  padding: 14px;
  transition: var(--transition-button);
  cursor: pointer;
  box-shadow: var(--shadow-sm);
}
.item-list .card-grid-item:hover, .item-list > div:hover {
  border-color: var(--accent);
  box-shadow: var(--shadow-md);
  transform: translateY(-1px);
}

/* ==================== Empty State ==================== */

.empty-state, .no-data {
  text-align: center;
  padding: 40px 20px;
  color: var(--text-secondary);
  font-size: var(--font-size-sm);
}

/* ==================== Toast ==================== */


/* ==================== Context Menu ==================== */

.context-menu, .custom-context-menu {
  position: fixed;
  z-index: 9000;
  background: var(--bg-card);
  border: 1px solid var(--border-light);
  border-radius: var(--radius);
  box-shadow: var(--shadow);
  padding: 4px;
  min-width: 160px;
}
.context-menu .menu-item, .custom-context-menu .menu-item {
  padding: var(--form-padding);
  font-size: var(--font-size-sm);
  color: var(--text-primary);
  cursor: pointer;
  border-radius: var(--radius-sm);
  tr

=== plstep at 14039 ===
 display: flex; flex: 1; overflow: hidden; } .pl-body > .pl-content { overflow-y: auto; flex: 1; }
.pl-steps { width: clamp(140px, 13vw, 200px); background: var(--bg-secondary); border-right: 1px solid var(--border-color); padding: 16px 8px; display: flex; flex-direction: column; gap: var(--gap); position: relative; }
/* .pl-step merged to L582 */
.pl-step.active { opacity: 1; background: var(--accent-dim); }
.pl-step.completed { opacity: 1; }
.pl-step.completed .pl-step-num { background: var(--success); }
.pl-step-num { width: 28px; height: 28px; border-radius: 50%; background: var(--bg-tertiary); color: var(--text-primary); display: flex; align-items: center; justify-content: center; font-size: var(--font-size-sm); font-weight: bold; flex-shrink: 0; transition: var(--transition-card); }
.pl-step.active .pl-step-num { background: var(--accent); }
.pl-step-label { font-size: var(--font-size-md); color: var(--text-primary); font-weight: 500; }
/* .pl-step-status merged to L705 */
.pl-content { flex: 1; padding: var(--space-lg); overflow-y: auto; min-width: 0; }
/* pl-step-content visibility handled by JS _plShowStep */
.pl-step-content .pl-desc { font-size: var(--font-size); color: var(--text-secondary); margin-bottom: var(--space-md); padding: 10px; background: var(--bg-secondary); border-radius: var(--radius); }
.pl-actions { display: flex; gap: var(--space-sm); margin-top: var(--space-md); }
.pl-result { background: var(--bg-secondary); border: 1px solid var(--border-color); border-radius: var(--radius); padding: var(--space-md); margin: var(--space-md) 0; max-height: 400px; overflow-y: auto; white-space: pre-wrap; font-size: var(--font-size-md); color: var(--text-primary); }
.pl-gen-options { display: flex; align-items: center; gap: var(--space-sm); margin-bottom: var(--space-md); font-size: var(--font-size-sm); color: var(--text-secondary); }
.pl-checkboxes { display: flex; gap: var(--gap); flex-wrap: wrap; }
.pl-checkboxes label { display: flex; align-items: center; gap: var(--gap); font-size: var(--font-size-sm); cursor: pointer; }

/* Memory Panel */
.mem-main { display: flex; flex-direction: column; height: 100%; }
.mem-header h3 { margin: 0; font-size: var(--font-size-md); color: var(--text-primary); }
.mem-body { display: flex; flex

=== plvol at 54804 ===
;
}
.pl-skills-list {
  display: flex; flex-wrap: wrap; gap: var(--gap); margin-bottom: 8px;
}
.pl-vol-confirm-hint {
  display: none; font-size: var(--font-size-sm); color: var(--text-muted);
  margin-top: 4px;
}
.pl-ch-layout {
  display: flex; gap: var(--gap); min-height: 300px;
}
.pl-ch-sidebar {
  width: 200px; flex-shrink: 0;
}
.pl-ch-main {
  flex: 1; min-width: 0;
}
.pl-context-summary {
  display: none; background: var(--bg-elevated, var(--bg-tertiary));
  border-radius: var(--radius-sm); padding: var(--space-sm) var(--space-md);
  margin-bottom: 8px; font-size: var(--font-size-sm);
  max-height: 200px; overflow-y: auto;
  border: 1px solid var(--border-color);
}
.pl-bound-settings-box {
  margin-top: 12px; padding: var(--space-sm);
  border: 1px solid var(--border-color); border-radius: var(--radius-sm);
  background: var(--bg-tertiary);
}
.pl-bound-settings-title {
  font-size: var(--font-size-sm); font-weight: 700; margin-bottom: 6px;
  color: var(--text-primary);
}

/* === Batch 3: Modal & Form Utility Classes === */
.provider-edit-header {
  display: flex; align-items: center; gap: var(--gap); margin-bottom: 14px;
}
.provider-edit-title-text {
  font-weight: 600; font-size: var(--font-size-md);
}
.provider-conn-status-text {
  font-size: var(--font-size-xs); color: var(--text-muted); margin-left: auto;
}
.provider-model-list-box {
  max-height: 200px; overflow-y: auto;
  border: 1px solid var(--border-color); border-radius: var(--radius-sm);
  padding: var(--space-xs);
}
.temp-row {
  display: flex; align-items: center; gap: var(--space-sm);
}
.range-flex { flex: 1; }
.range-val {
  min-width: 30px; text-align: center;
}
.range-full { width: 100%; }

/* === Batch 4: Settings Collection Bind Modal === */
.sc-bind-item-name-text {
  font-weight: bold; margin-bottom: 12px;
}
.sc-bind-hint-text {
  font-size: var(--font-size-sm); color: var(--text-secondary);
  margin-bottom: 8px;
}
.sc-bind-tree-box {
  max-height: 300px; overflow-y: auto;
  border: 1px solid var(--border-color); border-radius: var(--radius-sm);
  padding: var(--space-sm);
}

/* === Batch 5: Plugin Market Modal === */
.market-modal-content { max-width: 680px; max-height: 80vh; }
.market-body-scroll { overflow

=== plchapter at 163104 ===
ards and lists - hover lift and selection */
.sc-item, .mem-item, .provider-card, .pl-volume-card, .pl-chapter-card, .card-item, .agent-card, .skill-card {
  transition: var(--transition-card);
  border: 1px solid var(--border-color);
  background: var(--bg-secondary);
  overflow: hidden;
  min-width: 0;
}
/* [FIX] sc-item 在 flex column 容器中 flex-shrink 默认导致被压缩到 30px,overflow:hidden 裁掉内容;只针对 .sc-item 修正,不影响其他卡片 */
.sc-item { flex-shrink: 0; overflow: hidden; }
.sc-item:hover, .mem-item:hover, .provider-card:hover, .pl-volume-card:hover, .pl-chapter-card:hover, .card-item:hover, .agent-card:hover, .skill-card:hover {
  border-color: var(--accent);
  box-shadow: var(--shadow-md), 0 0 0 1px var(--accent-dim);
  transform: translateY(-2px);
}
.provider-card-active, .provider-card.active, .sc-item.selected {
  border-color: var(--accent);
  box-shadow: 0 0 0 1px var(--accent), 0 4px 16px var(--accent-dim);
}

/* Layer 4: Buttons - distinct hierarchy */

/* Layer 4b: Inputs - focus rings and polish */

/* Layer 5: Micro-interactions */
button { transition: var(--transition) ; }
button:active { transform: var(--tf-press) ; }
button:focus-visible { box-shadow: 0 0 0 2px var(--accent), 0 0 0 4px var(--accent-dim) ; outline: none ; }


/* Chat message bubbles */

/* Pipeline step indicators */
.pl-step-num { transition: var(--transition-slow); }
.pl-step:hover .pl-step-num { transform: scale(1.15); }

/* Loading spinner */
.spinner { border-color: var(--accent-dim); border-top-color: var(--accent); }

/* Skill area badges */
.skill-badge { transition: var(--transition-button); }
.skill-badge:hover { box-shadow: 0 2px 8px var(--accent-dim); transform: translateY(-2px); }

/* === Beautify v3 Round 2: Fine-grained UI polish === */

/* Editor header - button hierarchy */

/* Undo/Redo - icon style, subtle */

/* Generate button - primary accent */

/* Export buttons - subtle ghost style */

/* Panel separators */
#skill-area { border-top: 1px solid var(--border-color); background: linear-gradient(180deg, var(--bg-secondary) 0%, var(--bg-tertiary) 100%); }

/* Chat input area polish */

/* Breadcrumb items */

/* Status bar items */

/* Editor content area - comfortable reading */
#editor-content:focus { outline: none; }


=== plagent at 53433 ===
tor ML === */

/* === Form Element Polish === */

/* === Pipeline Panel Internal Polish === */
.pl-agent-bar, .pl-skill-bar { border-radius: var(--radius-sm); transition: var(--transition-input); }
.pl-skill-bar:hover { background: var(--bg-tertiary) !important; }
.pl-skill-bar select { transition: var(--transition-panel); }
.pl-skill-bar select:focus { border-color: var(--accent) !important; box-shadow: var(--shadow-focus) !important; }

/* === Modal Tab Polish === */

/* === Loading Indicator === */


/* === Panel Hidden States (replacing inline style=display:none) === */
.ow-chat-hidden { display: none; }

/* === Batch 2: Pipeline Component Classes === */
.pl-agent-bar:hover { border-color: var(--accent-dim); }
.pl-skill-bar {
  display: flex; align-items: center; gap: var(--space-sm); margin-bottom: 8px;
}
.pl-label {
  font-size: var(--font-size-sm); color: var(--text-muted);
  white-space: nowrap;
}
.pl-select {
  flex: 1; padding: var(--space-xs) var(--space-sm);
  background-color: var(--bg-input, var(--border-light)); color: var(--text-primary);
  border: 1px solid var(--border-color); border-radius: var(--radius-sm);
  font-size: var(--font-size-sm); outline: none; cursor: pointer;
  transition: var(--transition-input);
}
.pl-select:focus {
  border-color: var(--accent);
  box-shadow: var(--shadow-focus);
}
.pl-skills-list {
  display: flex; flex-wrap: wrap; gap: var(--gap); margin-bottom: 8px;
}
.pl-vol-confirm-hint {
  display: none; font-size: var(--font-size-sm); color: var(--text-muted);
  margin-top: 4px;
}
.pl-ch-layout {
  display: flex; gap: var(--gap); min-height: 300px;
}
.pl-ch-sidebar {
  width: 200px; flex-shrink: 0;
}
.pl-ch-main {
  flex: 1; min-width: 0;
}
.pl-context-summary {
  display: none; background: var(--bg-elevated, var(--bg-tertiary));
  border-radius: var(--radius-sm); padding: var(--space-sm) var(--space-md);
  margin-bottom: 8px; font-size: var(--font-size-sm);
  max-height: 200px; overflow-y: auto;
  border: 1px solid var(--border-color);
}
.pl-bound-settings-box {
  margin-top: 12px; padding: var(--space-sm);
  border: 1px solid var(--border-color); border-radius: var(--radius-sm);
  background: var(--bg-tertiary);
}
.pl-bound-settings-title {
  font-si

=== editor at 77246 ===
系统 === */

/* === 2. 按钮尺寸标准化 === */

/* === 3. 按钮组排列优化 === */

/* === 4. 编辑器工具栏按钮 === */
:is(.editor-header, .editor-toolbar) {
  gap: var(--toolbar-gap, 2px);
}
:is(.editor-header, .editor-toolbar) button,
:is(.editor-header, .editor-toolbar) .btn {
  height: var(--btn-sm-height);
  min-width: 28px;
  padding: 0 var(--space-2, 4px);
  border-radius: var(--radius-sm, 6px);
  margin: 0;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  transition: filter 0.12s ease, transform 0.12s ease, background 0.12s ease;
}
:is(.editor-header, .editor-toolbar).is-connected button,
:is(.editor-header, .editor-toolbar).is-connected .btn {
  border-right: 1px solid var(--border-color);
  border-radius: 0;
}
:is(.editor-header, .editor-toolbar).is-connected button:last-child,
:is(.editor-header, .editor-toolbar).is-connected .btn:last-child {
  border-right: none;
}
:is(.editor-header, .editor-toolbar) button:hover,
:is(.editor-header, .editor-toolbar) .btn:hover {
  transform: translateY(-1px);
}
:is(.editor-header, .editor-toolbar) button:active,
:is(.editor-header, .editor-toolbar) .btn:active {
  filter: brightness(0.95);
  transform: translateY(0);
  box-shadow: inset 0 1px 3px var(--bg-glass);
}

/* === 5. 弹窗底部按钮区 === */
:is(.modal-footer, .dialog-footer) {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: var(--btn-group-gap-standard, 8px);
}
:is(.modal-footer, .dialog-footer) button,
:is(.modal-footer, .dialog-footer) .btn {
  height: var(--btn-md-height, 32px);
  min-width: 80px;
  padding: 0 var(--space-md);
  font-size: var(--font-size-sm);
}
:is(.modal-footer, .dialog-footer) > :is(.btn-secondary, .btn-cancel, .footer-left) {
  margin-right: auto;
}
:is(.modal-footer, .dialog-footer) > :is(.btn-primary, .btn-confirm, .footer-right) {
  margin-left: auto;
}

/* === 6. 卡片内按钮 === */
:is(.card, .item-card) { position: relative; }
:is(.card, .item-card) > :is(.card-action, .item-action, .card-close, .card-menu) {
  position: absolute;
  top: var(--space-4, 8px);
  right: var(--space-4, 8px);
  width: var(--btn-xs-height);
  height: var(--btn-xs-height);
  min-width: var(--btn-xs-height);
  padding: 0;
  border-radius: var(--radius-xs, 4px);


=== sidebar at 63628 ===
n:focus-visible, .btn-primary:focus-visible, .btn-secondary:focus-visible, .btn-icon:focus-visible, .sidebar-btn:focus-visible, input:focus-visible, select:focus-visible, textarea:focus-visible { outline: 2px solid var(--accent); outline-offset: 2px; }
button:disabled, .btn-primary:disabled, .btn-secondary:disabled, .btn-icon:disabled { opacity: 0.45; cursor: not-allowed; pointer-events: none; }
#panel-backdrop { background: var(--bg-overlay); backdrop-filter: var(--blur-sm); -webkit-backdrop-filter: var(--blur-sm); animation: fadeIn 0.2s ease; }
.resizer-v, .resizer-h { transition: background var(--transition-fast); background: var(--border-color); }
.resizer-v:hover, .resizer-h:hover { background: var(--accent); }
.word-count { font-size: var(--font-size-xs); color: var(--text-muted); background: var(--bg-tertiary); padding: 2px var(--space-sm); border-radius: var(--radius-xs); border: 1px solid var(--border-color); }
.empty-hint { color: var(--text-muted); text-align: center; padding: var(--space-lg); font-size: var(--font-size-sm); line-height: 1.8; }
/* @keyframes slideIn L2294 removed: L2294 earlier, overridden by L10118 */
.pl-step.active .pl-step-num { animation: pulse 2s ease infinite; }
@keyframes pulse { 0%, 100% { box-shadow: 0 0 0 3px var(--accent-dim); } 50% { box-shadow: 0 0 0 5px var(--accent-dim); } }
@media (min-width: 1920px) { :root { --font-size: clamp(14px, 0.8vw, 18px); } #editor-panel { max-width: 1200px; } }
@media (min-width: 1280px) and (max-width: 1599px) { #chapter-tree { min-width: 200px; max-width: 280px; } }
@media (min-width: 1024px) and (max-width: 1279px) { :root { --font-size: clamp(12px, 0.9vw, 14px); } #chapter-tree { min-width: 180px; max-width: 240px; } .editor-toolbar { flex-wrap: wrap; } }
@media (min-width: 800px) and (max-width: 1023px) { :root { --font-size: clamp(11px, 1vw, 13px); } #app-sidebar { width: 48px; } .sidebar-btn { width: 36px; height: 36px; } #chapter-tree { min-width: 150px; max-width: 200px; } .header-selector { max-width: 100px; } }
@media (max-width: 799px) { :root { --font-size: var(--font-size-sm); } #app-sidebar { width: 44px; } .sidebar-btn { width: 32px; height: 32px; } #chapter-tree { display: none; } .btn-sm { padding: var(--btn-xxs-padding); font-size: var(--font-size-xs); } }

=== treebody at 232410 ===
r-panel,
#chat-panel,
#chapter-tree {
  overflow: hidden;
  min-width: 0;
}

/* FIX v2.7.27: .tree-body scrolling - allow chapter tree to show all chapters */
.tree-body {
  flex: 1;
  min-height: 0;
  overflow-y: auto;
  overflow-x: hidden;
}
/* All inputs/selects/textareas: box-sizing + max-width */
input[type="text"],
input[type="number"],
input[type="password"],
input[type="search"],
select,
textarea {
  box-sizing: border-box;
  max-width: 100%;
}
/* All buttons: no text overflow */
button,
.btn-primary,
.btn-secondary,
.btn-sm,
.btn-icon {
  overflow: visible;
  text-overflow: ellipsis;
}
/* Editor toolbar: wrap and don't overflow */
.editor-toolbar {
  flex-wrap: wrap;
  overflow: visible;
}
/* Panel headers: truncate long titles */
.pl-header h3,
.sc-header h3,
.mem-header h3,
.ow-editor-header span {
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
  min-width: 0;
}
/* Card content: prevent text overflow */
.provider-card,
.skill-card,
.agent-card,
.sc-item-card,
.mem-item,
td, th {
  overflow: hidden;
  text-overflow: ellipsis;
  word-break: break-word;
  max-width: 100%;
}
/* Sidebar buttons: contain SVG icons */
.sidebar-btn {
  overflow: hidden;
  display: flex;
  align-items: center;
  justify-content: center;
}
.sidebar-btn svg {
  flex-shrink: 0;
  max-width: 100%;
  max-height: 100%;
}
/* Pipeline steps: horizontal scroll instead of overflow */
.pl-steps {
  overflow-x: auto;
  overflow-y: hidden;
}
/* Form groups: full width with box-sizing */
.form-group input,
.form-group select,
.form-group textarea {
  width: 100%;
  box-sizing: border-box;
}
/* Bug fix: provider edit form-actions layout */
#provider-edit-view .form-actions {
  display: flex;
  

=== modal at 26732 ===
; }

/* === Modal Transition Animations === */
/* [R33] Removed display:flex from ID selectors - .modal-hidden in modal-panel.css handles visibility */
#settings-modal, #project-modal, #new-project-modal, #plugin-market-modal, #sc-bind-modal, #skill-bind-modal, #volume-modal {
  position: fixed; top: 0; left: 0; width: 100vw; height: 100vh;
   align-items: center; justify-content: center;
  background: var(--bg-overlay); backdrop-filter: blur(4px);
  opacity: 0; pointer-events: none;
  transition: opacity 0.2s ease;
}
#settings-modal.visible, #project-modal.visible, #new-project-modal.visible, #plugin-market-modal.visible, #sc-bind-modal.visible, #skill-bind-modal.visible, #volume-modal.visible {
  display: flex !important; opacity: 1; pointer-events: auto;
}

/* === Keyboard Shortcuts Reference === */
.kbd-shortcuts { display: flex; flex-direction: column; gap: var(--space-sm); margin-top: 12px; }
.kbd-row { display: flex; align-items: center; gap: var(--space-sm); font-size: var(--font-size-sm); color: var(--text-secondary); }
.kbd { display: inline-block; padding: var(--btn-xxs-padding); background: var(--bg-tertiary); border: 1px solid var(--border-color); border-radius: var(--radius-xs); font-size: var(--font-size-xs); font-family: monospace; color: var(--text-primary); min-width: 20px; text-align: center; }

/* === Breadcrumb === */

/* === Form UI Polish === */
/* .empty-state L1052 removed: layout overridden by L4886, props by !important */




/* === Editor Tab Navigation === */

/* === Outline Workspace Chat Area === */
#ow-chat-area {
  display: none; flex-direction: column; height: 240px;
  border: 1px solid var(--border-color); border-radius: var(--radius-sm);
  margin-top: 8px; overflow: hidden;
}
#ow-chat-messages {
  flex: 1; overflow-y: auto; padding: var(--space-sm);
  display: flex; flex-direction: column; gap: var(--space-sm);
  max-height: clamp(150px, 25vh, 300px); background: var(--bg-input); font-size: var(--font-size-sm);
}
#ow-chat-area .chat-input-row {
  display: flex; gap: var(--gap); padding: var(--btn-tight-padding);
  border-top: 1px solid var(--border-color);
}
#ow-chat-area .chat-input-row textarea {
  flex: 1; background: var(--bg-primary); color: var(--text-primary);
  border: 1px sol

=== breadcrumb at 106117 ===
== */


/* Dropdown menu */

/* Progress bar */

/* Switch toggle */

/* Breadcrumb */
/* .breadcrumb-item:hover L4593-4596 removed: no !important, covered by L7449/L10407 */




/* Avatar */

/* Search input with icon */

/* Content visibility for performance */

/* Overscroll containment */
/* ===== P4 Deep Polish: Form Control Standardization ===== */

/* ===== P4 Deep Polish: Editor Toolbar Enhancement ===== */

/* ===== P4 Deep Polish: Chat Message Enhancement ===== */

/* Typing indicator */
@keyframes typingBounce {
  0%, 60%, 100% { transform: translateY(0); opacity: 0.4; }
  30% { transform: translateY(-6px); opacity: 1; }
}

/* ===== P4 Deep Polish: Loading States ===== */

/* Skeleton loading shimmer (enhanced) */

/* ===== P4 Deep Polish: Panel Header Unification ===== */

/* ===== P4 Deep Polish: Tree Node Enhancement ===== */
.tree-node.active,

/* ===== P4 Deep Polish: Card Hover Effects ===== */

/* ===== P4 Deep Polish: Tab Bar ===== */
.tab-bar,
.tab-bar .tab,
.tab-bar .tab:hover,
.tab-bar .tab.active,
.tabs .tab.active {
  color: var(--accent);
  border-bottom-color: var(--accent);
  font-weight: var(--fw-semibold);
}

/* ===== P4 Deep Polish: Modal Dialog Enhancement ===== */

/* ===== P4 Deep Polish: B

=== pluginmarket at 228661 ===
ansition: var(--transition-button);
  overflow: hidden;
}

/* Round 16: Plugin market panel */
.plugin-market {
  background: var(--bg);
  padding: var(--space-md);
}
.plugin-card {
  background: var(--bg-card);
  border: 1px solid var(--border-color);
  border-radius: var(--radius-lg);
  padding: var(--space-md);
  margin-bottom: 12px;
  transition: var(--transition-card);
}
.plugin-card:hover {
  border-color: var(--border-light);
  box-shadow: var(--shadow-md);
}

/* Round 16: Header bar buttons */

/* Round 16: Word count badge */
.word-count {
  font-size: var(--font-size-xs); /* R53: 11px归一化为xs */
  color: var(--text-secondary);
  background: var(--bg-sidebar);
  padding: var(--btn-xs-padding);
  border-radius: var(--radius-lg);
  border: 1px solid var(--border-color);
  font-variant-numeric: tabular-nums;
}

/* Round 16: Empty hint */
.empty-hint {
  color: var(--text-muted);
  text-align: center;
  padding: 40px 20px;
  font-size: var(--font-size-sm);
  line-height: 1.8;
}

/* ===== Round 17: Layout Fix - Statusbar Position & Main Fill ===== */
/* 根因: #statusbar 被放在 #app-body 的水平flex流里, flex:0 0 auto 横向占251px, 把app-main从1855px挤到1603px */
/* 修复: 把statusbar固定在底部,不参与水平flex布局 */


/* main 需要底部留出statusbar的空间 */

/* app-body 不再有gap造成多余间距(已由border分隔) */

/* editor-panel 在1920px+时不应该被max-width限制 */
@media (min-width: 1920px) {
  #editor-panel { max-width: none; }
}

/* editor-toolbar 需要撑满editor-panel宽度 */

/* ===== Round 17B: Outline Workspace Overflow Fix ===== */
/* 根因: 第9039行 width:1200px 覆盖了 left/right 定位, 导致溢出视口 */
/* 修复: 强制用 left/right 定位, 不用固定 width */

/* ===== Round 17B: Editor Toolbar Width Fix ===== */
#editor-content {
  flex: 1;
  width: 100%;
  max-width: 100%;
  margin: 0;
  padding: va

=== dashboard at 37447 ===
 }

/* ===== P2-33: Writing Dashboard ===== */
.writing-dashboard { padding: var(--space-md); }
.dashboard-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(180px, 1fr)); gap: var(--space-md); margin-bottom: var(--space-md); }
.dashboard-card { background: var(--bg-tertiary); border: 1px solid var(--border-color); border-radius: var(--radius); padding: var(--space-md); }
.dashboard-card-title { font-size: var(--font-size-sm); color: var(--text-secondary); margin-bottom: var(--space-sm); }
.dashboard-card-value { font-size: var(--font-size-lg); font-weight: 600; color: var(--text-primary); }
.dashboard-bar-chart { display: flex; flex-direction: column; gap: var(--space-sm); margin-top: var(--space-sm); }
.dashboard-bar-row { display: flex; align-items: center; gap: var(--space-sm); }
.dashboard-bar-label { width: 80px; font-size: var(--font-size-sm); color: var(--text-secondary); flex-shrink: 0; }
.dashboard-bar-track { flex: 1; height: 20px; background: var(--bg-input); border-radius: var(--radius-sm); overflow: hidden; }
.dashboard-bar-fill { height: 100%; border-radius: var(--radius-sm); transition: width 0.3s ease; }
.dashboard-bar-fill.accent { background: var(--accent); }
.dashboard-bar-fill.success { background: var(--success); }
.dashboard-bar-fill.warning { background: var(--warning); }
.dashboard-bar-value { width: 40px; text-align: right; font-size: var(--font-size-sm); color: var(--text-primary); flex-shrink: 0; }

/* ===== P2-32: Chapter Overview Panel ===== */
.chapter-overview { position: fixed; right: 0; top: 0; width: 280px; height: 100%; background: var(--bg-secondary); border-left: 1px solid var(--border-color); transform: translateX(100%); transition: transform 0.25s ease; z-index: var(--z-base)00; overflow-y: auto; padding: var(--space-md); }
.chapter-overview.visible { transform: translateX(0); }
.chapter-overview-header { display: flex; justify-content: space-between; align-items: center; margin-bottom: var(--space-md); }
.chapter-overview-title { font-size: var(--font-size-md); font-weight: 600; color: var(--text-primary); }
.chapter-overview-close { background: none; border: none; color: var(--text-secondary); cursor: pointer; font-size: var(--font-size-lg); padding: var(--space-xs); }
.chapter
