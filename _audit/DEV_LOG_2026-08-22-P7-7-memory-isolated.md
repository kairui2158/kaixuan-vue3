# P7-7 隔离测试项目上的真实记忆闭环

日期：2026-08-22
状态：部分完成；未标记验收通过

## 本轮目标

通过项目管理界面新建隔离项目，在真实 UI 中测试正文记忆抽取、审核写入、四视图、JSON 文件导入导出、重启恢复以及聊天/流水线读取同源记忆。

## 结果勾选

- [x] 读取经验总结和 P7-6 日志。
- [x] 使用 `start-electron.bat` 启动源文件构建，取得 CDP 页面。
- [x] 真实打开项目管理弹窗，看到当前“未命名”项目和新建入口。
- [x] 真实打开记忆面板并确认入口、四视图按钮和更多菜单入口存在。
- [ ] 新建隔离项目：未完成。当前项目有内容时，新建流程先显示“保存并继续/删除并继续/取消”确认面板；自动化第一次未处理该分支，第二次在确认面板仍打开时查找新建表单，超时退出。
- [ ] 正文保存后真实 API 记忆抽取：未核销。
- [ ] 抽取结果拒绝/恢复/锁定/解锁/确认写入：未核销。
- [ ] 四视图由非空记忆更新：未核销；前序 P7-5 只核销过视图入口和同源代码。
- [ ] 原生 JSON 导出、文件存在且可读：未核销。
- [ ] 合并导入不覆盖已有数据：未核销。
- [ ] 覆盖导入经过用户确认并替换：未核销。
- [ ] 关闭、杀 Electron、重启并加载同一项目恢复记忆：未核销。
- [ ] 聊天和生成流水线真实读取同一份非空记忆：未核销。
- [ ] 动画/短剧读取记忆：未接入，保持独立缺口。

## 关键证据

第一次 UI 脚本没有处理项目过渡确认，因此没有成功新建隔离项目；第二次试跑再次观察到同一确认面板，并因继续查找已被遮挡的新建表单而超时。该结果证明的是验证路径阻塞，不是业务闭环通过。

本轮没有通过脚本直接写项目、记忆或聊天存储，也没有使用默认项目中的手工数据冒充 API 抽取结果。

## 根因与后续边界

P7-7 当前被项目过渡确认流程阻塞。后续必须先在真实 UI 中完成“删除并继续”或“保存并继续”，等待项目列表和表单状态刷新，再创建隔离项目；每次弹窗变化后重新读取可见 DOM，不能复用旧定位。

## 收尾

- [x] 本轮临时脚本删除。
- [x] Electron 进程清理。
- [x] 经验文件已追加 P7-7 的验证边界。

## 下一阶段（仅供审核，不执行）

P7-8：先完成项目过渡确认与隔离项目建立，再按“正文保存 → 真实 API 抽取 → 审核确认 → 四视图 → JSON 原生文件 → 重启恢复 → 聊天/流水线同源读取”顺序逐项核销；动画/短剧接入另列，不在本阶段假设完成。
