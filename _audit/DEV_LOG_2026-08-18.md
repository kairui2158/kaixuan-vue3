# 神意助手开发日志 2026-08-18

## 目标

完成生成流水线 `injectMode` 的行为等价闭环验证，仅覆盖 `compose` 与 `chain`。本轮不扩展 MCP、自定义 JSON schema、split-merge 或 multi-step。

## 执行顺序与证据

1. 读取 `_audit/神意开发经验总结.md`，确认行为等价、一次一个闭环、构建后杀进程、只用 `start-electron.bat`、CDP 真实验证、finally 恢复和脚本归档规则。
2. 读取 `PipelinePanel.vue` 和既有验证脚本，发现 chain 中间提示同时拼接了 Skill 模板和 injectMode 注入结果，存在重复注入风险。
3. 删除后重写验证脚本：`_audit/scripts/archive/verify_inject_mode_blackbox_20260818.cjs`。脚本通过流水线面板、第三层卷纲、模式下拉框、Skill 下拉框和“AI生成全卷”按钮执行；临时技能、项目数据、步骤配置和 `fetch` 在 `finally` 中恢复。
4. 修复 `src/components/pipeline/PipelinePanel.vue`：chain 中间 prompt 只保留上一步输出，当前 Skill 模板统一由 `getPromptParts` 按 `injectMode` 注入。
5. 构建：`npx vite build`，PASS，151 modules，产物 `index-D95CLxjN.js` / `index-MG59T7uX.css`。
6. 清理 Electron 进程：`taskkill /f /im electron.exe`，PASS；随后使用源文件启动器 `call start-electron.bat`，PASS，CDP 端口 9227。
7. CDP 验证：`node _audit/scripts/archive/verify_inject_mode_blackbox_20260818.cjs`，PASS。

## 黑盒验证结果

### compose

- API 调用次数：1
- system：`VERIFY_SYSTEM_MARKER` 出现 1 次
- user 顺序：`VERIFY_PREFIX_MARKER` → 业务正文 `VERIFY_BUSINESS_PROMPT` → `VERIFY_SUFFIX_MARKER`
- 生成卷数据：1 条临时验证卷
- 结论：PASS

### chain

- API 调用次数：3
- 第 1 步：system 中出现 `VERIFY_SYSTEM_MARKER`，user 保留业务正文
- 第 2 步：`VERIFY_PREFIX_MARKER` 只进入 user 前部，并收到上一步输出
- 第 3 步：`VERIFY_SUFFIX_MARKER` 只进入 user 尾部，并收到上一步输出
- 没有重复的模板拼接，链路输出按步骤传递
- 生成卷数据：1 条临时验证卷
- 结论：PASS

## 中途发现与修复

第一次黑盒夹具误把原有 2 个 Skill 一起算入，修正为先通过真实移除按钮清空再添加 3 个临时 Skill。随后发现页面函数作用域错误，改为在 `page.evaluate` 内定义断言函数。最终黑盒结果为 `passed: true`，测试状态已恢复。

## 收尾

- 经验文件已追加 J6 和 injectMode 验证边界。
- 本日志记录与源码、构建产物、归档脚本一起提交。
- 本轮未实现：自定义 JSON schema、MCP 接入、split-merge/multi-step 的 injectMode 扩展。

## 设定层融合 UI 修复

### 目标

修复生成流水线设定层融合后的拥挤、不规整和按钮布局失效问题。本轮只调整 `PipelinePanel.vue` 的设定层 CSS，不改设定数据、store、API 或其他四层业务逻辑。

### 实施

1. 删除旧的 `.pl-settings-list` 和 `.pl-setting-item` Flex 规则，避免旧规格与新规格叠加。
2. 将设定层外框改为两列 Grid：左侧分类导航固定在 `168-200px`，右侧内容区使用剩余空间。
3. 将设定卡片改为 Grid：首行放名称、分类、绑定、删除，第二行让属性输入横跨整卡，固定最小可读高度。
4. 按模板真实 DOM 修正按钮网格定位；模板没有 `pl-btn-bound` class，因此使用卡片直属按钮的受限选择器，不扩大到其他按钮。
5. 增加窄屏降级：分类栏转为上方区域，卡片操作仍保持清晰的三行结构。

### 真实验证

- 构建：`npx vite build`，PASS，151 modules。
- 启动：使用源文件启动器 `start-electron.bat`，CDP `9227` 可连接。
- 递进操作：打开生成流水线设定层 → 检查真实设定条目 → 打开新增设定面板 → 保存临时条目 → 检查分类归属 → 删除临时条目并清理测试分类。
- DOM 测量：设定层 `894x220`；分类栏 `200x220`；内容区 `674px`；卡片 `668x162`；属性输入 `638x86`。
- 证据：`_audit/screenshots/settings_ui_after_fix.png`、`_audit/screenshots/settings_ui_verify.json`。

### 结果与边界

- 设定层布局修复通过，按钮可见，新增/保存/删除递进路径通过。
- 本轮没有修改业务逻辑，也没有宣称生成流水线其他层完成。
- 临时验证脚本需移入 `_audit/scripts/archive/`；Electron 进程需在收尾时强制清理。

## 设定层按钮语义与行为等价修复

### 目标

按用户确认的设定层图和行为语义，恢复 Vue3 新架构的单一闭环：

```text
已确认大纲
  -> AI设定生成
  -> 当前 Agent + SKILL 调用 API
  -> 提取设定并动态分配分类
  -> 用户编辑/绑定/确认分类
  -> 确认/保存设定层
  -> 设定层点亮并进入卷纲层
```

“连通大纲层”“根据 API 和 SKILL 扫描大纲”“保存所有设定”“点亮设定层”均是按钮效果，不是额外按钮。

### 实施

1. 删除 `btn-pl-link-settings-outline`，不再把“连通大纲层并分配分类”单独暴露为按钮。
2. 删除 `btn-pl-save-settings`，将保存职责并入唯一的 `btn-pl-confirm-settings`。
3. 将确认按钮改为“确认/保存设定层”，调用 `confirmSettingsLayer`：先保存当前项目，再执行设定层确认和进入卷纲。
4. `genSettings` 和按钮入口同时要求大纲已存在且已锁定，提示明确读取“已确认大纲”；分类仍由 API/SKILL 输出的 `category` 动态创建。
5. 未修改卷纲、章节、正文层业务逻辑。

### 验证

- 构建：`npx vite build` PASS，151 modules；仅有既存 deai CommonJS 警告。
- 启动：先杀 Electron，再使用 `start-electron.bat` 启动；CDP `9227` 可连接。
- 脚本：`_audit/scripts/verify_settings_layer_semantics_20260818.cjs`。
- 报告：`_audit/screenshots/settings_layer_semantics_verify.json`，11/11 PASS。
- 覆盖：流水线打开、设定层可见、旧入口删除、新布局无旧 class、动态分类、AI入口状态、确认入口状态、分类展开、设定项绑定、确认/保存后进入卷纲。
- 验证脚本在 `finally` 中恢复原项目快照；未把临时设定写入用户项目。
- 边界：本轮没有触发真实 API 生成，因此没有把模型输出质量写成已验证。

### 过程纠偏

第一次验证选择了空的“设定类”并误报无设定项，后改为选择真实有内容的动态分类后通过。另一个旧验证器仍引用已删除按钮，已确认不能继续作为本轮证据。

## P6 卷纲层 API 反馈闭环

### 目标

验证客户反馈中的“逐卷生成时，卷框内显示 API 滚动信息和进度条”，并确保 API 长时间无响应时不会让界面永久卡在生成中。

### 精准修改

- 文件：`src/components/pipeline/PipelinePanel.vue`
- 修改：卷纲层 `runStepSkills(2, ...)` 增加 `120000` 毫秒超时参数。
- 保持：卷框内日志、进度条、成功/失败状态和项目保存逻辑不变；没有批量修改其他层。
- 验证器：`_audit/scripts/p6_volume_generation_feedback_verify.cjs` 等待窗口改为 300 秒，并记录请求/响应时间戳。

### 执行链路

1. `npx vite build`：PASS，151 modules。
2. `taskkill /F /IM electron.exe`：PASS，清理旧 Electron 进程。
3. `_audit/scripts/restart_source_app.ps1`：PASS，使用 `start-electron.bat` 源启动器，CDP 9227 就绪。
4. `node _audit/scripts/p6_volume_generation_feedback_verify.cjs`：PASS。

### CDP/行为证据

- 项目快照保存：`wa_project_proj-1787018619240`。
- 锁定大纲读取成功：`outlineLength=12529`，`locked=true`。
- 点击逐卷生成后即时状态：3 条 API 日志、进度 `10%`、状态“AI生成卷纲中”。
- 实际请求：`https://openapi.cloud-ai.cn/v1/chat/completions`。
- 受控超时后终态：第 4 条日志为“API 调用失败：API超时”，状态为 `failed: API超时`，`generating=false`。
- 完整证据：`_audit/P6_volume_generation_feedback_verify.json`；截图：`_audit/P6_volume_generation_feedback_verify.png`。

### 经验

本阶段暴露的根因不是“反馈 UI 没有写”，而是异步调用没有终态收敛。今后每个 API 闭环必须同时验证请求发出、过程反馈、成功或失败终态、按钮恢复、数据是否落盘，以及验证脚本是否恢复快照。

## P7 卷纲层字数联动闭环

### 目标

让大纲层确认的全书字数在卷纲层形成用户可见、可计算、可持久化的联动状态。

### 修改

- 文件：`src/components/pipeline/PipelinePanel.vue`
- 新增卷纲层只读状态区：显示“大纲已锁定全书字数”。
- 自动分配提示改为显示每卷字数和计算出的卷数。
- 保留现有 `linkedVolumeCount` computed、只读卷数输入和 `bookWordCount` 持久化逻辑，未改动计算规则。

### 执行与验证

1. `npx vite build`：PASS，151 modules。
2. `taskkill /F /IM electron.exe`：PASS，清理旧 Electron 进程。
3. `_audit/scripts/restart_source_app.ps1`：PASS，使用 `start-electron.bat` 源文件启动器，CDP 9227 就绪。
4. `node _audit/scripts/archive/p7_volume_word_link_verify.cjs`：11/11 PASS。
5. 真实链路：大纲输入 60 万字并确认 → 进入设定层 → 进入卷纲层 → 显示 60 万字、每卷 10 万字、自动分配 6 卷、卷数只读、配置保存为 600000 字。
6. 证据：`_audit/P7_volume_word_link_verify.json`、`_audit/P7_volume_word_link_verify.png`。
7. 验证器在 `finally` 中恢复项目与流水线配置快照，脚本位于 `_audit/scripts/archive/`。

### 纠偏记录

首次验证器把“确认大纲后进入设定层”的真实状态机误判为直接进入卷纲层，并错误写死 1 卷预期；已修正验证器后重跑通过。该问题属于验证器假设错误，不是应用联动错误。
### P8-P9 卷纲层闭环与按钮精简（2026-08-18）

#### P8 卷框保存、锁定、删除与章节层联动
- 修复 `PipelinePanel.vue` 中 Vue 3 `v-if` 与 `v-for` 同节点造成的运行时崩溃。
- 每个卷框提供保存并锁定、删除；锁定后字段只读，锁定卷才进入章节层选择器。
- 构建通过：`npx vite build`，151 modules。
- 源启动器验证：先清理 Electron，再使用 `start-electron.bat`，CDP 9227 正常。
- 证据：`_audit/P8_volume_save_delete_verify.json`，12/12 PASS；截图为 `_audit/P8_volume_save_delete_verify.png`。

#### P9 卷纲底部按钮语义收敛
- 删除重复的“自动生成卷纲”和未纳入当前规格的“批量续生成”。
- 保留唯一的三个用户命令：`AI生成全卷`、`逐卷生成`、`确认完成下一步`。
- CDP 真实验证：流水线打开、卷纲层显示、三按钮可见、旧按钮不存在、确认后进入章节层，10/10 PASS。
- 证据：`_audit/P9_volume_buttons_verify.json`；截图为 `_audit/P9_volume_buttons_verify.png`。
- 验证脚本均归档在 `_audit/scripts/archive/`，并在 `finally` 中恢复项目快照与流水线配置。

#### 本阶段结论
P8 与 P9 已完成并验证；P10 章节层空状态与布局仍未开始，不能提前标记完成。

### P10 章节层空状态与布局（2026-08-18）
- 修改 `src/components/pipeline/PipelinePanel.vue`：无锁定卷纲时不渲染章节列表；有锁定卷但无章节时保留内容区并显示独立空状态。
- 将重复 `pl-ch-empty-hint` 拆为 `pl-ch-empty-no-volume` 与 `pl-ch-empty-no-chapters`。
- 构建：`npx vite build` PASS，151 modules。
- 启动：`taskkill /F /IM electron.exe` 后使用 `start-electron.bat`，CDP 9227 正常。
- 验证：`_audit/scripts/archive/p10_chapter_empty_state_verify.cjs`，6/6 PASS。
- 证据：`_audit/P10_chapter_empty_state_verify.json` 与 `_audit/P10_chapter_empty_state_verify.png`。
- 验证器第一次因 reload 后未重新打开流水线而失败，已修正验证器并重跑通过；不是应用功能失败。

### P1 项目管理彻底删除闭环（2026-08-18）

#### 目标

解决客户反馈“项目管理无法全部删除、存在项目占位无法删除”，并验证删除行为不是只停留在列表 UI。

#### 验证顺序

1. 使用 taskkill /F /IM electron.exe 清理旧进程。
2. 使用源文件启动器 start-electron.bat 启动，确认 CDP 9227 监听。
3. 通过项目管理真实界面加载临时项目列表，验证新旧存储键合并后同一项目只出现一条。
4. 点击临时项目“删除”，确认弹窗后检查新键、旧键和列表。
5. 删除当前临时项目，检查 Pinia 当前项目状态、章节树和流水线状态。
6. reload 后重新检查 lastProjectId、项目存储键、当前项目和项目管理列表，确认不存在复活。

#### 结果

- 证据：_audit/P1_project_delete_verify.json，8/8 PASS。
- 截图：_audit/P1_project_delete_verify.png。
- 通过项：双键去重、双键清理、列表即时移除、项目指针清理、当前状态清空、章节树无残留、重载不复活、列表不显示已删除临时项目。
- 真实用户项目未被删除；测试使用 p1_verify_* 临时 ID，并在验证器 finally 中恢复快照。
- P1 完成；P2 及后续阶段尚未开始，不将其写成完成。

### P2 大纲文件导入闭环（2026-08-18）

#### 客户问题

客户反馈导入按钮无亮度、只能导入 TXT。检查后发现当前 Vue3 运行包的入口已声明 TXT/MD/RTF/DOCX，不能只凭按钮文案断言完成，必须实际走文件入口验证。

#### 执行与验证

1. 使用 taskkill /F /IM electron.exe 清理旧 Electron 进程，使用 start-electron.bat 启动源文件应用。
2. CDP 真实打开大纲工作台，读取导入按钮 display、visibility、opacity、尺寸和 disabled 状态。
3. 点击“从文件导入”后分别导入 TXT、MD、RTF、DOCX 样本。
4. 检查编辑器文本特征、项目 storage 中 outlineText 完全一致，并在重载后确认没有测试残留。

#### 结果

- `_audit/P2_outline_import_verify.json`：16/16 PASS。
- `_audit/P2_outline_import_verify.png`：运行界面截图证据。
- TXT、MD、RTF、DOCX 均能经用户导入入口进入编辑器并写入项目存储。
- 本轮未发现需要修改业务源码的 P2 缺陷；P2 完成，进入 P3 前不提前合并其他阶段。

## P3 项目管理闭环

### 目标

完成项目管理的真实用户闭环：新建、切换、保存、删除、取消，以及切换后章节树和生成流水线读取同一项目状态。

### 精准修改

1. `src/components/common/ProjectModal.vue`：将新建/切换前的浏览器确认改为应用内三选一面板：保存并继续、删除并继续、取消；取消不执行待处理动作。
2. `src/components/sidebar/ChapterTree.vue`：监听当前项目 ID，切换项目时清理旧展开卷和选中章节，自动展开新项目第一卷。
3. `src/stores/project.ts`：项目加载和新建后统一从大纲建立基础卷章结构，保证章节树和流水线使用同一份项目状态。

### 执行与验证

1. 每次验证前执行 `taskkill /F /IM electron.exe`，使用 `start-electron.bat` 源文件启动器，连接 CDP `127.0.0.1:9227`。
2. `node _audit/scripts/p3_project_management_verify.cjs` 首次重跑发现启动时转换面板遮罩，修正验证器入口后重新执行；未误改业务代码。
3. 最终验证报告：`_audit/P3_project_management_verify.json`，18 项递进断言全部 PASS。
4. 最终截图：`_audit/P3_project_management_verify.png`。
5. 覆盖：项目弹窗打开、新建并切换、新建前取消/保存/删除选择、切换前取消/保存/删除选择、项目大纲进入当前状态、章节树同步、生成流水线同步大纲、删除后列表移除。
6. 验证脚本已移入 `_audit/scripts/archive/p3_project_management_verify.cjs`；临时项目使用 `p3_verify_*`，`finally` 恢复原存储快照。

### 结果

P3 项目管理闭环完成。P4 大纲工作台闭环尚未开始，不能提前标记完成。


## P4 大纲工作台闭环收尾

### 本阶段目标

按客户反馈验证大纲工作台的完整行为闭环，不以“页面能打开”或“按钮存在”代替行为证据。

### 执行与证据

1. 读取经验总结后，确认本阶段只处理大纲工作台，不混入生成流水线或章节层遗留改动。
2. 使用已归档验证器 `_audit/scripts/archive/p4_outline_workspace_verify.cjs`，通过真实页面 DOM、Pinia/项目状态和重载路径递进检查。
3. 最终报告 `_audit/P4_outline_workspace_verify.json`：9/9 PASS。
4. 截图证据 `_audit/P4_outline_workspace_verify.png` 已保留。
5. 覆盖：打开工作台、载入大纲、自由编辑、保存按钮状态、确认锁定、跳转流水线、流水线读取同一大纲、重载持久化。

### 证据边界与纠偏

本轮保存到本地的可验证部分是按钮对应的 IPC 写入：临时文件实际存在且内容长度正确。原生 Electron 保存对话框会阻塞 CDP，因而没有把原生对话框中的路径选择和确认写成 PASS。今后报告必须分别记录网页层、IPC 层和原生窗口层证据，不能把直接 IPC 调用当作完整用户点击流程。

### 收尾状态

P4 行为闭环证据完成并归档；P4 文档回写完成。P5（编辑器与右侧对话框双向同步）是下一阶段，未在本日志中提前标记完成。
