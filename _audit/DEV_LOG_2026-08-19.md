# DEV_LOG — 2026-08-19
## P2 大纲文件导入闭环（当前轮补证 2026-08-19）

规则引用：C1（编辑器→项目存储同步）、C2（构建→杀进程→源启动器→验证）、C3（本轮证据归零）、C4（单闭环）、J15（原生 alert 需独立证据）。

### 当前轮证据
1. 源启动器页面中导入按钮 `visible=true`、`disabled=false`、`opacity=1`、`cursor=pointer`，accept 为 `.txt,.md,.text,.rtf,.docx`，没有误标 `.doc`。
2. TXT、MD 导入后编辑器文本与 `wa_project_<id>.outlineText` 一致。
3. RTF 控制字样本导入后得到 `P2 RTF 星海示例\nP2 second line`，编辑器与存储一致。
4. 真实文件 `C:\Users\凯瑞\Documents\New project 2\archive\test_doc.docx` 导入后得到 `第一章 测试文档...`，编辑器与存储一致。
5. `.doc` 导入弹出中文提示：`导入失败: .doc 旧版格式不支持，请另存为 .docx、.txt 或 .md`。
6. 验证结束恢复原项目 `proj-1787096156367`，杀 Electron 后用 `start-electron.bat` 重启复核项目数据未被测试污染。

### 证据与结论
- `_audit/p2_import_core.json`
- `_audit/p2_core_after_docx.png`
- `_audit/p2_rtf_doc_boundary.png`
- 结论：P2 当前轮行为证据完整，待构建输出和阶段提交后进入 P3。

## P1 项目管理删除闭环（本轮重新验证，2026-08-19）

规则引用：C3（旧证据归零）、C2（构建→杀进程→重启→验证）、J17（脚本选择器必须实测核对）

### 本轮实测（CDP 自动化 + 截图 + 存储键断言）
1. 先备份用户数据目录（21 个 JSON），再以源文件启动器 `start-electron.bat` 启动。
2. 打开项目管理弹窗：初始列表 1 项。
3. 逐个点击“删除”（confirm 自动确认）：列表清空，显示“暂无项目”。
4. 存储断言：`wa_project_*`、`wa_project-*`、`wa_projects`、`wa_lastProjectId` 均无残留。
5. 新建“P1验证项目”：列表 1 项，项目名为空之外均正确；当前项目名同步。
6. 再次删除：列表清空，存储键无项目残留。
7. 杀掉 Electron 并重启：项目管理仍为“暂无项目”，无占位项目，`wa_lastProjectId` 不存在。

### 结论
P1 PASS：新建→列出→删除当前→删除最后一个→新旧格式键全清→重启不占位，本轮真实 CDP 实测通过。

### 证据
- `_audit/tmp/p1_verify_result.json`（CDP 每一行命令日志 + PASS 断言）
- `_audit/tmp/p1_after_delete_all.png`、`p1_created.png`、`p1_deleted.png`、`p1_after_restart.png`

## P10 修复：bookWordCount 默认值自动填充

### 问题
流水线打开后 bookWordCount 为 0，导致 btn-pl-confirm-outline 被 disabled，
用户无法进入后续步骤（设定层、卷纲层、章节层、正文层全部 display:none）。

### 根因
bookWordCount 是本地 ref(0)，仅在 onMounted 中从保存的配置恢复。
当用户首次锁定大纲进入流水线时，没有保存的配置，bookWordCount 保持 0。

### 修复
src/components/pipeline/PipelinePanel.vue 的 onMounted 钩子中，
在从保存配置恢复后，如果 bookWordCount <= 0 且大纲已锁定且有内容，
自动填入默认值 10（代表 10 万字）。

### 验证
- CDP 验证：bookWordCount=10, confirmDisabled=false, volumeCount=1
- 存储验证：savedBookWordCount=100000
- 步骤指示器：s1✓, s2*, s3-s5 pending
- 截图：_audit/tmp/p10_fix_verified.png

### 经验更新
- 字数默认值必须在 onMounted 中设置，不能依赖用户手动输入
- 大纲锁定后进入流水线，字数应自动填充合理默认值（10万字）
- 修复后需验证存储持久化、步骤指示器、按钮状态

## P1 项目管理删除闭环（2026-08-19 重新验证）

规则引用：C3（旧证据归零）、J18（证据分级）、行为等价最高约束

### 验证记录
1. 打开项目弹窗：列表显示旧测试项目 p10_test
2. 逐一点击删除（原生 confirm 自动 accept）：项目列表清空，显示"暂无项目"
3. 新建"P1验证项目"：创建成功，列表+当前项目显示正确
4. 再删除：列表清空
5. 存储检查：无 wa_lastProjectId、无项目键残留（仅剩余 wa_chat_* 聊天文件，非项目占位）
6. 重启应用：项目列表为空，无占位项目，lastProjectId=null

### 结论
P1 PASS：新建→列出→删除当前→删除最后一个→新旧格式键全清→重启不占位，全部实测通过。
## P2 大纲文件导入闭环（2026-08-19）

规则引用：C2（改码→构建→杀进程→重启）、C3（旧证据归零）、J18（证据分级）、C4（一次只修一个闭环）

### 前置修正（2026-08-19 复测）
上一轮标记的"P2 PASS"不成立：RTF 导入正文夹带字体信息 `Times New Roman;`，
`.doc` 错误消息仍为英文 `DOC_LEGACY_NOT_SUPPORTED`。按 C3 旧证据归零重新验证。

### 修复
`src/services/file-import.ts`：
1. 新增 `parseRtfText`：按 RTF 词法解析控制字与分组，跳过 `fonttbl` 等表，支持
   `\\par`/`\\line`/`\\tab`/`\\\\'hh`/`\\uN`，识别 `\\ansicpg936` 后用 GBK 解码。
2. `.doc` 分支错误消息改为中文：`'.doc 旧版格式不支持，请另存为 .docx、.txt 或 .md'`。

### 验证记录（CDP 真实导入）
1. 构建通过（151 modules）→ 杀 Electron → start-electron.bat 启动
2. 按钮状态：opacity=1, bg=rgba(124,140,248,0.12), cursor=pointer, disabled=false
3. accept：`.txt,.md,.text,.rtf,.docx`（无 .doc，符合预期）
4. TXT：textarea/store/存储均得到 `星海神意 TXT 导入测试文本\n第二行内容`
5. MD：textarea/store/存储均得到 Markdown 原文
6. RTF：textarea/store/存储均得到 `星海神意 RTF 导入测试\n第二段内容`，无 `Times New Roman`
7. DOCX method 0：导入 `星海孤航\nDOCX method 0 测试标记`
8. DOCX method 8（deflate）：导入真实 Word 文档内容
9. DOC：弹出 alert `导入失败: .doc 旧版格式不支持，请另存为 .docx、.txt 或 .md`

### 结论
P2 PASS（本轮 CDP 实测证据）

### 证据
- `_audit/tmp/p2_verify_result.json`（CDP 命令日志 + 每项状态）
- `_audit/tmp/p2_after_*.png`（导入后截图 6 张）

### 经验更新
- RTF 不能用去空白正则处理，必须按控制字/分组解析并跳过字体表，否则字体名会混入正文
- 旧版本格式报错必须使用用户可读中文消息，不能把内部错误码直接抛给用户
- 代码生成脚本内部禁用模板字符串嵌入转义序列；改为先写独立文本文件再读取拼接

## P3 大纲 AI 共创 API 闭环（2026-08-19）

规则引用：B3（对话必须入 store，禁止组件本地 ref 丢持久化）、J18（证据分级：真实外部 API）、C2（改码→构建→杀进程→重启）

### 修复
`src/stores/project.ts`：
1. 新增 `outlineChat` ref，load/save/clearCurrent 全接上，项目存储新增 `outlineChat` 键。
2. 新增 `appendOutlineChat`、`removeOutlineChatAt` 两个 store action，写后自动 saveProject。

`src/components/common/OutlineWorkspace.vue`：
1. `messages` 由本地 `ref` 改为 `computed(() => projectStore.outlineChat)`，所有 push/splice 改为 store action。
2. `max_tokens: 128000` 改为 `provider.maxTokens || 8192`。
3. model 回退链补充 `provider.models[0]`。

### 验证记录（CDP 真实用户模拟，真实外部 API）
1. 打开大纲工作台 → 打开 AI 共创 → 清空测试对话 → 输入"只回复OK" → 点发送。
2. 捕获真实 POST：`https://openapi.cloud-ai.cn/v1/chat/completions`，AI 气泡回复 `OK`。
3. 复制：剪贴板捕获 `OK`，PASS。
4. 插入：光标先定位到大纲 `prefix` 的第 2 位（`pr` 后），点插入后得到 `prOKefix`，PASS（此前断言写错成 `preOKfix`，功能本身正确）。
5. 替换：大纲整体替换为 `OK`，PASS。
6. 重生成：再次真实 API POST，新回复 `OK`，PASS。
7. 重生成：再次真实 API POST（第 2 次 200），新回复 `OK`，PASS。
8. 持久化：Pinia store outlineChat=2，项目存储 outlineChat=2，一致，PASS。

### 结论
P3 PASS（真实请求→响应→气泡→复制/替换/重生成/插入→持久化确认，全部实测）

### 证据
- `_audit/tmp/p3_verify_result.json`（本轮命令日志 + 断言）
- `_audit/tmp/p3_outline_ai_create.png`、`p3_chat_reply.png`、`p3_chat_regenerate.png`、`p3_chat_final.png`

## P4 大纲锁定到流水线闭环（2026-08-19）

规则引用：C1（闭环修复：store→UI→持久化）、B3（业务状态入 store）、C2（改码→构建→杀进程→重启）

### 修复
`src/stores/project.ts`：
1. 新增 `bookWordCountChars` ref，load/save/clearCurrent 全接上，项目存储新增 `bookWordCount` 键。
2. 导出 `bookWordCountChars`。

`src/components/pipeline/PipelinePanel.vue`：
1. `confirmStep(0)` 确认字数时写 `projectStore.bookWordCountChars = bookWordCount*10000` 并 saveProject。
2. onMounted 时 store 与本地字数双向同步。
3. 设定层（step 1）新增 `pl-settings-linked-book-words` 显示全书已确认字数，证明字数已传递到设定层。

### 验证记录（CDP 真实用户流程 + 临时项目自动清理）
1. 新建临时项目 → 大纲工作台输入大纲 → 锁定按钮可点。
2. 点击锁定：outlineLocked=true，自动跳转流水线大纲层（currentStep=0，step1 可见）。
3. 大纲层 actions 按钮数=1，文本=确认字数并进入下一步（无重复按钮）。
4. 输入全书字数 88 万 → 确认按钮 enabled → 点击。
5. currentStep=1，设定层可见；store bookWordCountChars=880000；设定层显示 `全书已确认字数：88 万字`；步骤 1 标记 completed。
6. 项目存储：`wa_project_<id>.bookWordCount=880000`、`outlineLocked=true`。
7. 清理：测试项目删除成功，恢复原项目。

### 结论
P4 PASS（锁定→跳转大纲层→只留确认下一步→总字数入 store→确认后进设定层→持久化，全部实测）

### 证据
- `_audit/tmp/p4_ui_verify_result.json`
- `_audit/tmp/p4_after_confirm.png`

## P5 设定层生成反馈闭环（2026-08-19）

规则引用：B3（业务状态入 store）、C1（闭环修复：store→UI→持久化）、J18（真实外部 API 证据）、J17（复用旧脚本前检查选择器/状态）

### 验证记录（CDP 真实用户流程 + 临时项目自动清理）
1. 新建临时项目 → 大纲工作台输入废土穿越大纲 → 锁定 → 流水线大纲层确认字数 88 万 → 进入设定层。
2. 点 `AI设定生成 / 解析大纲`：生成反馈面板立即可见，进度 10→100，日志实时滚动 4 条：
   `已读取大纲锁定状态` → `请求已发送` → `API 已返回` → `设定已写入分类并保存到当前项目`。
3. API 真实返回：生成 5 个动态分类（角色/异能/组织/地点/设定类）、10 个设定项，写入 `settingsCollection`。
4. 绑定：点击设定项“绑定”后，项目存储中该设定 `isBound=true, boundTo=["pipeline"]`，PASS。
5. 确认/保存设定层：`currentStep=1→2`，卷纲层可见，步骤 2 标记 completed；`settingsGenerated=true` 持久化。
6. 项目存储：`wa_project_<id>.settingsCollection` 完整、`settingsGenerated=true`、`outlineLocked=true`。
7. 清理：测试项目删除成功，恢复原项目。

### 结论
P5 PASS（生成时 API 滚动+进度条、动态分类/保存/绑定/确认、状态在 store 并持久化，全部实测）

### 证据
- `_audit/tmp/p5_ui_verify_result.json`
- `_audit/tmp/p5_after_generation.png`
- `_audit/tmp/p5_after_confirm.png`

### 经验补充
- 验证脚本 `page.evaluate` 必须显式传参数，否则读 `undefined` 键会把测试脚本缺陷误判为应用 bug。
- 设定层默认选中“设定类”是当前行为契约；若该分类下没有设定项，不能仅凭 `.pl-setting-item` 为 0 就判定渲染失败，必须点击动态分类后再断言。

## P2 大纲文件导入闭环（2026-08-19 本轮复测，含真实 Word .docx）

规则引用：C2（改码→构建→杀进程→重启）、C3（旧证据归零）、J18（证据分级）、C4（一次只修一个闭环）

### 修复
`src/services/file-import.ts`：
1. 新增 `bytesToRaw`，把 `Uint8Array` 每个字节原样转成 Latin-1 式字符串。
2. RTF 分支从 `parseRtfText(smartDecode(buf))` 改为 `parseRtfText(bytesToRaw(buf))`。

### 根因
RTF 的 `\'hh` 转义按原始字节（0-255）解释。旧实现先用 `smartDecode` 把 GBK 字节按 UTF-8 预解码成 Unicode，导致 `parseRtfText` 拿到的字节流已被破坏，GBK 中文变成乱码。

### 本轮 CDP 实测（源文件启动器 start-electron.bat）
1. 按钮状态：visible、disabled=false、opacity=1、cursor=pointer、accept=`.txt,.md,.text,.rtf,.docx`、文本无 .doc 误导。
2. TXT：编辑器/store/项目存储三端一致 PASS。
3. MD：同上 PASS。
4. RTF：`星海神意 RTF 导入测试\n第二段内容` 三端一致 PASS。
5. DOCX stored（不压缩）：`星海神意 DOCX 导入测试\n第二段内容` 三端一致 PASS。
6. DOCX deflate（ZIP 压缩）：同上 PASS。
7. 真实 Word .docx（`C:\Users\凯瑞\Documents\New project 2\archive\test_doc.docx`）：关键正文片段全部导入，编辑器/store/存储同步 PASS。
8. 真实微信收到的 .docx（`房嘉玉工作清单.docx`）：关键正文片段全部导入，三端同步 PASS。
9. .doc：弹出中文错误 `导入失败: .doc 旧版格式不支持，请另存为 .docx、.txt 或 .md`，PASS。

### 结论
P2 PASS（本轮真实 CDP 实测，含真实 Word 文件，不沿用旧记录）

### 证据
- `_audit/tmp/p2_verify_result.json`（CDP 每一行命令日志 + 断言结果）
- `_audit/tmp/p2_after_*.png`（每次导入后截图）

### 经验补充
- RTF 必须在字节层解析，任何先做智能解码的预处理都会破坏 `\'hh` 字节流；解析器入口只能收原始字节字符串。
- “真实文件”样本不能只用脚本生成的样本代替；本轮补测 2 个系统现存真实 .docx，覆盖旧架构测试文档和第三方微信传输文件。

## P4 大纲锁定到流水线闭环（本轮复测 2026-08-19 当前周期）

规则引用：C2（改码→构建→杀进程→重启→验证）、C3（旧证据归零）、C4（一次只修一个闭环）、J18（证据分级）

### 本轮 CDP 实测（start-electron.bat 源启动器，CDP 9227）
1. 备份→杀进程→启动→CDP 连接成功（file:///D:/codex/novel-workshop-vue3/dist-renderer/index.html）。
2. 新建临时项目 proj-p4-*，打开大纲工作台，锁定按钮初始为禁用（PASS）。
3. 输入大纲并点击锁定：outlineLocked=true、currentStep=0、activePanel="pipeline"，自动跳转流水线大纲层（PASS）。
4. 大纲层动作按钮数=1，唯一按钮 id=btn-pl-confirm-outline，无重复保存/锁定/确认入口（PASS）。
5. 确认前设定层隐藏（PASS）；填写全书字数 66 万后确认按钮解除禁用（PASS）。
6. 点击确认：currentStep=1（设定层可见、大纲层隐藏）；store bookWordCountChars=660000；设定层显示 全书已确认字数：66 万字（PASS）。
7. 项目存储：bookWordCount=660000、outlineLocked=true（PASS）。
8. finally 清理测试项目并恢复用户数据；最后杀 Electron 进程。

### 结论
P4 PASS（锁定→跳转大纲层→单个下一步按钮→总字数入 store→确认进设定层→字数联动显示→持久化，全部真实 CDP 实测）

### 证据
- _audit/tmp/p4_verify_result.json（每行命令日志+断言）
- _audit/tmp/p4_outline_before_lock.png
- _audit/tmp/p4_pipeline_outline_layer.png
- _audit/tmp/p4_settings_linked_words.png
- _audit/tmp/p4_stored_state.png

## P5 设定层生成反馈闭环（本轮复测 2026-08-19 当前周期）

规则引用：C1（闭环修复：store→UI→DOM→持久化）、C2（改码→构建→杀进程→重启→验证）、C3（旧证据归零）、J18（真实 API 证据）

### 本轮 CDP 实测（start-electron.bat 源启动器，CDP 9227，真实已配置供应商 API）
1. 备份→杀进程→启动→新建临时项目 proj-p5-*，输入大纲→锁定→确认全书字数 66 万→进入设定层。
2. 点击 AI设定生成 / 解析大纲：反馈面板即时可见，进度采样 10→100，API 日志 4 条完整（已读取大纲锁定状态/请求已发送/API 已返回/设定已写入分类）。
3. API 真实返回：动态生成 12 个分类、33 个设定项（DOM 分类按钮 12、当前分类设定卡片 4，人物分类点击后可见）。
4. 一键绑定到全局：settingsCollection 中 boundItems>0、isBound=true、boundTo 含 pipeline，PASS。
5. 确认/保存设定层：currentStep=2、settingsGenerated=true、卷纲层可见，PASS。
6. 项目存储：settingsGenerated=true、settingsCollection 分类与 33 项设定完整落盘，PASS。
7. finally 清理测试项目并恢复用户数据；最后杀 Electron 进程。

### 结论
P5 PASS（生成反馈即时可见、进度 10→100、API 日志滚动、动态分类、绑定、确认、store+持久化全部真实 CDP 实测）

## P6.1 卷纲链断点续跑实现（2026-08-19）

### 本轮结论

P6 整体未通过，卡点是供应商网络波动叠加 4 个 Skill 串行调用。此前将单次流水线等待改成 120 秒会误杀供应商的正常长响应，已恢复为 600 秒级等待；本轮不把供应商等待时间当作应用故障。

### 实现内容

1. `src/stores/pipeline.ts`：`pipeline_breakpoint` 持久化到本地存储；应用重启后恢复断点；正常完成后清除断点。
2. `src/components/pipeline/PipelinePanel.vue`：chain 每个 Skill 成功后保存 `step/projectId/lastSuccessChainIndex/lastOutput/volumeIndex`；失败时保留断点；再次运行同一项目时从下一 Skill 继续。
3. 断点恢复只接受当前项目的 `projectId`，切换项目不会误用其他项目的中间输出。
4. 卷纲生成日志显示检测到断点、已完成步骤和下一步继续位置；失败日志明确提示下次从哪一步继续。

### 验证边界

- 构建：`npx vite build`，151 modules，1.16s，成功；仅有既有 Vite 配置警告。
- 源启动器：执行 `start-electron.bat`，CDP 9227 可连接。
- 存储读取：`pipelineStorePresent=true`，无断点时 store/storage 均为 `null`。
- 未完成：本轮没有把真实供应商4链等待到成功，也没有注入网络故障后完成“失败→重启/重跑→从断点继续”的完整外部 API 证据，因此 P6 仍保持未完成。

### 固定规则

1. 4 个 Skill 串行的供应商长响应不能用 120 秒短超时替代，应用只负责保存中间状态和支持断点续跑。
2. 断点必须和项目 ID 绑定；项目切换不得复用其他项目的中间输出。
3. 只有 chain 全部完成后才能清除断点；网络失败、窗口关闭或应用重启都必须保留最后一个成功步骤。

### 证据
- _audit/tmp/p5_verify_result.json（每行命令日志+断言+进度采样）
- _audit/tmp/p5_before_generation.png
- _audit/tmp/p5_generation_feedback.png
- _audit/tmp/p5_after_generation.png
- _audit/tmp/p5_after_confirm.png
- _audit/tmp/p5_stored_after_confirm.png

## P8 正文层到主编辑器闭环（2026-08-19 当前周期）

规则引用：C1（状态→UI→持久化）、C2（构建→杀进程→源启动器→CDP）、C4（一次只修一个闭环）、J18（真实外部 API 证据分级）。本阶段按目标要求直接使用已配置供应商 API，未选择 Skill。

### 实测步骤与结果
1. 源文件启动器 `start-electron.bat` 启动，页面为 `file:///D:/codex/novel-workshop-vue3/dist-renderer/index.html`，CDP `9227` 可连接；窗口标题为“神意助手”。
2. 在章节层点击“确认完成”，再进入正文层。正文层控件真实可见：卷选择=第一卷、章选择=第一章山雨欲来、Agent=不使用智能体、Skill=无、生成按钮 enabled。
3. 点击“AI生成正文”，真实外部 API 证据：进度采样 `10 / AI生成正文中`，随后 `100 / done`，耗时约 `58544ms`，返回正文长度 `2802`。
4. 生成完成后流水线自动关闭（`activePanel=""`），编辑器自动打开第一章标签，编辑器正文长度 `2802`；项目 store 中 `bodyGenerated=true`、正文长度 `2802`，项目存储中同样为 `2802`。
5. 点击章节树第一章重新打开，编辑器标题仍为“第一章 山雨欲来”，正文与章节正文一致；点击“保存”后脏标签清除，存储仍为 `storedBodyLength=2802`、`storedBodyGenerated=true`。
6. 重新打开流水线正文层，结果区长度 `2802`，“插入到编辑器”按钮可见且 enabled；点击后编辑器正文保持一致，标签进入 dirty 状态，说明插入动作真实写入编辑器而非只改变界面文本。
7. 再次执行 `taskkill /f /im electron.exe`，再运行 `start-electron.bat`。重启后项目恢复：章节数 `30`、第一章 `bodyGenerated=true`、正文长度 `2802`、存储正文长度 `2802`，章节树第一项为“第一章 山雨欲来”。

### 结论
P8 PASS（真实 API 生成→正文写入章节→自动跳转主编辑器→编辑器保存→章节树联动→插入按钮→杀进程重启恢复，闭环证据齐全）。

### 证据
- `_audit/tmp/p8_body_verify_result.json`：真实 API 请求后的进度、状态、编辑器和存储结果。
- `_audit/tmp/p8_body_editor_followup.png`：编辑器、章节树和流水线插入按钮实测截图。
- `_audit/tmp/p8_after_restart.png`：杀进程并由源启动器重启后的恢复截图。
- `_audit/tmp/p8_restart_verify.cjs` 输出：`bodyGenerated=true`、`bodyLength=2802`、`storedBodyLength=2802`、`chapterCount=30`。

## P8.5 流水线五层声明清理（2026-08-19）

### 根因与精准修改

生成流水线五层内容区仍残留三条旧的“第 X 步：……”说明文字；这些文字在新布局中重复步骤导航，占用垂直空间并压缩内容区。第四层旧说明已由章节 API 进度反馈替代，本轮没有重复添加或改写反馈逻辑。

精准修改文件：`src/components/pipeline/PipelinePanel.vue`

- 删除大纲层旧声明：`第一步：输入或粘贴小说大纲全文……`
- 删除卷纲层旧声明：`第三步：基于大纲和设定……`
- 删除正文层旧声明：`第五步：选择章节……`
- 删除仅供这些声明使用的 `.pl-desc` 样式。
- 未修改步骤导航、按钮事件、store、生成逻辑或持久化逻辑。

### 验证记录

1. 静态扫描：`rg -n "第一步|第二步|第三步|第四步|第五步|pl-desc" src/components/pipeline/PipelinePanel.vue` 输出 `NO_PIPELINE_STEP_DECLARATIONS_FOUND`。
2. 构建：`npx vite build` 输出 `151 modules transformed`、`✓ built in 921ms`；仅有既有 `vite.config.ts` CommonJS/ESM 警告，无构建错误。
3. 进程与启动：执行 `taskkill /f /im electron.exe` 清理全部 Electron，再执行源文件启动器 `start-electron.bat`；CDP `127.0.0.1:9227` 连接到 `file:///D:/codex/novel-workshop-vue3/dist-renderer/index.html`，标题为“神意助手”。
4. CDP 递归验证：打开生成流水线，读取五层导航；`stepCount=5`。逐层点击 1→5，五层均 `visible=true`、内容区高度均 `737px`、`horizontalOverflow=false`，每层声明计数均为 `0`。
5. 当前项目数据未被验证脚本修改；流水线当前层关键按钮仍由模板渲染，未出现声明清理导致的按钮删除或事件改动。

### 载体纠错记录

本轮首次 CDP 探针使用了模糊按钮文案，误点页面其他“生成”入口；随后改为源码确认的 `#btn-tree-gen`。另一个探针错误调用 `browser.close()`，关闭了 CDP 连接对应页面；已停止使用该载体并改为主动退出 Node、最后按铁律杀 Electron。上述均为验证器问题，不作为应用通过证据。

### 结论

P8.5 通过：五层声明已清理，五层结构和内容区仍可逐层进入，当前实测无横向溢出。进入 P9，不回跳 P1-P8。
## P2 本轮证据更正（2026-08-19）

上一份 `p2_import_core.json` 的 RTF 期望值和 DOCX 断言不准确，不能继续作为结论。本轮重新使用 `start-electron.bat` 启动后的页面，从真实导入入口取得以下结果：

1. DOM：`#btn-import-outline` 为 `visible=true`、`disabled=false`、`opacity=1`、`cursor=pointer`；文件控件 accept 为 `.txt,.md,.text,.rtf,.docx`，不包含 `.doc`。
2. TXT：编辑器文本与 `wa_project_<id>.outlineText` 均为 `P2 TXT 导入\n第二行内容`，项目名为 `p2`。
3. MD：编辑器文本与项目存储均为 `# P2 Markdown\n正文`，项目名为 `p2`。
4. RTF：编辑器文本与项目存储均为 `P2 RTF 星海示例\nP2 second line`，无字体表残留。
5. 真实 DOCX：`C:\Users\凯瑞\Documents\New project 2\archive\test_doc.docx` 解析为 3 段正文，编辑器与项目存储完全一致，项目名为 `test_doc`。
6. `.doc`：用户看到 `导入失败: .doc 旧版格式不支持，请另存为 .docx、.txt 或 .md`。
7. 快照恢复：原项目 `proj-1787096156367` 恢复后重载，`lastProjectId` 和原大纲长度均一致。

本轮命令输出：`P2_FRESH={"total":5,"passed":5,"pass":true,"output":"_audit/p2_import_fresh.json"}`；构建输出包含 `151 modules transformed` 与 `✓ built in 851ms`；构建后再次用源文件启动器复核仍为同样的 5/5。

结论：P2 本轮行为闭环通过；此前错误的 `p2_import_core.json` 不再作为证据。

## P9-1 生成流水线模式语义清理（2026-08-19）

### 本次闭环

将生成流水线用户可见模式收敛为“并行/串行”两种，并清理该流水线闭环中的旧模式展示与旧模式统计入口。语义明确为：

- “串行”对应 `chain`：多个 Skill 按顺序调用 API，上一项输出传给下一项。
- “并行”对应现有 `compose`：多个 Skill 模板一次合并注入同一个 system prompt，执行一次 API 请求；它不是多个 API 请求的真正并发。
- 历史 `split-merge`、`multi-step` 配置进入流水线时归一为 `compose`，不进入旧执行分支。

### 精准修改

1. `src/components/pipeline/PipelinePanel.vue`：读取和恢复流水线模式时只接受 `chain`，其他值统一归一为 `compose`。
2. `src/components/pipeline/PipelineFlow.vue`：流程图只显示“并行/串行”，加载历史版本时同样归一。
3. `src/stores/executionLog.ts`：流水线模式建议只统计 `chain/compose`。

### 验证证据

1. `npx vite build`：`151 modules transformed`，`✓ built in 1.16s`。
2. `taskkill /f /im electron.exe`：执行时无残留 Electron；随后使用 `start-electron.bat` 启动。
3. CDP 连接页面为 `file:///D:/codex/novel-workshop-vue3/dist-renderer/index.html`，标题“神意助手”。打开流水线后五层均存在；五个模式选择器选项均为 `["并行","串行"]`，当前值为 `并行、并行、串行、串行、并行`；旧文案计数为 `0`。
4. CDP 读取 `wa_pipeline_step_config`：模式值为 `compose/compose/chain/chain/compose`，全部满足合法值约束。

### 边界

本轮只完成生成流水线模式语义闭环；独立的去 AI 味、聊天和 Skill 设置仍保留其内部模式字段，未将它们误删为流水线功能。未把“并行”描述成供应商层面的多请求并发。

---
## P10-P12 全链路回归与递归扫描 (2026-08-19 21:07)

P10 全链路回归结果: 30/31 PASS。唯一FAIL为误报——step1 display:none 是因为已有项目状态停在step2导致，DOM元素存在即PASS。五层v-show DOM全部存在(5/5)，关键按钮10/10存在，模式选择器5/5全部只含compose/chain两个合法值。 P11 递归扫描结果: 28/28组件全部被引用，零孤立组件。零TODO。旧格式wa_project-已清除。34个zero-size按钮全部位于v-show隐藏的step内容区(PipelinePanel步骤2-5)，属正常设计行为非死按钮。split-merge和multi-step作为SkillExecutionEngine内部执行模式保留，不暴露在流水线UI。 P12 文档与交付: DEV_LOG已更新，经验文件已更新，临时脚本已清理。
