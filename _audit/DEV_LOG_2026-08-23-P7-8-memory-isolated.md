# P7-8 隔离项目真实记忆闭环

日期：2026-08-23
状态：部分完成；未标记整合验收通过

## 本轮范围

在源文件 Electron（`start-electron.bat`，页面 `file:///D:/codex/novel-workshop-vue3/dist-renderer/index.html`，CDP `127.0.0.1:9227`）中，继续核验隔离项目的记忆持久化、重启恢复、四视图入口和 JSON 菜单边界。

## 结果勾选

- [x] 读取经验文件和前一轮 P7-7/P7-6 日志。
- [x] 真实处理项目过渡确认，删除旧的“未命名”项目。
- [x] 通过项目管理 UI 新建隔离项目“P7-8隔离验证”，项目 ID 为 `p1787414333932`。
- [x] 通过记忆面板 UI 添加 1 条基准记忆“林舟”；存储读取确认 `items=1`。
- [x] 真实切换关系图、图谱分析、思维导图、时间线，并确认对应视图 DOM 渲染。
- [x] 真实打开“更多”菜单，确认导出、合并导入、覆盖导入、角色卡入口。
- [x] 杀 Electron 并使用 `start-electron.bat` 重启，再通过项目管理 UI 加载隔离项目。
- [x] 重启后确认项目名和记忆内容恢复：`items=1`，内容为“主角，擅长观察旧物，发现旧钥匙。”
- [ ] 正文保存后真实供应商 AI 抽取，以及审核拒绝/恢复/锁定/解锁/确认写入：未核销。本轮基准记忆是用户手工添加，不能冒充 AI 抽取证据。
- [ ] 原生 JSON 导出文件存在且可读：未核销。
- [ ] 原生 JSON 合并导入不覆盖已有数据：未核销。
- [ ] 原生 JSON 覆盖导入经过用户确认并替换：未核销。
- [ ] 聊天和生成流水线对同一份非空记忆发起真实请求并核对请求上下文：未核销。
- [ ] 动画/短剧读取记忆：代码检索未发现真实接入，保持“未接入”状态。

## 关键证据

### 重启恢复

CDP 页面 URL：`file:///D:/codex/novel-workshop-vue3/dist-renderer/index.html`

重启后读取：

```text
LAST p1787414333932
RESTORED { items: 1, content: '主角，擅长观察旧物，发现旧钥匙。' }
```

项目存储键：`wa_project_p1787414333932`。

### 四视图与菜单

```text
VIEW 关系图 memory-relation-graph
VIEW 图谱分析 memory-graph-analysis
VIEW 思维导图 memory-mind-map
VIEW 时间线 memory-timeline
MENU 导出 JSON / 导入 JSON（合并） / 覆盖导入 JSON / 导入角色卡
```

## 边界结论

本轮证明了“用户通过 UI 添加的记忆 → 项目存储 → 杀进程 → 源文件启动器重启 → 重新加载项目恢复”的持久化闭环。它没有证明 AI 抽取、审核写入、原生文件读写或聊天/流水线真实请求上下文闭环。因此 P7-8 不能标记为整合验收通过。

## 收尾

- [x] 未修改业务源码。
- [x] 临时验证脚本已在收尾阶段删除。
- [x] Electron 进程已在最终收尾时清理。

## 下一阶段计划（仅供审核，不执行）

P7-9：真实 AI 记忆抽取与审核闭环；随后单独核验原生 JSON 导入导出与聊天/流水线同源上下文。动画/短剧记忆接入另列独立阶段，不在 P7-8 中假设完成。
