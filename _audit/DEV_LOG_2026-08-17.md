# 神意助手开发日志 2026-08-17（Phase C 修复 + 全回归验证 + Phase F 流水线可视化）

## 本次变更

### Phase C 修复（4 项失败 → 全部通过）
1. **修复验证脚本** — 设置弹窗打开后未切换到技能标签页就查找编辑按钮，导致找不到自定义变量 UI
   - 在打开设置弹窗后增加 #tab-skill 点击
   - 保存按钮选择器从 .sf-edit-form .btn-primary 改为 #btn-save-skill
   - 关闭按钮选择器从 .modal-close 改为 #btn-close-settings

2. **验证结果**: 15/15 全部通过
   - C1-7 引擎模板解析 ✅
   - C8.0 打开技能编辑弹窗 ✅
   - C8.1 自定义变量UI存在 ✅ (cv/add/ref 全部存在)
   - C9.0 添加自定义变量行 ✅
   - C10.0 刷新解析预览 ✅
   - C11.0 流水线集成 ✅
   - C12.0 chain prevResponse传递 ✅
   - C13.0 customVars持久化 ✅

### 全回归验证
- 7 项快速回归检查全部通过：A1-A3（设置弹窗/技能标签页/编辑按钮）、B1-B2（流水线面板/五层模式下拉）、C1-C2（引擎/模板解析）
- 无回归问题

## 关键经验
1. 验证脚本必须模拟真实用户操作路径（先打开设置 → 切换技能标签页 → 再点编辑），不能跳步
2. 带有 -if 条件的组件不会在 DOM 中渲染，选择器找不到元素不一定代表代码有问题，也可能是导航路径不对
3. 保存按钮选择器必须精确匹配模板中的 ID，通用选择器容易失效
4. 全回归检查比单阶段验证更重要，能发现跨阶段的回归问题

### Phase D 执行日志功能（完整实现）
1. **runStepSkills 包裹日志记录** — 将原函数拆分为 wrapper + _runStepSkillsInner，wrapper 自动记录每次调用的 step/mode/skillNames/prompt/result/duration/status
2. **执行日志 UI** — 流水线面板头部增加"执行日志"按钮，点击展开可折叠面板，显示日志列表（stepName/mode/skills/status/duration），支持展开查看详情（prompt/result），点赞/点踩反馈，优化建议，清空日志
3. **executionLog Pinia store** — 完整实现 addLog/setFeedback/removeLog/clearLogs/getStats/getSuggestions
4. **CDP 验证**: 4/4 全部通过 — 按钮存在、面板打开、空状态提示、store 注册

### 修复经验（Phase D）
1. apply_patch 工具对 </template> 附近的内容无法正确处理，必须用 Python 脚本代替
2. Python 脚本的 or 关键字在 JavaScript 中不存在，必须用 ||
3. 多次运行替换脚本会导致函数重复包裹，必须用 git checkout 恢复后一次性完成
4. 执行日志 store 必须在 PipelinePanel.vue 的 script setup 中 import 和调用，否则不会被 Pinia 注册
5. CDP 验证时按钮在 overlay 面板内，必须先打开流水线面板才能找到


### Phase E: MCP/Agent 集成
1. **src/stores/mcp.ts 创建** — MCP 服务器 CRUD + 持久化到 storageKey + 测试连接 + 调用工具
2. **src/components/settings/McpSettings.vue 创建** — MCP 配置界面，服务器列表/添加/测试连接/启用禁用/删除
3. **SettingsModal.vue 更新** — 添加 MCP 标签页 (import + template + tabs 数组)
4. **CDP 验证**: 6/6 核心功能通过 — 设置按钮存在、MCP 标签页存在、标签文字 MCP、MCP 服务器配置标题、添加服务器按钮、store 点击后注册
5. **构建验证**: npx vite build 通过

### 关键经验 (Phase E)
1. MCP store 使用 Pinia 懒加载机制，首次注册发生在组件调用 useMcpStore() 时，不是 store 文件被 import 时
2. 验证时需先点击 MCP 标签页渲染 McpSettings 组件，store 才会注册到 window.__pinia._s
3. SettingsModal 的 tabs 数组需要手动添加 MCP 条目，仅 import 和 template 不够

### Phase F: 流水线可视化 + 版本管理
1. **src/components/pipeline/PipelineFlow.vue 创建** — 流水线可视化流程图，显示 5 个步骤节点（大纲/设定/卷纲/章节/正文），每个节点显示步骤编号、名称、完成状态、执行模式标签、智能体名称、Skill 芯片
2. **版本管理** — 保存/加载对话框，支持配置名称输入、保存/加载/删除版本，持久化到 storageKey('pipeline_versions')
3. **PipelinePanel.vue 更新** — 添加"流程视图"切换按钮，<PipelineFlow> 组件条件渲染，所有步骤面板包裹在 -show="!showFlowView" 中
4. **构建验证**: npx vite build 通过（736ms）
5. **CDP 验证**: 9/9 全部通过
   - 流水线按钮存在 ✅
   - 流程视图切换按钮存在 ✅
   - PipelineFlow 组件可见 ✅
   - 5 个节点显示 ✅
   - 保存配置按钮存在 ✅
   - 加载配置按钮存在 ✅
   - 保存弹窗可打开 ✅
   - 列表切换按钮存在 ✅
   - 切换回列表视图后步骤面板可见 ✅
6. **截图**: phaseF_flow_view.png 已保存
7. **临时脚本清理**: 6 个 Phase F 辅助脚本已移至 _audit/scripts/archive/

### 关键经验 (Phase F)
1. PipelineFlow 作为独立组件，通过 props 接收 stepsWithIds/stepAgents/stepSkills/stepSkillModes，通过 emit('toggle-view') 通知父组件切换视图
2. 使用 -show="!showFlowView" 包裹步骤面板，避免与 PipelineFlow 同时渲染产生冲突
3. 版本管理使用 storageKey 持久化，保存完整配置快照（agents/skills/modes）
4. 验证脚本需要先点击流水线按钮打开面板，再点击流程视图切换按钮，不能跳步
5. 临时脚本使用后必须清理，防止残留干扰后续开发
