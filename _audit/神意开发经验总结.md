# 神意助手开发经验总结（完整版）

> 生成时间：2026-08-16
> 范围：覆盖神意（Vue3 新架构）从旧架构迁移至今的全部开发、修复、验证过程

---

## 第一卷：错误全景与根因分析

### 错误类型 A：架构迁移类

| # | 错误操作 | 根因 | 改正措施 | 避免再犯 |
|---|---------|------|---------|---------|
| A1 | 直接把旧架构代码复制到 Vue3 组件中 | 认为"代码相同=功能相同"，没意识到两个系统本质不同（旧：事件+全局变量+手动刷新；新：状态+响应式+store+computed+生命周期） | 重写为新架构写法：业务状态放 store，组件用 computed 读取，联动用 emits/watch | 每次改代码前先读旧架构分析集的手册，确认"旧架构这一步做了什么"，然后想"新架构怎么做等价"，禁止直接复制 |
| A2 | 移除 v-show 导致流水线五层全渲染 | 看到 5 层面板"隐藏"以为是 bug，没理解 step-based 导航的设计意图 | 恢复 `v-show="pipelineStore.currentStep === N"` | 在改任何"隐藏/显示"逻辑前，先确认它是不是有意为之的状态控制 |
| A3 | CSS 从单文件到模块化迁移时变量名不统一 | 用多个 CSS 文件（global.css/tokens.css/modal.css）时，变量定义分散，选择器冲突，响应式断点丢失 | 统一 tokens.css 变量定义，建立视觉基准文档 | 所有 CSS 变量必须在 tokens.css 中定义，禁止组件内硬编码（组件级设计需要除外） |
| A4 | 自动去重脚本"保留最后定义"破坏了有意层叠覆盖 | 用脚本批量去重时没考虑 CSS 的层叠顺序，删掉了前面的有意覆盖 | 重建 CSS 结构，按"基类→组件→覆盖"层级管理 | 禁止用自动去重脚本处理 CSS，每个删除必须手工确认 |

### 错误类型 B：组件化拆分类

| # | 错误操作 | 根因 | 改正措施 | 避免再犯 |
|---|---------|------|---------|---------|
| B1 | 编辑器事件监听在 App.vue 分发但 EditorPanel 没监听 | 拆分到 32 个 .vue 文件时，事件分发链路被遗漏 | 补全 EditorPanel 的撤销/重做/保存事件监听 | 拆分时先画事件流图，确认每个事件的"发出→传递→接收→处理"完整闭环 |
| B2 | v-for 内 ref 收集成数组但 .focus() 失效 | Vue 3 的 v-for ref 数组行为与 Vue 2 不同，ref 数组是函数形式 | 改用 `(el) => { if (el) refs[i] = el }` 方式 | 迁移前先确认 Vue 3 的 breaking changes，特别是 ref 和 reactive 行为 |
| B3 | 组件本地状态替代持久化状态 | 正文生成结果用组件本地 `ref` 存，用户关掉面板再打开，数据丢失，按钮禁用 | 改为从 `projectStore.chapters[vol].body` 读取的 computed | 凡是用户关掉面板再打开还需要看到的数据，必须放 store 或 storage，禁止用组件本地 ref |

### 错误类型 C：修复与验证类

| # | 错误操作 | 根因 | 改正措施 | 避免再犯 |
|---|---------|------|---------|---------|
| C1 | 修 A 漏 B，修功能不修 UI，修 UI 不修数据 | 没有"闭环修复"意识，只修了表面症状 | 修复必须覆盖：store 状态 → UI 渲染 → DOM 验证 → 截图 | 每个修复划一个闭环边界，闭环内所有层必须一起修完 |
| C2 | 不构建就断言修复生效 | 改了 .vue 源文件但没跑 `npx vite build`，Electron 加载的是旧包 | 任何源文件修改后必须 `npx vite build` 再重启 | 每次修改后按顺序：改代码 → 构建 → 杀进程 → 重启 → 验证 |
| C3 | 不杀进程就重启 | 旧 Electron 进程残留，新进程加载旧数据 | 先 `taskkill /f /im electron.exe` 再启动 | 写入铁律：每次修改代码后必须杀进程再重启 |
| C4 | 批量修复而非精准修复 | 图省事一次改多个文件，每改一个引入一个新 bug | 一次只修一个功能闭环，修完验证再进入下一个 | 硬约束：一次只改一个文件，一个闭环，修完验证再继续 |
| C5 | 声称完成但没验证 | 代码编译通过就认为"修好了"，没有做行为验证 | 必须用 CDP 做真实用户模拟：点击 → 检查 store → 检查 UI | 没有 CDP 验证结果的"已完成"都是未完成 |
| C6 | 只检查表面不递归 | 只看了按钮是否存在，没点进去看效果 | 按钮点击后必须检查：store 变化 + UI 响应 + 菜单/弹窗展开 | 每次检查必须递归深入：点击按钮 → 检查弹窗 → 点击弹窗内按钮 → 检查 store |

### 错误类型 D：脚本与工具类

| # | 错误操作 | 根因 | 改正措施 | 避免再犯 |
|---|---------|------|---------|---------|
| D1 | 脚本修改应用真实状态（seeding 项目、锁定大纲） | 以为"测试完就清掉"没问题，但脚本异常退出后 cleanup 没跑 | 脚本设计为纯读检查：只检查元素存在性、可见性、store 状态，不点击不修改 | 检查脚本必须 100% 只读，所有操作必须可逆，异常退出时必须有 cleanup |
| D2 | 脚本残留持久化数据 | 脚本通过 `electronAPI.storageWrite` 写了数据，异常退出后 cleanup 没跑，重启后数据还在 | 写脚本时 identity 每个写操作，确保 cleanup 在 finally 中执行 | 凡涉及持久化写入的脚本，必须 try...finally 保证 cleanup |
| D3 | 选择器写错导致脚本误报 | `pl-s1-agent` 没加 `#` 前缀，变成了标签名选择器 | 所有 ID 选择器必须加 `#` 前缀，脚本增加选择器验证步骤 | 脚本写完后先跑一小段验证选择器正确性，再跑完整流程 |
| D4 | 截图 fullPage 超时 | Electron 内全页截图需要等待字体加载，30s 超时 | 截图改为：先 fullPage 15s 超时，失败则降级为视口截图 | 截图函数必须带超时和降级逻辑 |
| D5 | PowerShell 转义问题 | 在 PowerShell 中内联 JSON/正则/嵌套引号时，转义字符互相冲突 | 复杂脚本用 apply_patch 写文件，或用 .js/.ps1 文件 + node/cmd 执行 | 任何含嵌套引号/JSON/正则的命令，必须写文件执行，禁止内联 |
| D6 | 总是自己写脚本而不是用现成工具 | 每次需要检查就写一个新的 .cjs 脚本，重复造轮子 | 直接使用 Playwright 标准 API，复用已有验证脚本 | 先看 `_audit/` 有没有可复用的脚本，没有才新建 |

### 错误类型 E：链路与流程类

| # | 错误操作 | 根因 | 改正措施 | 避免再犯 |
|---|---------|------|---------|---------|
| E1 | 不对比旧架构链路就下手改 | 只看了新架构代码，没看旧架构的实现逻辑，改了之后功能不完整 | 每次改功能前必须读旧架构分析集的手册，理解"旧架构为什么这么干" | 硬约束：改任何功能前，必须读对应的旧架构行为手册 |
| E2 | 用开发版启动器替代源文件启动器 | 搞混了 vite 开发服务器和 start-electron.bat 的区别 | 始终用 `start-electron.bat` 启动 | 铁律：只能用 start-electron.bat 启动，禁止其他方式 |
| E3 | 不理解应用链路就断言"修好了" | 只看了一个页面，没看点击后跳转、数据联动、状态同步 | 验证必须覆盖：面板打开 → 按钮点击 → 数据变化 → 页面跳转 → 次级页面 → 返回 | 验证链路时必须模拟真实用户完整流程 |

---

## 第二卷：技术发现与决策记录

### T1. IPC 序列化
- **问题**：Vue ref/reactive Proxy 不能直接传 Electron IPC，`ipcRenderer.sendSync` 用 structured clone 序列化，Proxy 触发 "An object could not be cloned"
- **修复**：所有 `storageWrite` 调用前必须 `JSON.parse(JSON.stringify())` 深拷贝
- **覆盖范围**：agent.ts、chapter.ts、deai.ts、project.ts、provider.ts、pipeline.ts、skill.ts、theme.ts

### T2. CDP 验证要点
- Playwright 的 `connectOverCDP` 在 `file://` 协议下 `waitForLoadState('networkidle')` 会无限挂起，必须用 `waitForTimeout` 代替
- CSS overlay 拦截 Playwright 的 `click()`，需要 `force:true` 或 `evaluate()` 方式触发
- `evaluate()` 方式触发点击比 `click({force:true})` 更可靠，因为能触发 Vue 事件处理
- 验证必须同时读 store 和真实 DOM 类名，不能只依赖一个

### T3. 构建与启动
- 构建命令：`npx vite build`（输出到 dist-renderer/）
- 启动命令：`start-electron.bat`（使用 `--remote-debugging-port=9227`）
- 每次启动前必须杀干净所有 Electron 进程
- 启动后等待 3-5 秒再连接 CDP

### T4. 存储路径
- 数据持久化到 `Documents/神意助手数据/`（由 electron/ipc/storage.js 定义）
- 键前缀：`wa_project_`、`wa_chat_`、`wa_agents`、`wa_skills`、`wa_pipeline_step_config` 等
- 卸载应用时文件不会丢失，在 `Documents/神意助手数据/` 中保留

---

## 第三卷：神意开发规则（铁律）

### 规则 1：行为等价原则（最高优先级）
```
1. 旧架构行为 = 旧架构这一步到底做了什么
2. 新架构实现 = 用 Vue3 的 store/computed/emits/lifecycle 实现等价行为
3. 禁止直接复制旧代码，两个系统本质不同
4. 唯一可信的旧行为基准在：C:\Users\凯瑞\Documents\New project 2\旧架构分析集
```

### 规则 2：新架构写法标准
```
1. 业务状态必须放 Pinia store，禁止用全局变量替代
2. 组件读取状态只能用 computed，禁止各自保存一份不同步的数据
3. 子组件不能直接改父组件状态，必须通过 emits 通知父组件
4. 联动靠 store 更新 + computed/watch 响应，禁止手动刷新式到处调用
5. 同一个业务数据的单一事实来源只能有一份，禁止双份来源
6. 表单用 v-model，面板打开关闭用生命周期和状态控制
```

### 规则 3：修复纪律
```
1. 一次只修一个功能闭环，禁止批量改一堆无关功能
2. 每个闭环修完输出完整验证结果，再进入下一个
3. 修复必须覆盖：store 状态 → UI 渲染 → DOM 验证 → 截图
4. 禁止自动去重/批量替换，每个修改必须手工确认
5. 先删后改：删除旧代码再写新代码，不留死代码
6. 遇到旧架构和新架构无法对齐的行为，先停下来报告，不要自己绕路
```

### 规则 4：验证纪律
```
1. 没有 CDP 验证结果的"已完成"都是未完成
2. 验证必须走真实用户操作：点击 → 检查 store → 检查 UI → 截图
3. 检查必须递归深入：按钮点击后检查弹窗，弹窗内按钮再检查
4. 验证必须同时读 store 和真实 DOM，不能只依赖一个
5. 每次修改后按顺序：改代码 → 构建 → 杀进程 → 重启 → 验证
6. 截图函数必须带超时和降级逻辑
```

### 规则 5：脚本纪律
```
1. 检查脚本必须 100% 只读，不点击不修改不 seeding
2. 所有脚本必须 try...finally 保证 cleanup
3. 凡涉及持久化写入的脚本，cleanup 在 finally 中执行
4. 所有 ID 选择器必须加 # 前缀
5. 复杂脚本（含嵌套引号/JSON/正则）必须写文件执行，禁止内联
6. 脚本跑完后必须清理残留，不留脏状态
```

### 规则 6：工程纪律
```
1. 只用 start-electron.bat 启动，禁止其他方式
2. 每次启动前必须 `taskkill /f /im electron.exe` 杀干净进程
3. 源文件修改后必须 `npx vite build` 再重启
4. 改任何功能前，必须读对应的旧架构行为手册
5. 先读代码再改，用 apply_patch 编辑，验证后再声称完成
6. 一个工具调用失败 2 次后换方案，不重试同一格式
```

### 规则 7：UI 统一纪律
```
1. 所有 CSS 变量必须在 tokens.css 中定义，禁止组件内硬编码
2. 弹窗统一先定基类（modal.css），再逐个组件替换
3. 按钮统一使用 global.css 定义的 .btn-primary/.btn-secondary/.btn-danger
4. 字号统一按 tokens.css 的 --font-size-xs/sm/md/lg 四级
5. 组件级字号硬编码保留为设计需要，不加全局覆盖
6. 同一 UI 只有一种写法，发现重复立即清理
```

### 规则 8：链路完整性
```
1. 每次改功能前先画链路图：输入→处理→输出→联动→存储
2. 验证时必须模拟真实用户完整流程，不能只看一个页面
3. 字数链必须自动传导：大纲字数→卷数→每卷字数→章数
4. 关闭应用时的状态必须持久化，重启后恢复
5. 所有按钮必须有可见入口，禁止无入口的功能
```

### 规则 9：GitHub 版本管理铁律
```
1. 每次代码变动必须提交并上传 GitHub，禁止只改本地不推送
2. 神意仓库为 GitHub 私有仓库（shenyi），不对外公开
3. 提交前必须检查 git status，确认本次提交只包含本次变更内容
4. 提交信息必须写明改动内容（格式：类型: 组件 - 改动描述）
5. 推送失败时先排查远程/认证，不重复 push 同一错误命令
6. 每次上传后输出推送结果，证明远程仓库已同步
```

### 规则 10：经验回写铁律
```
1. 每次开发前必须读取 _audit/神意开发经验总结.md，确认已遵守全部规则
2. 每次开发完成后必须更新 _audit/神意开发经验总结.md，记录新错误/新经验
3. 每次开发完成后必须更新 _audit/DEV_LOG_*.md 开发日志，按日期追加
4. 经验总结和开发日志必须与代码变更一起提交 GitHub
5. 禁止出现“改了代码但没更新文档”的未闭环状态
```

### 规则 11：验证作用域铁律
```
1. 相同 class 可能出现在多个组件，测量/选择必须限定组件作用域（如 #ow-chat-messages）
2. 全页面 querySelectorAll 统计同一 class 的结果不能作为组件验证证据
3. 字号修复必须走真实输入→发送→渲染链路，不得用临时 DOM 注入冒充
```

---

## 第四卷：已记录的错误快照

> 以下为开发过程中实际发生的错误，已修复并验证，保留作为历史参考。

| 日期 | 错误 | 影响 | 修复方式 |
|------|------|------|---------|
| 08-13 | IPC 序列化失败（An object could not be cloned） | 所有 storageWrite 可能崩溃 | 全部加 JSON.parse(JSON.stringify()) |
| 08-14 | 自动去重 CSS 破坏层叠覆盖 | 样式错乱 | 恢复被误删的 CSS，手工重建 |
| 08-15 | AI 共创按钮被 v-show 隐藏 | 用户看不到按钮 | 移出 v-show，常驻显示 |
| 08-15 | 编辑器 8 个功能按钮被 v-if 隐藏 | 非 body 模式用户看不到 | 去掉 v-if 条件 |
| 08-15 | 查找/替换栏无入口 | 只有 Ctrl+F 可触发 | 新增 #btn-find 按钮 |
| 08-16 | 流水线 v-show 被移除导致五层合一 | 所有层同时渲染，界面混乱 | 恢复 v-show 条件 |
| 08-16 | 项目名只在"新建"分支初始化 | 文件导入后项目名为空 | 从 outline 首行提取并回写 |
| 08-16 | 按钮失效根源：函数结构损坏 | 所有 Skill 添加按钮不工作 | 重写 addStepSkill/removeStepSkill |
| 08-16 | 追加设定无反应 | @click 指向不存在的函数名 | 改为 openAddSettingModal |
| 08-16 | 脚本选择器错误 | 漏了 # 前缀导致 0/15 选择器存在 | 修复选择器，加验证步骤 |
| 08-16 | 截图 fullPage 超时 | 整页截图等待字体加载超时 | 加 shot() 降级函数 |
| 08-16 | 深度脚本 seeding 后 cleanup 没跑 | 临时项目持久化残留 | 写 cleanup 脚本清理 |
| 08-16 | 大纲工作台 AI 聊天字号过小 | API 气泡 12px / 按钮 10px / 输入框 12px 阅读费眼 | 气泡 14px、按钮 12px、输入框 13px，走真实发送链路验证 |

---

> 本文件由 Codex 整理，基于 08-13 至 08-16 全部开发日志、修复记录、验证报告汇总而成。
> 每次新的错误发生后，必须更新本文件，保证开发经验的持续积累。

### P2 本轮证据更正（2026-08-19）

上一份 `p2_import_core.json` 的 RTF 期望值和 DOCX 断言不准确，不能继续作为结论。本轮重新使用 `start-electron.bat` 启动后的页面，从真实导入入口取得以下结果：TXT、MD、RTF、真实归档 DOCX 和 `.doc` 错误边界共 5 项全部通过；编辑器文本、Pinia/项目存储文本和项目名按样本契约逐项核对；构建后重启再次复核仍为 5/5。新鲜证据为 `_audit/p2_import_fresh.json` 与 `_audit/p2_import_fresh.png`。

新增规则：验证器的期望值必须来自明确的样本契约，不能用文件名或推测字符串替代实际内容断言；证据记录发现错误时，必须先撤销该证据的结论并重新实测，不能沿用旧报告。

---

### injectMode 验证补充（2026-08-18）

| # | 错误操作 | 根因 | 改正措施 | 避免再犯 |
|---|---------|------|---------|---------|
| J6 | chain 模式把当前 Skill 模板同时拼进链路说明文本和 injectMode 注入区 | 没有把“链路上下文”和“Skill 注入通道”分成两个职责，导致 `user_prefix`/`user_suffix` 重复出现或落入错误位置 | chain 中间提示只传递上一步输出；当前 Skill 模板统一经过 `getPromptParts`，由 `system_prefix/user_prefix/user_suffix` 决定唯一注入位置 | 同一个 Skill 模板只能有一个注入来源；验证必须检查每个标记出现次数、所在 role 和相对顺序，不能只检查请求是否发出 |

**本轮验证边界**:
1. compose：一次 API 调用；system 中只出现 system Skill，user 中按“前缀→业务正文→后缀”排列。
2. chain：多次 API 调用；每一步只使用当前 Skill 的注入模式，并把上一步输出传给下一步。
3. 验证通过前不允许把“源码存在”“构建通过”写成行为完成；必须读取真实 fetch 请求消息并检查 DOM 操作路径。

### 设定层融合后 UI 拥挤修复（2026-08-18）

| # | 错误操作 | 根因 | 改正措施 | 避免再犯 |
|---|---------|------|---------|---------|
| J7 | 设定合集融合到生成流水线后，分类栏、设定卡片、属性输入和操作按钮继续沿用旧的横向 Flex 布局，内容被挤压且卡片高度不稳定 | 融合只完成了功能迁移，没有重新按“分类导航 + 内容编辑区 + 卡片操作区”的新组件布局建立尺寸约束；旧规则还与后一套规则并存 | 删除旧的 `.pl-settings-list`/`.pl-setting-item` 重复规则，使用两列 Grid 分离分类栏和内容区；设定卡片改为“名称/分类/绑定/删除”首行、“属性编辑”整行；补充移动端降级布局 | UI 融合后必须重新测量真实 DOM 的宽高、文字可读性、按钮位置和输入区高度；先删除旧规格再保留唯一规则，不得靠第三套覆盖层叠修补 |
| J8 | 绑定/删除按钮的布局选择器使用了模板中不存在的 class，导致网格定位不生效 | CSS 假设了按钮类名，未以实际模板 DOM 为准 | 改用当前模板真实存在且范围受限的 `button:nth-of-type(1/2)`，并在 CDP 中逐项读取按钮矩形和可见状态 | 修改 CSS 前必须先读模板确认 class；验证必须包含按钮 `text/rect/display/visibility`，不能只看源码选择器 |

**本次设定层 UI 验证证据**:
1. `npx vite build` 通过，151 modules。
2. 源文件启动器 `start-electron.bat` 启动，CDP 端口 `9227` 可连接。
3. 真实设定数据下测得：布局 `894x220`、分类栏 `200x220`、内容区 `674px`、设定卡片 `668x162`；名称输入 `330x36`、分类选择 `180x34`、属性编辑 `638x86`。
4. CDP 递进验证：打开设定层 → 新增设定 → 保存 → 检查新条目进入分类 → 删除临时条目；新增设定、AI生成设定、保存设定到合集、确认完成、新增分类按钮均可见。
5. 临时数据已清理；验证脚本应归档到 `_audit/scripts/archive/`，不得留在工作脚本目录。

### 错误类型 F：Phase A 新增

| # | 错误操作 | 根因 | 改正措施 | 避免再犯 |
|---|---------|------|---------|---------|
| F1 | 代码改完但不做 CDP 真实验证就声称完成 | 认为"代码能编译=功能正常"，没意识到编译通过和运行时行为是两回事 | 每次改码必须：改码→npx vite build→杀 Electron→重启→CDP 真实行为验证→截图 | 铁律：代码改完后必须跑 CDP 验证脚本，验证按钮存在性、点击行为、弹窗、store 状态变化，一项都不能少 |
| F2 | 验证脚本写完后不跑就跑偏做其他事 | 认为"脚本写好了=验证完成了"，脚本没跑就切到下一个任务 | 脚本写完后立即 node 执行，验证结果写入报告 | 脚本→执行→报告是原子操作，禁止拆分到不同轮次 |
| F3 | 多个测试按钮但只检查第一个 | 认为"第一个能点=全部能点"，没检查不同按钮的差异 | 每个测试按钮都检查：点击→弹窗→输入→运行→结果 | 验证时遍历所有同类按钮，不是只检查第一个 |
| F4 | 验证报告写完后不看就过 | 认为"报告生成了=验证通过了"，没检查报告内容 | 读取报告验证结论行，确认 passed 值正确 | 报告生成后必须读报告确认结论，不能只看脚本 exit code |
 | F4 | 验证报告写完后不看就过 | 认为"报告生成了=验证通过了"，没检查报告内容 | 读取报告验证结论行，确认 passed 值正确 | 报告生成后必须读报告确认结论，不能只看脚本 exit code |

 ### 错误类型 G：Phase B 新增
 
 | # | 错误操作 | 根因 | 改正措施 | 避免再犯 |
 |---|---------|------|---------|---------|
 | G1 | Playwright evaluate 传多参数报错 | 不知道 evaluate 只接受 1 个参数，传递了 (sel, key) 两个参数 | 将所有参数包成对象 `{ sel, key }` 传递 | 所有 Playwright evaluate 回调必须只接受一个参数，多个参数必须包成对象 |
 | G2 | 验证后未恢复原始模式配置 | 认为"测试完状态不重要" | 每个模式切换后必须 restore 原始值，确保不遗留测试配置 | 修改前记录原始值，修改后恢复，finally 中保证执行 |
 | G3 | 真实 API 调用验证耗时估计不足 | 以为 split-merge 和 multi-step 几秒能完成，实际各 5-15 秒 | 设置合理 timeout（300s），mock 测试与真实 API 测试分开 | 真实 API 调用必须预设足够 timeout，mock 测试先跑验证编排逻辑 |
 | G4 | 截图 fullPage 模式下超时 | 部分页面加载完成后 fullPage 截图仍超时 | 截图改为非 fullPage (viewport) | 验证脚本的截图统一用 fullPage: false，避免字体加载等待 |
 
 ## 错误快照追加（Phase B）
 
 | 日期 | 错误 | 影响 | 修复方式 |
 |------|------|------|---------|
 | 08-16 | Phase B 验证脚本 evaluate 多参数 | 验证脚本崩溃 | 改为对象参数 |
 | 08-16 | 验证后模式未恢复 | 用户配置被测试覆盖 | restore 原始值，finally 中执行 |
 | 08-16 | 真实 API 调用耗时超预期 | 验证脚本未完成 | 设置 300s timeout |


---

### Phase E: MCP/Agent 集成 — 2026-08-17

**错误操作**:
1. 验证脚本默认检查 store 注册状态，但未考虑 Pinia 懒加载机制，导致首次检查失败
2. 验证脚本的 #btn-settings 选择器依赖特定 ID，如果模板改动 ID 会失效

**改正措施**:
1. 先点击 MCP 标签页触发组件渲染，再检查 store 注册状态
2. 验证脚本使用多层选择器 + 兜底判断

**避免再犯**:
1. 验证前先确认组件渲染路径（是否需要先点击某个按钮/标签页）
2. 对懒加载组件/stores，先触发渲染再验证

**神意开发规则新增**:
- 铁律：每次变动必须上传 GitHub 私有仓库
- 铁律：每次开发前读取经验文件，开发完更新经验文件和开发日志
- 行为等价原则：新架构用 Vue3 的响应式/组件化/生命周期实现，不做代码移植

### 全应用美化 G1-G4 新增经验（2026-08-17）

| # | 错误操作 | 根因 | 改正措施 | 避免再犯 |
|---|---------|------|---------|---------|
| H1 | 只改 scoped 组件样式以为弹窗变大了 | scoped 样式优先级低于全局 modal.css，实际被覆盖 | 全局 .modal-content.modal-lg 宽度提到 960px，删除组件内无效覆盖 | 弹窗尺寸改动后必须通过 CDP 读取实测宽度，不能只看源码 |
| H2 | 设置弹窗 tab-api id 重复 | 标签按钮与 modal-body 共用一个 id，验证脚本选择器命中错误节点 | modal-body 改为 #settings-modal-body | 改 UI 前用脚本扫描重复 id；验证选择器必须唯一 |
| H3 | MCP 标签页点击后 TS 报类型错 | settings store 的 activeTab 联合类型没加 'mcp' | 类型与 setActiveTab 参数同步补齐 | 新增标签页时必须同步更新 store 类型和动作签名 |
| H4 | 验证测到只读卡片而不是编辑表单 | 没有进入"编辑/添加供应商"子状态 | 脚本先点编辑/添加再测量 input | 验证必须模拟真实用户点击链，先进入能输入的 UI 再量尺寸 |
| H5 | 行高断言 1.7 失败 | 浏览器 computedStyle 行高返回 px（23.8px）而不是 1.7 | 断言接受 23.8px ≈ 1.7 | 样式验证按 computed 值断言，按比例换算 |

### UI 递归统一 P0-P11（2026-08-17）

| # | 错误操作 | 根因 | 改正措施 | 避免再犯 |
|---|---------|------|---------|---------|
| H6 | tokens.css 有 11 个重复定义的变量，值不同导致层叠冲突 | 早期手写 token 时直接写值，未做别名引用 | 改为 var(--space-1) 别名引用，补充缺失的 --font-size-xxs/--font-size-xxl/--space-14 | 新增 token 前先检查是否已有等价变量，优先别名引用 |
| H7 | 38 个组件 scoped 样式硬编码 443 处 px 值 | 开发时图快直接写数值，未引用 token | 用 token_replace.js 脚本批量替换为 var() 引用 | scoped 样式禁止硬编码字号/间距/圆角，必须引用 token |
| H8 | 旧按钮 class（btn-confirm/btn-cancel/btn-stop）13 处残留 | 迁移时未统一替换 | 全部替换为 btn-primary/btn-secondary/btn-danger/btn-sm btn-ghost | 新增按钮必须用标准 class，禁止自造 class 名 |
| H9 | 验证脚本只测了主页面 L1，没递归到 L2/L3 弹窗 | 验证范围不够深 | 写 ui_recursive_verify.js 做三层递归 CDP 验证（L1 主页面→L2 设置弹窗→L3 技能编辑子弹窗） | UI 验证必须三层递归：主页面→弹窗→弹窗内弹窗 |
| H10 | 临时脚本留在 _audit/scripts/ 干扰后续开发 | 脚本用完没归档 | 每次任务收尾时把临时脚本移到 _audit/scripts/archive/ | 铁律：脚本用完立即归档，不留根目录 |

**UI 递归统一关键经验**:
1. 社区参考（Open Props 的 token 分层 + Reka UI 的无样式组件）优于自己拍脑袋设计
2. 令牌标准化顺序：先定义 tokens.css → 再替换 CSS 文件 → 再替换组件 scoped → 最后清除旧 class
3. CDP 三层递归验证是唯一可信验证方式：L1 主页面 → L2 弹窗 → L3 弹窗内弹窗，逐层读取 computedStyle
4. 458 处替换（CSS 25 + 组件 443 + 语义颜色 15）必须脚本化，手工替换不可靠
5. 保留 6 处有设计意图的硬编码（32px/24px 装饰图标 + 99px pill 胶囊圆角），不一刀切

### UI 视觉修复 V2（2026-08-17）

| # | 错误操作 | 根因 | 改正措施 | 避免再犯 |
|---|---------|------|---------|---------|
| H11 | 把UI统一理解成变量名统一，458处px替换成var()但值不变，用户零感知 | 偏差1：UI统一=用户视觉感知一致，不是变量名一致 | 先修用户能看到的问题（按钮位置/卡片大小/字体可读），基础设施在后面 | 修复前问自己：用户能看到这个变化吗？看不到就不算修复 |
| H12 | 验证脚本只检查字号数值一致(12px=12px PASS)，没检查按钮位置/卡片高度 | 偏差2：验证标准错误 | CDP截图修前修后对比+computedStyle+人眼判断合理性 | 验证必须包含截图对比，不能用数值一致代替视觉合理 |
| H13 | 只修用户报告的3个问题，没主动扫描其他UI表面 | 偏差3：只修被报的不主动扫 | 全量扫描34个组件CSS，发现13类问题 | 每次修复必须先全量扫描，不能只修被报的 |
| H14 | node -e内联命令在PowerShell里转义失败，反复重试浪费轮次 | PowerShell把\n解释成n | 用Set-Content写.js文件再node执行，或用PowerShell行号操作Get-Content/Set-Content | 禁止node -e内联，复杂操作写文件 |
| H15 | DeAiModeCard.vue被修改但实际未被引用，去AI设置页用的是DeAiSettings.vue里的.deai-mode-card | 未确认组件实际引用关系就改 | 改之前先rg搜索组件是否被import | 修改前先确认目标class/组件在实际运行中被渲染 |

**V2关键经验**:
1. 视觉修复顺序：用户报告问题优先 > 主动扫描发现的问题 > 基础设施(token替换)
2. CDP验证必须用正确的选择器：#btn-pipeline不是textContent匹配
3. PowerShell here-string会吃\n，用Set-Content + Get-Content行号操作更可靠
4. scoped样式[data-v-xxx]优先级高于全局，修改后要检查构建产物
5. provider-list class不在DOM上(容器class为空)，grid样式要加到正确的容器

### 设定合集融合 (2026-08-17)

| # | 错误操作 | 根因 | 改正措施 | 避免再犯 |
|---|---------|------|---------|---------|
| I1 | 融合时只改了 PipelinePanel 的 UI，忘了 ChatPanel 还在读旧 settings | 只修了主入口，没全局搜索旧字段引用 | rg 搜索 projectStore.settings 后逐一替换 | 数据层迁移必须全局搜索旧字段所有消费方，不能用完就删 |
| I2 | migrateSettingsToCollection 把 attrs 对象压扁成 { desc } | 简化了数据结构，丢失原字段 | 保留 attrs 原对象，content 用 attrsText 或 JSON.stringify(attrs) | 迁移逻辑必须 1:1 保留原数据，禁止字段简化 |
| I3 | isBound 迁移后丢失 | 新增 collection 时硬编码 isBound:false | 迁移时取 s.isBound || settingBindings.length > 0 | 历史数据的布尔状态必须迁移，不能默认 false |
| I4 | genSettings 每次生成都重复追加 | 没检查已有条目 | 按 name+category 去重 | AI生成写入前必须检查 collection 中是否已有同名条目 |
| I5 | 分类侧栏无默认选中，首次进入空屏 | 没监听分类变化 | watch(settingCategories) 自动选第一个 | 有列表数据的面板必须有默认选中项 |

### P1-P6 阶段验证经验 (2026-08-17 追加)

| # | 错误操作 | 根因 | 改正措施 | 避免再犯 |
|---|---------|------|---------|----------|
| I6 | CDP验证脚本WebSocket偶发Runtime.enable超时 | Electron刚启动CDP服务未就绪 | 重启Electron后等待10秒再连, 脚本加8秒超时退出 | CDP连接前确保Electron完全启动, 不在启动后立即连 |
| I7 | PowerShell here-string转义导致脚本文件写入失败 | 单引号嵌套在双引号命令中被解析器吃掉 | 用 `@'...'@` here-string写文件, 避免内联引号 | 复杂脚本一律用here-string写到.ps1文件再执行 |
| I8 | Electron启动报SingletonLock/DevToolsActivePort冲突 | 上次进程残留锁文件 | 每次启动前清理锁文件+用独立user-data-dir | 启动前必须杀进程+清理临时目录 |
| I9 | CDP选择器用假设的class名导致验证误报FAIL | 没确认实际DOM结构就写选择器 | 先用rg搜索组件template确认实际class/id, 再写选择器 | 验证脚本选择器必须从源码确认, 不能假设 |
| I10 | 验证脚本node进程不退出导致后续命令hang | WebSocket close后event loop未空 | 脚本末尾必须 `process.exit(0)` 显式退出 | 所有CDP脚本必须显式exit, 不依赖自然结束 |

**P1-P6 关键经验:**
1. CDP真机验证是唯一可信验证方式, 每个阶段必须模拟真实用户操作(点击/输入/检查store)
2. PowerShell转义是最大时间杀手: 复杂命令必须用here-string写文件, 禁止内联
3. Electron启动参数 `--disable-gpu --no-sandbox --user-data-dir=临时目录` 是稳定启动的标准配置
4. 孤儿组件扫描必须用递归JS脚本(不是PowerShell), 因为需要读取每个文件内容做字符串匹配
5. 验证脚本选择器必须从源码确认, 不能用假设的class名
6. 一次只修一个闭环, 修完构建→杀进程→重启→CDP验证, 不跳步

### SKILL 增强需求（2026-08-17）

| # | 错误操作 | 根因 | 改正措施 | 避免再犯 |
|---|---------|------|---------|---------|
| J1 | 在旧构建产物上验证新源码 | 改码后没有先确认 Electron 实际加载的 hash 产物，导致 `providerStore.callApi` 缺失的旧包错误掩盖真实行为 | 先 `npx vite build`，再杀干净 Electron，必须用 `start-electron.bat` 重启后才做 CDP 验证 | 验证前读取 `location.href` 和构建产物 hash；构建不同步时停止测试，不把旧包失败归因给源码 |
| J2 | 章节补充验证脚本重复点击流水线入口 | 没判断弹层当前是否已打开，第二次点击触发 toggle 关闭，造成“按钮不存在”的假失败 | 验证脚本先判断 `#pipeline-panel` 是否存在，只在未打开时点击，再显式切到章节层 | 所有 toggle 入口都要先读当前 DOM 状态，不能无条件重复点击 |
| J3 | 用不符合 chain 语义的 mock 验证补充逻辑 | chain 的第二个 SKILL 会接收并返回上一个 SKILL 的结果，mock 让第二步替换了第一步章节集合，制造了假缺章 | mock 必须模拟每个 chain 步骤的输入输出传递，再检查最终集合、调用次数和提示词 | 测试多步链路时先画调用序列：每一步输入、输出、下一步接收内容，禁止只按调用次数造返回值 |
| J4 | 只检查“按钮存在/请求发出”就判定章节补充完成 | 没有同时检查补充提示词、最终章节数、标题去重、断点状态 | CDP 真实流程验证：打开面板→进入章节层→点击按钮→读取 API 请求→读取 project.chapters→读取 pipeline.breakpoint→确认恢复原始状态 | 章节补充闭环必须同时满足：补充调用发生、`已有N章`提示正确、最终数量达标、标题唯一、断点记录正确 |
| J5 | 把 outputFormat 当成 schema 校验能力 | 当前实现只做 JSON.parse 失败重试，不做字段 schema 或数组长度校验 | 明确当前契约：`outputFormat=json` 只触发解析和一次重试；内容校验仍由 `validateChapters/validateVolumes` 等业务函数负责 | 后续扩展 schema 前先单独定义校验契约和失败恢复策略，禁止把“可解析”声称为“字段完整” |

### SKILL 增强需求验证结论（2026-08-17）

1. **需求1 P0：chain 内逐 SKILL Agent 覆盖**：已通过。chain 芯片可独立选择 Agent，配置写入 `skillAgents`，运行时每个 SKILL 使用自己的 Agent system prompt。
2. **需求2 P0：chain 中间状态与断点续跑**：已通过。每步成功后保存 `lastSuccessChainIndex/lastOutput/volumeIndex`，恢复从下一步继续，完成后清理断点。
3. **需求3 P1：章节补充生成**：已通过。章节不足时按剩余数量补充，提示词带 `已有N章，继续从第N+1章`，重复标题不追加，最终章节数和断点达标。
4. **需求4 P2：outputFormat=json 基础实现**：已通过。非法 JSON 自动重试一次并追加合法 JSON 提示，合法结果继续进入后续 chain；本阶段不包含自定义 schema。

### 本轮验证纪律补充

- 验证脚本只使用页面内临时 mock，并在 `finally` 恢复 `fetch`、项目卷/章节、技能格式和断点；不写入用户项目文件。
- 脚本执行后必须归档到 `_audit/scripts/archive/`，不能停留在应用源码或运行目录。
- “源码有实现”“构建通过”“按钮可点击”“完整行为通过”是四个不同层级，必须分别记录证据。

### 设定层按钮语义与行为等价修复（2026-08-18）

| # | 错误操作 | 根因 | 改正措施 | 避免再犯 |
|---|---------|------|---------|---------|
| J8 | 把“AI设定生成/连通大纲/分类分配”中的行为说明拆成独立按钮，并把保存与确认拆成两个入口 | 没有区分用户可见命令和命令执行后的内部效果，导致入口重复、行为边界混乱 | 删除独立连通大纲按钮和独立保存按钮；保留 `AI设定生成` 作为唯一的大纲读取/API/SKILL/动态分类入口，保留 `确认/保存设定层` 作为全量保存、点亮和进入卷纲的唯一确认入口 | 需求文字中“/”后的内容先判断是按钮还是效果说明；只有用户明确要求独立操作时才增加入口 |
| J9 | 生成设定按钮在大纲未锁定时仍可点击 | 只检查 `hasOutline`，没有把大纲已确认/锁定作为设定层输入契约 | `genSettings` 与按钮 disabled 同时要求 `hasOutline && outlineLocked`，生成提示明确读取已确认大纲 | 跨层联动必须读取上游最终确认状态，不能只检查是否有草稿内容 |
| J10 | 验证器无条件选择第一个分类，空的“设定类”导致误报没有设定项 | 测试夹具没有按真实数据递进选择有内容分类 | 验证器选择有设定项的动态分类，并在 `finally` 恢复完整项目快照 | 递进验证先读取当前状态，再选择能继续完成闭环的真实入口；脚本失败先判断夹具还是应用 |

**本轮行为验证证据**:
1. `npx vite build` 通过，151 modules；仅有既存 deai CommonJS 警告。
2. 使用 `start-electron.bat` 启动，CDP `9227` 可连接；未使用开发服务器。
3. `_audit/screenshots/settings_layer_semantics_verify.json`：11/11 PASS。
4. 真实递进路径：打开流水线 → 进入设定层 → 检查旧入口删除 → 检查动态分类和新布局 → 打开有内容分类 → 切换设定项绑定 → 点击“确认/保存设定层” → 当前步骤从设定层切换到卷纲层。
5. 验证器保存并恢复了原项目快照；本轮没有把临时数据留在项目存储中。
6. 本轮未触发真实 API 生成，故不把模型返回内容、SKILL 输出质量写成已验证。


### 设定层分类导航与当前分类内容框拆分经验（2026-08-18 追加）

| # | 错误操作 | 根因 | 改正措施 | 避免再犯 |
|---|---------|------|---------|---------|
| J11 | 把动态设定分类导航放进设定内容框，导致分类按钮和当前分类内容视觉上混在同一个框内 | 把“分类选择区”和“当前分类编辑区”当成一个工作区容器，没有把用户确认的层级结构映射为独立 DOM 区域 | 删除旧左右栏容器的边框职责；分类导航作为独立横向导航渲染；新增 .pl-sc-content-frame，只包当前分类编辑器和该分类操作区 | 先画出并核对 DOM 层级：Agent/模式行 → Skill 行 → 独立分类导航 → 当前分类内容框 → 分类操作 → 底部全层操作；导航不得成为内容框后代 |
| J12 | 内容框没有只承载当前分类，切换分类时无法用结构证据证明显示的是所选分类 | 只检查页面是否出现分类按钮，没有验证点击后的标题、内容容器归属和边界关系 | CDP 逐个点击可见分类，读取当前标题、活动状态、内容框高度，并断言内容框不包含导航 | 每个动态导航都要验证“点击 → 活动状态 → 当前分类标题/内容变化 → 导航仍在框外”，不能只做静态截图 |

**本轮新增硬规则**:
1. 分类导航是独立结构，不得放进 .pl-sc-content-frame、卡片或滚动内容容器。
2. .pl-sc-content-frame 只负责当前分类的编辑内容与该分类底部操作；全层确认/保存必须位于内容框外的最底部。
3. “显示在页面上”不等于“结构正确”：必须同时检查 DOM 包含关系、实际坐标顺序、边框范围、点击后的状态变化。
4. 修改布局前先删除旧布局规格和旧容器职责，再建立唯一的新结构，禁止用额外覆盖层叠补救。

### P6 卷纲生成反馈与超时收尾经验（2026-08-18 追加）

| # | 错误操作 | 根因 | 改正措施 | 避免再犯 |
|---|---|---|---|---|
| J13 | 卷纲 API 请求悬挂时，界面长期停在 10% 和“等待 API 返回”，没有终态反馈 | 卷纲层调用 `runStepSkills` 时未传入超时；chain 内任一步请求未收敛，`genVolumes` 无法进入成功或失败分支 | 为卷纲生成调用增加 120 秒受控超时；异常路径写入“API 调用失败”日志并恢复生成按钮状态 | 每个真实 API 闭环都必须有成功、失败、超时三种终态；不能只验证请求发出 |
| J14 | 验证器 180 秒后提前结束，把多步 chain 的合法等待误判为应用无终态 | 验证窗口短于 chain 各步骤的最大超时总和 | 验证窗口调整为 300 秒，并为请求/响应记录时间戳 | 验证等待上限必须覆盖“步骤数 × 单步超时 + 页面收尾时间” |

**P6 验证证据**:
1. `npx vite build` 通过，151 modules；仅有既存 deai CommonJS 警告。
2. 先杀 Electron，再使用 `start-electron.bat` 源启动器；CDP `9227` 可连接。
3. `_audit/P6_volume_generation_feedback_verify.json`：全部 11 项 PASS。
4. 真实路径覆盖：进入卷纲层 → 点击逐卷生成 → 卷框内显示 API 日志和进度条 → API 请求发出 → 超时后显示“API 调用失败：API超时” → 生成状态为失败且按钮恢复可用。
5. 验证器在 `finally` 中恢复项目快照；没有把临时卷纲写入用户项目。

**新增硬规则**:
1. API 反馈 UI 必须有“请求中、成功、失败/超时”三态，不能只有 loading 态。
2. 真实验证器的等待窗口必须按最坏链路时间计算，并记录请求/响应时间。

### P7 卷纲层字数联动闭环（2026-08-18）

**精准修改**:
1. 卷纲层新增只读状态区，明确显示“大纲已锁定全书字数”。
2. 自动分配提示明确显示“按每卷 X 万字自动分配 Y 卷”。
3. 保留 `linkedVolumeCount` 作为唯一计算来源；大纲字数存在时卷数输入保持只读。

**验证证据**:
1. 构建：`npx vite build` 通过，151 modules；仅有既存 CommonJS 警告。
2. 启动：杀干净 Electron 后使用 `start-electron.bat`，CDP 9227 正常。
3. `_audit/P7_volume_word_link_verify.json`：11/11 PASS；截图：`_audit/P7_volume_word_link_verify.png`。
4. 真实链路：大纲输入 60 万字并确认 → 按现有行为进入设定层 → 进入卷纲层 → 显示 60 万字、每卷 10 万字、自动分配 6 卷、卷数只读。
5. 验证器保存并恢复项目及流水线配置快照，未污染用户数据；脚本归档于 `_audit/scripts/archive/`。

**新增经验**:
1. 验证链路必须服从当前应用真实状态机；大纲确认后的下一步是设定层，不能把目标功能所在的卷纲层误当作直接跳转结果。
2. 字数联动验证必须使用可计算样本（60 万字 ÷ 10 万字 = 6 卷），不能写死与输入无关的预期值。
3. “数据已计算”与“用户可见”是两个闭环要求，必须同时验证 store 持久化、只读约束、DOM 文案和布局尺寸。

### P8-P9 卷纲层闭环与按钮语义收敛（2026-08-18）

**精准修改**:
1. 每个卷框增加“保存并锁定本卷”和“删除本卷”，锁定后编辑字段只读，锁定卷才进入章节层选择器。
2. 修复 Vue 3 模板中 `v-if` 与 `v-for` 同节点导致的运行时崩溃：使用 `<template v-for>` 包裹条件项。
3. 卷纲层底部只保留 `AI生成全卷`、`逐卷生成`、`确认完成下一步` 三个用户命令；删除与第一项同调用的“自动生成卷纲”和未纳入当前规格的“批量续生成”。

**验证证据**:
1. 构建：`npx vite build` 通过，151 modules；仅有既存 CommonJS 警告。
2. 启动：先执行 `taskkill /F /IM electron.exe`，再使用 `start-electron.bat` 源启动器，CDP 9227 正常。
3. P8：`_audit/P8_volume_save_delete_verify.json`，12/12 PASS；覆盖保存锁定、只读、章节层下拉联动、删除和持久化清理。
4. P9：`_audit/P9_volume_buttons_verify.json`，10/10 PASS；覆盖流水线打开、卷纲层显示、三按钮可见、两个旧按钮不存在、确认后进入章节层。
5. 截图：`_audit/P8_volume_save_delete_verify.png`、`_audit/P9_volume_buttons_verify.png`。
6. 验证脚本均位于 `_audit/scripts/archive/`，在 `finally` 中恢复项目快照和流水线配置，未污染用户数据。

**新增经验**:
1. “按钮存在”不等于“按钮规格正确”：同一业务调用的重复入口必须删除，只保留用户能理解且唯一的命令。
2. 按钮精简后必须验证确认按钮的后续状态迁移，不能只做 DOM 数量检查。
3. P8/P9 共用一个生成流水线闭环时，应保留同一份源码与构建产物一起提交，避免源文件和 `dist-renderer` 版本不一致。

### P10 章节层空状态与布局闭环（2026-08-18）

**精准修改**:
1. 没有已锁定卷纲时，不渲染章节列表容器和章节卡片，只显示“暂无已锁定卷纲”状态。
2. 有已锁定卷纲但当前卷尚未生成章节时，保留章节内容区但不渲染章节卡片，只显示“暂无章节，请先生成”。
3. 将原来重复的 `pl-ch-empty-hint` 拆成 `pl-ch-empty-no-volume` 和 `pl-ch-empty-no-chapters`，避免 DOM 重复 ID 和状态误判。

**验证证据**:
1. 构建：`npx vite build` 通过，151 modules；既有 CommonJS 警告不变。
2. 启动：清理 Electron 后使用 `start-electron.bat` 源启动器，CDP 9227 正常。
3. `_audit/P10_chapter_empty_state_verify.json`：6/6 PASS；覆盖章节层打开、无锁定卷状态、有锁定卷但无章节状态、章节卡片数量和容器渲染条件。
4. 截图：`_audit/P10_chapter_empty_state_verify.png`。
5. 验证脚本：`_audit/scripts/archive/p10_chapter_empty_state_verify.cjs`，重载后会重新打开流水线，最终恢复原项目快照。

**新增经验**:
1. 空状态不是“列表为空”这么简单，必须区分前置条件缺失和当前对象尚未生成两种用户状态。
2. 验证器修改项目后 reload 会关闭 toggle 面板；每次 reload 后必须重新判断面板状态再进入目标步骤。

### P1 项目管理彻底删除闭环（2026-08-18 追加）

| # | 错误/风险 | 根因 | 改正措施 | 避免再犯 |
|---|---|---|---|---|
| P1-1 | 只验证项目列表消失，就把“删除成功”当作完成 | 列表是局部 UI 证据，不能证明存储、当前状态和重载恢复都已清理 | 递进验证删除双格式存储键、列表即时移除、当前项目状态、章节树残留、重载后项目不复活 | 项目删除闭环必须覆盖“存储→store→章节树→重载”四层，不得只检查一行列表 |
| P1-2 | 验证器把“删除最后一个临时项目”误写成“全局必须没有项目” | 测试夹具没有区分临时项目和用户原有项目，容易把真实项目误判成残留 | 断言只针对临时项目 ID；同时记录真实项目键数量，并验证 P1 临时项目不再出现 | 所有破坏性测试必须按测试 ID 精确断言，不能用全局数量替代对象级断言 |
| P1-3 | 未验证重启后状态，无法排除 lastProjectId 复活路径 | 删除内存状态和持久化状态是两条路径 | 删除后清理项目指针，reload 后再次读取 storage、Pinia 和项目列表 | 涉及持久化的删除、切换、新建都必须增加 reload/restart 后复核 |

**P1 行为验证证据**:
1. 源文件启动器：start-electron.bat；CDP：127.0.0.1:9227。
2. 证据报告：_audit/P1_project_delete_verify.json，8/8 PASS。
3. 覆盖：同一项目新旧键去重；删除同时清理 wa_project_<id> 与 wa_project-<id>；列表即时移除；删除当前项目后 currentProjectId/projectName/outlineText 清空；章节树无残留；重载后 lastProjectId、当前项目和临时项目列表均不复活。
4. 截图：_audit/P1_project_delete_verify.png。
5. 临时项目只使用 p1_verify_* ID，验证结束后在 finally 中恢复原存储快照。

### P2 大纲文件导入闭环（2026-08-18 追加）

| # | 错误/风险 | 根因 | 改正措施 | 避免再犯 |
|---|---|---|---|---|
| P2-1 | 只通过文件控件注入验证导入，未点击用户入口 | 只能证明解析函数可用，不能证明按钮可见、可用和入口连通 | 先检查按钮 DOM/尺寸/可用状态，再点击导入按钮后设置文件并检查编辑器、存储 | 文件导入必须覆盖“按钮→文件控件→解析→编辑器→项目存储”完整链路 |
| P2-2 | 只测 TXT 就判定导入功能完成 | 客户反馈涉及文件类型，单一格式不能代表整个入口 | 同一真实入口覆盖 TXT、MD、RTF、DOCX，并检查 accept 和文案 | 任何文件类型反馈都必须按格式逐项验证，不能用一个格式外推 |
| P2-3 | 验证器异常退出时可能留下导入内容 | 快照恢复只放在正常路径，异常路径没有 finally 清理 | 保存完整 storage 快照，并在 finally 中按键删除新增数据、恢复原数据；重载后再次确认无残留 | 所有写入存储的验证器必须有异常清理和重载复核 |

**P2 行为验证证据**:
1. 源文件启动器：start-electron.bat；CDP：127.0.0.1:9227。
2. 证据报告：_audit/P2_outline_import_verify.json，16/16 PASS。
3. 覆盖：大纲工作台打开、导入按钮可见可用、accept 约束、TXT/MD/RTF/DOCX 点击导入、编辑器更新、项目存储写入、重载后无验证残留。
4. 截图：_audit/P2_outline_import_verify.png。
5. 临时数据在验证器 finally 中恢复；未修改生产项目内容。

### P3 项目管理闭环（2026-08-18 追加）

| # | 错误/风险 | 根因 | 改正措施 | 避免再犯 |
|---|---|---|---|---|
| P3-1 | 新建或切换项目只用浏览器 confirm，用户无法明确选择保存、删除或取消 | 旧实现把“是否保存”压缩成二选一，未覆盖项目切换/新建的三种真实用户意图 | 在项目管理弹窗内增加保存并继续、删除并继续、取消三选一状态面板；取消只关闭待处理动作，不改变当前项目 | 项目转换动作必须显式验证三种分支，不能只验证默认确认分支 |
| P3-2 | 项目切换后章节树可能继续显示旧项目的展开卷和选中章 | 章节树的展开状态和选中状态是组件本地状态，项目切换时没有重置 | 监听 `currentProjectId`，清空旧展开/选中状态并展开新项目第一卷 | 任何项目级状态切换都必须检查组件本地视图状态是否同步清理 |
| P3-3 | 新建/加载项目后只有大纲文本，没有卷章结构，章节树和流水线各自表现不一致 | 项目加载路径没有保证从大纲建立基础卷章结构 | 在 project store 的加载和新建路径统一调用 `ensureVolumesFromOutline()`，让章节树和流水线读取同一份状态 | 项目切换闭环必须同时验证大纲、卷、章、章节树和流水线，不得只看项目名称 |
| P3-4 | 验证器遇到启动时遗留的转换面板会把遮罩误判成业务失败 | 验证脚本假设启动后没有任何待处理面板 | 验证器入口先处理启动时可见的转换面板，再进入正式断言；同时增加“新建取消后不创建”和“切换取消后保持当前项目”断言 | CDP 脚本必须有启动态清理和对象级断言，失败后先诊断遮罩/状态再改业务代码 |

**P3 行为验证证据**:
1. 构建：`npx vite build` 通过，151 modules；仅保留既有 CommonJS 警告。
2. 启动：每次验证前执行 `taskkill /F /IM electron.exe`，使用 `start-electron.bat` 源文件启动器，CDP `127.0.0.1:9227`。
3. 证据报告：`_audit/P3_project_management_verify.json`，18 项递进断言全部 PASS；截图：`_audit/P3_project_management_verify.png`。
4. 覆盖：项目管理打开、新建、保存并继续、删除并继续入口、取消不继续、切换、章节树同步、流水线大纲同步、项目删除后的列表移除。
5. 验证器已归档：`_audit/scripts/archive/p3_project_management_verify.cjs`；所有临时项目 ID 使用 `p3_verify_*`，在 `finally` 中恢复原存储快照。


### P4 大纲工作台闭环收尾（2026-08-18）

#### 目标

验证大纲工作台的真实用户闭环：打开工作台、载入当前项目大纲、自由编辑、保存到本地、确认并锁定、进入生成流水线，以及重载后的状态恢复。

#### 验证结果

1. 证据报告：`_audit/P4_outline_workspace_verify.json`，9/9 PASS。
2. 截图：`_audit/P4_outline_workspace_verify.png`。
3. 覆盖：工作台打开、编辑器载入项目内容、自由编辑、保存按钮可见可点击、确认后项目锁定、锁定后进入生成流水线、流水线读取同一份大纲、重载后大纲和锁定状态持久化。
4. 验证脚本：`_audit/scripts/archive/p4_outline_workspace_verify.cjs`。

#### 重要边界

本轮“保存到本地”通过真实按钮对应的 IPC 写入能力完成验证，报告中明确记录了临时文件写入成功；原生 Electron 保存对话框会阻塞 CDP，因此没有把“用户在原生对话框中选择路径并确认”写成已验证。以后涉及原生对话框时，必须把 IPC 能力证据、按钮触发证据和原生窗口操作证据分开记录，缺少任一层都不能声称完整通过。

#### 防止再犯

验证报告必须区分“功能实现存在”“网页 DOM 操作成功”“原生窗口操作成功”三种证据等级；CDP 无法可靠控制原生窗口时，不得用直接 IPC 或夹具调用冒充完整用户流程。P4 完成后才能进入 P5，不能因部分证据缺口擅自扩大结论。

### P5 编辑器与对话框双向同步（2026-08-18）

#### 本轮真实根因

| # | 错误/风险 | 根因 | 改正措施 | 避免再犯 |
|---|---|---|---|---|
| P5-1 | 首次从编辑器选区发起“改写”时请求路径抛出 `Cannot read properties of undefined (reading 'role')` | `messages` 是 Vue `computed`，脚本逻辑却按普通数组访问，导致 `messages[i]` 不是消息对象 | 所有脚本态遍历、取末条消息和 role 判断统一使用 `messages.value` | 组件模板可自动解包 computed，脚本不能照搬模板写法；每个 computed 在脚本中的读取方式必须单独检查 |
| P5-2 | 重载后右侧对话显示默认会话，当前项目的持久化记录没有自动恢复 | App 启动时先加载默认会话，项目状态稍后才恢复，ChatPanel 没有在项目 ID 变化后重新加载项目会话 | 集中封装 `syncChatProject()`，在挂载和 projectId 变化时按项目键加载会话并恢复当前编辑标签会话 | 项目级组件初始化必须验证“默认状态→项目状态”的竞态；重载验证必须检查实际 `wa_chat_<projectId>` 键和当前会话 |

#### 新架构行为等价实现

1. 编辑器选区通过 `editor-action` 事件传递 `tabId`、选区起止位置和选中文本。
2. ChatPanel 将选区上下文放入待处理状态，发送到右侧对话，不复制一份正文作为独立事实来源。
3. AI 回复气泡的“替换”只替换同一标签页同一选区；没有有效选区时才进入整章替换确认分支。
4. 流式 assistant 回复每次更新都落盘，刷新后按当前项目恢复对话，避免只在最终消息完成时才保存。

#### 证据与边界

- 构建：`npx vite build` 通过，151 modules；仅有既存 CommonJS 警告。
- 源启动器：清理 Electron 后使用 `start-electron.bat`，CDP `9227`。
- 报告：`_audit/P5_editor_chat_verify.json`，11/11 PASS。
- 真实闭环：章节树打开章节 → 编辑器选区 → 内联改写 → 真实 API 回复 → 气泡替换 → 前后正文保留 → 会话消息保存 → 重载后恢复。
- 单章节夹具下标签隔离采用源码 guard 证据；多标签真实操作仍需在 P6 回归中补充。

#### 新增硬规则

1. 双向同步必须同时验证事件载荷、编辑器正文、对话消息、项目存储和重载恢复，不能只看气泡出现。
2. 替换操作必须记录替换前后正文，并断言选区外前后文保持不变。
3. 流式消息持久化必须在流式更新路径验证，不能只检查最终 `addMessage`。

### P12 正文生成到编辑器收尾经验（2026-08-18）

| # | 错误/风险 | 根因 | 改正措施 | 避免再犯 |
|---|---|---|---|---|
| P12-1 | 正文已经写入编辑器，但生成流水线遮罩仍覆盖页面，用户无法继续点击编辑器 | `insert-text` 事件只传了正文，没有声明这是“打开主页编辑器”的跨面板动作；App 只更新编辑器，没有关闭 activePanel | 事件 detail 增加 `openEditor: true`；App 收到后清空 `activePanel`，由状态驱动流水线卸载 | 跨面板动作必须同时验证目标数据、目标 UI 和源覆盖层的生命周期；“编辑器打开”不能只看 tab 是否创建 |
| P12-2 | 旧验证器把流水线结果节点仍存在当作“正文已显示”，修复后因面板按设计关闭而误报失败 | 验证脚本没有跟随最新行为契约更新，混淆了“流水线结果展示”和“正文进入编辑器”两个不同终态 | 删除旧断言，重写同一验证器：以章节 store、编辑器 tab、textarea 三处正文一致作为显示证据，并单独断言流水线已关闭 | 业务状态机变化后必须同步审查验证器断言；面板卸载是预期行为时，不得用 DOM 不存在判定业务失败 |
| P12-3 | 汇总器把不同历史报告字段 `pass`/`passed` 和不同结果结构混在一起，可能把有效证据误判为缺失 | 报告格式没有统一，检查脚本只认识一种 schema | 汇总时先识别报告 schema，再按 `results[].pass`、`results[].passed` 或顶层 `passed` 读取；对没有统一结果结构的报告保留边界说明 | 证据汇总不能只按文件名、行数或单一字段；格式不一致时先报告证据等级，不得强行合并 |

**P12 行为证据**:
1. `_audit/P12_body_editor_verify.json`：13/13 PASS；截图同名 PNG 已保留。
2. 真实链路覆盖 API 请求、章节 store、生成终态、流水线关闭、编辑器 tab/textarea、保存持久化、重载恢复和章节树重新打开。
3. 验证器使用临时项目和受控 fetch；所有恢复逻辑在 `finally` 中执行，脚本已移至 `_audit/scripts/archive/`。

**新增硬规则**:
1. 目标面板打开与源面板关闭必须作为同一个闭环验证，不能只验证其中一边。
2. 验证器必须以当前行为契约为准；行为改动后先更新断言模型，再运行验证，不允许用旧断言逼迫代码保留错误 UI。
3. 全回归报告必须标出"真实外部 API""受控 API""源码/构建证据"三种等级，不能把受控响应写成外部服务质量已验证。


### 六项验证边界收口经验（2026-08-18）

| # | 错误/风险 | 根因 | 改正措施 | 避免再犯 |
|---|---|---|---|---|
| J15 | 原生对话框阻塞 CDP 验证 | 将原生窗口证据分为三类：CDP 句柄、控件识别、截图 | 原生窗口操作不能只靠 CDP 断言，必须同时提供三类证据。 |
| J16 | CJS/ESM 混合警告被忽略 | 逐条审计构建日志，删除 `module.exports` 分支 | 构建警告必须逐条处理，不能因"不影响运行"而忽略。 |
| J17 | 旧脚本选择器/状态断言失效 | 复用旧脚本前必须检查选择器是否存在、store 字段是否仍存在 | 旧验证脚本的选择器可能已失效，复用前必须确认。 |
| J18 | 真实 API 与受控 fetch 证据混同 | 全回归报告必须标注每项证据的等级：真实外部 API、受控 API、源码/构建证据 | 不同证据等级不能混用，不能把受控响应写成外部服务质量已验证。 |

### 工作区清理规则（2026-08-19）

| # | 规则 | 说明 |
|---|---|---|
| C1 | 每次任务完成后必须清理工作区 | 删除本次任务产生的所有临时脚本、证据文件、截图、中间文件，只保留 DEV_LOG 和经验文件的核心更新 |
| C2 | 每次任务开始时工作区必须干净 | 检查 `_audit/` 目录是否有旧任务遗留文件，有则先清理再开始新任务。严禁基于旧文件做"验证已通过"的结论 |
| C3 | 旧证据文件归零 | 证据文件只在当前任务周期内有效，跨任务周期必须重新验证，不能复用旧周期的证据文件汇报完成 |
| C4 | 使用 git clean 或手动删除 | 任务完成后使用 `git clean -fd` 或手动删除 `_audit/tmp/` 和本次任务不再需要的证据文件 |
| J18 | 真实 API 与受控 fetch 证据混同 | 全回归报告必须标注每项证据的等级：真实外部 API、受控 API、源码/构建证据 | 不同证据等级不能混用，不能把受控响应写成外部服务质量已验证。 |

### P1 项目管理彻底删除修复（2026-08-19）

| # | 问题 | 根因 | 修复 | 验证 |
|---|---|---|---|---|
| P1-1 | 幽灵项目占位无法删除 | storage:remove 只从主目录删除，旧目录（写作助手数据）文件永远不会清理 | electron/ipc/storage.js 增加 legacyDir 清理；project.ts 增加 wa_project_ 格式删除 | CDP 验证通过：创建→删除→0 项目→存储无 ghost 键→重启后无项目 |
| P1-2 | 删除后项目列表显示旧数据 | loadProjectList 没有前置清空 projectList | 增加 projectList.value = [] | CDP 验证通过 |
| P1-3 | 删除最后一个项目后 lastProjectId 残留 | 未清理 lastProjectId | 在 projectList.length === 0 时清理 lastProjectId | CDP 验证通过：lastProjectId=NONE |
| P1-4 | 旧格式 wa_project- 键未被清理 | deleteProject 只删除了部分格式 | 增加 wa_project_ 格式删除，项目全空时扫描所有 key 清理 legacy 格式 | CDP 验证通过：ghost 键数组为空 |

经验总结：文件系统存储的删除操作必须考虑所有历史格式和目录。删除后必须验证存储层和 UI 层都已同步。P1 通过后检查 lastProjectId 是否被清除。

### P2 大纲文件导入修复（2026-08-19）

| # | 问题 | 根因 | 修复 | 验证 |
|---|---|---|---|---|
| P2-1 | RTF 导入正文夹带 `Times New Roman;` | 旧实现用正则删除控制字并直接删除 `{}`，字体表文本被当作正文保留 | 新增 `parseRtfText`：词法解析控制字/分组，跳过 `fonttbl` 等目的组，支持 `\par`、`\line`、`\tab`、`\'hh`、`\uN`，`\ansicpg936` 时按 GBK 解码 | CDP 实测 textarea/store/存储均为 `星海神意 RTF 导入测试\n第二段内容`，无字体名残留 |
| P2-2 | `.doc` 报错为英文 `DOC_LEGACY_NOT_SUPPORTED` | 直接把内部错误码抛给用户 | 改为中文 `'.doc 旧版格式不支持，请另存为 .docx、.txt 或 .md'` | CDP 实测 alert 为中文提示 |

**新增硬规则**：
1. RTF 解析必须解析控制字与方括分组，不能只做正则删字符。
2. 用户可见错误必须是用户能看懂的中文，不把错误码当消息。
3. 写代码生成脚本时，禁止用模板字符串内嵌转义序列（如 `\n`）再写入源码；先写独立纯文本文件，再读取拼接。否则 `\n` 会被模板字面量解释成真实换行，把源文件写坏。


### P5 设定层生成反馈闭环（2026-08-19）

| # | 问题 | 根因 | 改正措施 | 避免再犯 |
|---|---|---|---|
| P5-1 | 验证脚本读到 `project-storage: null`，一度误判持久化失败 | `page.evaluate` 包装器没有透传参数，实际读取的是 `wa_project_undefined` | 验证工具函数必须支持 `...args` 并传给 `page.evaluate(fn, ...args)` | 写 CDP 验证脚本时先核验 evaluate 的参数传递，再下“应用 bug”结论 |
| P5-2 | 生成后 `.pl-setting-item` 数量为 0，一度误判列表渲染失败 | 默认选中的是“设定类”，该分类可能没有设定项；生成项在“角色/异能”等动态分类里 | 断言前先检查 active 分类，必要时点击动态分类再数设定项 | 渲染断言必须与当前选中分类一致；先读 activeCat，匹配分类后读数 |

**P5 行为证据**：
1. `_audit/tmp/p5_ui_verify_result.json`：日志 60+ 行，进度 10→100，分类 5、设定项 10、绑定持久化、跳转卷纲层全部通过。
2. `_audit/tmp/p5_after_generation.png`、`_audit/tmp/p5_after_confirm.png`：生成反馈与确认后界面截图。


### P10 bookWordCount 默认值修复（2026-08-19）

| # | 错误/风险 | 根因 | 改正措施 | 避免再犯 |
|---|---------|------|---------|---------|
| P10-1 | 流水线打开后 bookWordCount=0 导致确认按钮 disabled | bookWordCount 是本地 ref(0)，仅有恢复路径没有默认值路径 | onMounted 中增加：大纲已锁定且 bookWordCount<=0 时自动填入 10 | 字数默认值必须在组件初始化时设置，不能依赖用户手动输入；大纲锁定后进入流水线应自动填充合理默认值 |
| P10-2 | 后续 4 层全部 display:none | 步骤 2-5 的 v-show 由前置条件驱动，bookWordCount=0 导致条件不满足 | 修复 bookWordCount 后步骤 2 正常显示（flex），步骤 3-5 等待前置确认 | 步骤显示链依赖前置条件，修复前置条件后需验证整条链是否恢复 |

**验证证据**:
1. CDP: bookWordCount=10, confirmDisabled=false, volumeCount=1
2. 存储: savedBookWordCount=100000
3. 步骤指示器: s1✓, s2*, s3-s5 pending

### P6 卷纲链断点续跑经验（2026-08-19）

| # | 错误/风险 | 根因 | 改正措施 | 避免再犯 |
|---|---|---|---|---|
| P6-1 | 4 个 Skill 串行调用超过 300 秒，被应用侧 120 秒超时误判为失败 | 供应商响应时间由网络和模型决定，链式调用会叠加等待时间 | 卷纲单次调用恢复 600 秒级等待，不用短超时打断供应商；应用改为保存每步成功结果并支持续跑 | 先区分供应商长响应、网络断开、应用死循环三类问题；没有证据不能把等待时间当代码故障 |
| P6-2 | 链中第3步失败后从第1步重跑，浪费已完成的API调用 | 断点只存在内存，失败或重启后丢失 | 每个 Skill 成功后写入 `wa_pipeline_breakpoint`，记录 step、projectId、lastSuccessChainIndex、lastOutput | 断点必须在成功步骤之后立即保存，不能等整条链结束才保存 |
| P6-3 | 切换项目后可能拿错另一个项目的链中间结果 | 旧断点没有项目隔离 | 续跑前校验 `bp.projectId === currentProjectId`；成功完成后清除 | 所有可恢复任务的断点都必须绑定项目、任务类型和步骤 |
| P6-4 | 验证器等待超时后仍把代码改动当成通过 | 只验证了生成面板出现，没有取得失败后恢复的完整行为证据 | 将“代码/构建证据”“真实供应商正常链路”“受控网络故障恢复”分开记录；P6 在缺少最后两类证据时保持未完成 | 未通过真实行为闭环，不得标记 PASS；超时只能说明证据不足或供应商不稳定 |

**新增硬规则**：
1. 长链 API 任务必须具备可持久化断点；应用重启后从最后成功步骤继续。
2. 断点恢复验证必须覆盖：成功步骤写入、失败后保留、重启读取、当前项目匹配、下一步继续、成功后清理。
3. 供应商长响应不通过缩短应用超时解决；应用层只做状态保存、恢复和用户可见进度。
### P10-P12 全链路回归与递归扫描经验（2026-08-19）
| # | 错误/风险 | 根因 | 改正措施 | 避免再犯 |
|---|---------|------|---------|---------|
| P10-1 | 回归脚本选择器用#btn-project导致0匹配 | 实际ID是#btn-open-project，不是#btn-project | 从源码确认选择器再写脚本 | 回归脚本写完前必须先从源码确认所有DOM ID |
| P10-2 | step1 display:none被误判为FAIL | 已有项目状态停在step2，v-show正确隐藏step1 | v-show隐藏元素验证以DOM存在为准，不以display为准 | 验证v-show元素时检查DOM存在性而非可见性 |
| P11-1 | 34个zero-size按钮被误判为死按钮 | 全部位于v-show隐藏的step内容区(步骤2-5) | 死按钮扫描必须排除v-show隐藏区域 | 扫描死按钮前先确认元素是否在隐藏区域内 |
| P11-2 | split-merge和multi-step残留被误判为旧模式 | 这些是SkillExecutionEngine内部执行模式，不在流水线UI暴露 | 内部执行模式不等于用户可见模式 | 区分内部执行模式和用户可见模式 |
| P11-3 | build后JS函数名被minify导致源码检查FAIL | Vite production build会minify函数名 | 源码检查和CDP DOM检查是权威证据，build JS检查不可靠 | 不用build JS做函数名级别验证 |

**P10-P12 新增硬规则**：
1. 回归脚本选择器必须从源码确认，不能凭记忆推测。
2. v-show隐藏元素验证以DOM存在为准，不以display为准。
3. 死按钮扫描必须排除v-show隐藏区域。
4. 内部执行模式(split-merge/multi-step)不等于用户可见模式(compose/chain)。
5. build JS函数名被minify，不以build JS做函数名级别验证。

### P8 正文层到主编辑器行为等价经验（2026-08-19）

| # | 错误/风险 | 根因 | 改正措施 | 避免再犯 |
|---|---|---|---|---|
| P8-1 | 章节已经生成但正文层无法验证 | 验证时只看章节数据，没有按用户链路点击“章节确认完成”进入正文层 | 先通过界面确认章节，再读取正文层控件；不直接修改 Pinia 状态 | 前置层的 store 状态存在，不等于当前面板已进入；每层必须真实点击进入 |
| P8-2 | 正文生成可能被误判成 Skill 链生成 | 正文函数复用了统一 `runStepSkills` 入口，容易忽略当前用户配置 | 本轮读取正文层 Agent/Skill：Agent 为空、Skill 为空；以真实供应商 API 结果作为 P8 证据，Skill 组合留到 P9 | 验证前先记录本层 Agent、Skill、模式；不能把统一执行入口直接当成 Skill 已被选择 |
| P8-3 | 只验证正文气泡/结果区，漏掉编辑器实际联动 | 生成函数同时更新章节、项目存储并派发 `insert-text`；只看其中一个节点会造成假闭环 | 同时断言 `activePanel`、编辑器标题/正文、章节 `bodyGenerated`、项目存储正文和章节树；再杀进程重启复核 | 生成类功能必须验证“源面板关闭→目标编辑器打开→正文三处一致→保存→重启恢复” |
| P8-4 | 点击“插入到编辑器”后出现脏标签被误判为失败 | 插入动作按编辑器契约标记 `isDirty=true`，等待用户保存 | 先验证正文文本一致，再点击编辑器“保存”，确认脏标记清除且项目存储不变 | 插入和保存是两个用户动作；不能把 dirty 状态当作写入失败 |

### P8 行为证据

1. 真实外部 API：`p8_body_verify.cjs` 输出进度 `10→100`，约 `58544ms` 返回，正文 `2802` 字符。
2. store/UI/存储：编辑器正文、章节正文、项目存储三端均为 `2802`，`bodyGenerated=true`。
3. 重启：源启动器重启后章节数 `30`、第一章正文长度 `2802`、持久化标记仍为 true。

### P8 新增硬规则

1. 正文层验证必须从章节层确认按钮进入，禁止直接通过 store 或 DOM 强行切换层级。
2. 自动跳转编辑器必须同时验证源流水线关闭、目标 tab 打开、正文内容一致和项目存储落盘。
3. “插入到编辑器”与“保存”分别验证；插入后 dirty 是正常状态，保存后才检查持久化终态。

### P8.5 流水线声明清理经验（2026-08-19）

| # | 错误/风险 | 根因 | 改正措施 | 避免再犯 |
|---|---|---|---|---|
| P8.5-1 | 新布局中五层内容被旧“第 X 步”说明压缩 | 步骤导航已经表达层级，旧说明仍占用内容区高度 | 精准删除三条残留声明和唯一 `.pl-desc` 样式；第四层保留真实 API 反馈区域 | 迁移布局时先扫描旧声明文本，再删除旧规格；不能为了“看起来整齐”批量改功能区 |
| P8.5-2 | 一次探针把页面其他“生成”按钮当成流水线入口 | 入口筛选使用模糊文案 | 改用源码确认的稳定入口 `#btn-tree-gen` | CDP 入口必须使用稳定 id；找不到入口时先输出按钮清单，不要猜 |
| P8.5-3 | 验证脚本调用 `browser.close()` 后把 Electron 页面一起关闭 | CDP attach 的 browser close 会影响被控页面生命周期 | 验证脚本只主动 `process.exit(0)`，应用关闭统一由 `taskkill /f /im electron.exe` 完成 | 不在 attach 到 Electron 的验证脚本中调用 `browser.close()`；验证器生命周期和应用生命周期分开 |
| P8.5-4 | 只检查声明计数会漏掉层级被误删或内容溢出 | 文本扫描不是 UI 行为验证 | CDP 逐层点击 1→5，检查 `stepCount=5`、每层可见、高度、横向溢出 | UI 清理必须同时验证 DOM 结构、真实导航和几何边界，不能只做 `rg` |

P8.5 证据：`DEV_LOG_2026-08-19.md` 中“P8.5 流水线五层声明清理”章节；源码改动为 `src/components/pipeline/PipelinePanel.vue` 三处模板删除及 `.pl-desc` 删除。

### P9-1 流水线模式语义清理经验（2026-08-19）

| # | 错误/风险 | 根因 | 改正措施 | 避免再犯 |
|---|---|---|---|---|
| P9-1 | 用户界面只显示两种模式，但流程图、历史配置和日志仍保留旧模式名称；直接把 `compose` 叫“并行”又容易误导为多请求并发 | 用户可见模式、存储值和执行语义没有建立一一对应关系；独立的去 AI 味/聊天模式也被误认为是流水线模式 | 生成流水线只接受 `chain/compose`；`chain` 明确为逐 Skill 顺序 API 调用；`compose` 明确为全部模板一次拼入 system prompt、一次 API 调用；旧历史值归一为 `compose`；流程图和日志只展示两种合法模式 | 任何模式改名先核对实际调用次数、prompt 拼接位置和历史存储；不能只改 label，也不能把组合注入冒充真正并行；独立功能的旧模式字段不能未经引用扫描就删除 |

**P9-1 验证边界**：源文件启动器启动后，流水线五层模式选择器均只含“并行/串行”；旧模式文案为 0；`wa_pipeline_step_config.modes` 的值全部为 `compose` 或 `chain`。本轮只验证生成流水线，不把独立去 AI 味、聊天和 Skill 设置功能的内部模式字段误报为流水线遗留。
### P1-P5 神意助手5项UI联动修复 (2026-08-20)

| # | 错误/风险 | 根因 | 改正措施 | 避免再犯 |
|---|---------|------|---------|---------|
| P1-P5-1 | 章节卡片无删除按钮 | 初始章节卡片只有内容区，未设计操作行 | 在 .pl-ch-card-head 内增加 btn-pl-del-ch-{i} 按钮，调用 deleteChapterCard splice store + saveProject | 每类卡片（章节/卷纲/设定）都必须有删除入口，不能只增不删 |
| P2-1 | 章节树只能看卷名和章节标题，无法快速查看纲要/概要 | 树节点未设计点亮按钮 | 在卷名旁增加 btn-tree-vol-outline 按钮（打开 vol-outline tab），在章节标题旁增加 btn-tree-ch-plot 按钮（打开 ch-plot tab） | 树节点不只是导航，还应该是内容快捷入口；每个节点类型都应能打开对应内容 |
| P5-1 | 编辑器标签页标题文字溢出 | 标题设置了 max-width:200px + flex-shrink:0，长标题被截断 | 改为 flex:1 1 auto + min-width:0，让标题自适应可用空间 | flex 布局中固定 max-width 会截断内容；优先用 min-width:0 + flex 自适应 |
| P3-1 | 对话框信息栏不知道当前编辑区内容属于哪一层 | ChatPanel header 只显示供应商信息，未同步编辑器 tab 状态 | 新增3个 computed（syncedStepLabel/syncedAgentName/syncedSkillNames），映射 editorStore.activeTab.mode 到流水线步骤，读取对应 agent 和 skill | 对话框不是独立面板，它必须感知编辑器当前内容上下文；信息栏应实时反映编辑区状态 |
| P4-1 | 对话框发送消息时不包含编辑区内容作为上下文 | sendMessage 只用用户输入构建 system prompt，未读取编辑器内容 | watch(editorStore.activeTab) 触发 setCurrentContext；watch(activeTab.content) 实时更新 content；sendMessage 从 currentContext.content 提取 ctxText 注入 systemParts | 对话框的 system prompt 必须包含当前编辑区内容；watch + currentContext 是标准模式 |

**新增硬规则**：
1. 每类树节点（卷/章/设定）必须能通过点击打开对应内容编辑 tab，不只是展开/折叠。
2. flex 布局中标题类元素用 min-width:0 + flex:1 1 auto，禁止 max-width 固定截断。
3. 对话框信息栏必须同步编辑器 tab 的层级别、agent 和 skill，用 computed 映射。
4. 对话框 system prompt 必须包含编辑区当前内容，用 watch + currentContext 实时绑定。
5. 章节卡片必须有删除按钮，删除后调用 store splice + saveProject 持久化。

### 规则12：防无限回滚铁律（2026-08-20）
```

### 规则14：记忆增量合并必须纯函数化（2026-08-20）
```
1. 合并器只接收当前 MemoryData、抽取结果和章节上下文，返回新快照与变更记录，不直接操作 Electron 存储。
2. 实体必须按名称/别名去重；新增事实叠加，证据去重，状态变化保留 statusHistory。
3. lockedFields 和黑名单是应用层硬边界，任何抽取结果都不能绕过；跳过项必须进入变更记录。
4. 受控测试的断言必须覆盖多章无重复、锁定值不变、黑名单不进入结果和元数据计数，不以“能编译”代替行为证据。
```
1. 每项任务完成勾选并经过CDP验证后，视为"终态闭环"，不可整体回滚重做
2. 后续任务发现前期任务的问题时，只能在当前状态上做精准修复，不得回滚已完成的整个阶段
3. 精准修复后必须重新验证该修复项，但不需要重新验证该阶段的其他已通过项
4. 任何"我觉得可以做得更好"不是回滚理由，只有"功能行为与旧架构不一致"才是修复理由
5. 回滚请求必须附带具体证据（哪个行为不一致、哪个按钮失效、哪个数据丢失），不得笼统回滚
6. 已经通过CDP验证的截图和日志保留为不可推翻的证据，后续不得以"怀疑有问题"为由推翻
7. 如果修复某功能导致新 bug，修复新 bug 即可，不需要回滚到修复前的状态从头来
8. 勾选清单中的每一项一旦标记 [x] 并附带了验证证据，即视为永久完成，不可撤销
```

### 记忆板块 P0 数据基础核销经验（2026-08-20）

| 风险 | 根因 | 改正措施 | 避免再犯 |
|---|---|---|---|
| 只完成 MemoryData 代码，却没有同步核销计划和留存验证边界 | 把类型扩展、持久化和运行行为当成同一层证据 | P0 必须分别核对类型定义、store 加载/保存、旧数据兼容、新数据持久化和重启一致性，并在计划表逐项附证据 | 计划勾选必须绑定实际文件或本轮命令/行为证据；只有构建通过不能代替存储与重启验证 |

本轮确认：记忆数据的唯一事实来源是 `project` store；旧 `categories/items` 只作为兼容字段保留；后续 P1 抽取服务必须通过 store 方法写入，不能另建组件本地记忆副本。

### 记忆板块 P4.7 可视化来源导航经验（2026-08-20）

| 风险 | 根因 | 改正措施 | 避免再犯 |
|---|---|---|---|
| 可视化组件只显示“已选中”，用户无法回到正文来源 | 视图组件各自维护选中状态，却没有把来源事件交给应用导航层 | 四个视图统一发出 `open-source` 事件，由 `MemoryPanel` 解析事件/实体的章节来源并调用唯一的 `editorStore.openTab()` | 可视化组件只负责展示和发事件，不复制编辑器导航或项目数据 |
| 空数据被误报为事件跳转通过 | 当前项目只有实体样本，没有 `MemoryEvent` 可点击项 | 明确记录事件数量；没有真实事件就保持未验收，不能用源码存在或空状态代替真实行为证据 | 每个可视化交互必须有真实数据样本完成“点击→目标 tab→正文一致”验证 |
| 关闭记忆面板后无法确认是否真的打开正文 | 只检查了事件处理函数，没有同时核对面板状态和编辑器内容 | CDP 真实点击来源按钮，同时断言记忆面板关闭、`#editor-content` 内容和当前编辑模式 | 导航闭环至少同时检查来源、目标 UI、内容一致性和状态终态 |

### 记忆板块 P1 抽取服务经验（2026-08-20）

| 风险 | 根因 | 改正措施 | 避免再犯 |
|---|---|---|---|
| 抽取服务直接依赖 provider/store，容易形成第二套 AI 配置和记忆状态 | 把服务层误写成组件层，绕过现有统一入口 | 服务只接收注入的 `MemoryAiCall`，只返回 `ExtractionResult`，由上层负责写入 project store | 新服务先画输入→AI入口→解析→返回边界；不在服务中复制 Pinia 状态或供应商配置 |
| 模型输出能解析但 evidence 属于别章，仍会污染记忆库 | 只检查 evidence 存在，不检查证据归属 | evidence 必须同时满足当前 `chapterId`、非空 snippet；不合格条目丢弃 | 记忆抽取的最小验收项是结构合法、证据存在、证据归属正确三项，不能只测 JSON.parse |
| 全量类型检查结果混入本次服务判断 | 项目已有 `electronAPI` 声明和旧 JS 模块错误 | 单独记录项目既有类型错误；本次服务用构建和受控边界测试验证 | 先区分新增错误与基线错误，禁止为“绿检查”顺手改无关文件 |
### 记忆板块 P7：版本审计与回滚（2026-08-20）

- **问题/根因**：记忆合并有数据快照，但没有独立的变更历史和可恢复版本；直接把 `history` 嵌进快照会造成递归膨胀。
- **改正措施**：新增 `src/services/memoryVersion.ts`，用 `MemoryChangeRecord` 保存不含 `history` 的 before/after 快照；`project` store 统一提供 `recordMemoryChange`、`getMemoryChangeHistory`、`rollbackMemoryTo`、`rollbackMemoryByChapter`，回滚本身追加审计记录并持久化。
- **验证证据**：受控测试确认历史可查、目标版本状态恢复、按章节回滚可用；`npm run build:vue` 构建通过。源启动器/CDP 尚未作为 P7 的通过证据，不能冒充 UI 验证。
- **避免再犯**：版本功能必须走 store 的唯一入口；快照永不包含 `history`；任何回滚都要产生新的审计记录；逻辑验证和 UI/CDP 验证分开记账，缺 UI 证据就保持未验证状态。
### 记忆板块 P2 面板展示路径修复（2026-08-20）

| 风险 | 根因 | 改正措施 | 避免再犯 |
|---|---|---|---|
| 正文确认已把实体写入 `MemoryData.entities`，记忆面板仍显示“暂无记忆条目” | 面板只读取旧兼容字段 `memories.items`，没有把新模型作为展示来源 | 在 `MemoryPanel.vue` 增加统一展示计算层：旧 `items` 保留编辑能力，新模型实体、关系、事件、世界观、伏笔转为只读展示；分类从实际数据动态汇总 | 每次扩展数据模型都必须沿“写入 store → 面板读取 → 分类筛选 → 关闭重开 → 重启恢复”递归核对，不能只验证写入成功 |
| 重启后验证脚本直接查找面板内部按钮而超时 | 面板默认关闭，验证脚本漏掉了真实打开入口 | 验证脚本先点击 `#btn-memory`，再检查分类和条目 | 默认状态不是功能状态；CDP 验证必须包含入口打开步骤，并记录入口后的 DOM 证据 |

本轮证据：`_audit/DEV_LOG_2026-08-20-P2-memory-panel.md`；构建输出 `154 modules transformed`；CDP 结果包含 `ENTITY_CARD_COUNT=1`、`PERSISTED_ENTITY_COUNT=1`。连续 20 章和真实 API 失败注入仍未验证，不得提前标记完成。

### 记忆板块 P4.8 真实链路验证经验（2026-08-20）

| 风险 | 根因 | 改正措施 | 避免再犯 |
|---|---|---|---|
| 验证器从设定层重复启动，实际页面已经停在正文层 | 验证器把前置 UI 状态写死，没有先读取当前可见层 | 失败后停止重跑前置 API，只从当前可见层继续真实点击 | CDP 长链验证必须先读取当前状态；已通过的层不能因验证器状态假设而重复调用 |
| 用模糊中文文案定位视图按钮，误选了其他面板同名按钮 | 全页面存在多个“时间线”等按钮，缺少组件作用域 | 先从源码确认稳定 ID，再使用 `#btn-memory-relation-graph` 和 `#btn-memory` | 记忆面板验证必须使用作用域稳定选择器，不能用全页面文本首个匹配 |
| 记忆面板入口被遮罩拦截 | `App.vue` 的 `#panel-backdrop` 随 `activePanel` 存在，用户必须先关闭上层面板 | 真实点击 `#btn-close-pl` 或 `#panel-backdrop` 后再点击记忆入口 | 验证侧栏面板前先处理当前活动面板和遮罩，不能 force 穿透遮罩 |
| 空事件/空关系被误报为功能通过 | 当前正文抽取样本只产生实体，`events=0`、`relations=0` | 分别记录实体、事件、关系数量；缺少节点就保持事件导航/关系一致性未验收 | 空状态只能证明空状态渲染，不证明节点点击和来源跳转 |

本轮真实 CDP 证据：设定→卷纲→章节→正文逐层按钮导航；正文确认后出现“记忆变更预览”；四态循环 DOM 依次为 `memory-relation-graph`、`memory-graph-analysis`、`memory-mind-map`、`memory-timeline`；关系图文本为 `节点 3 · 关系 0`，时间线文本为 `事件 0/0`。临时探针已清理，未保留临时脚本。

### 记忆板块 P5 检索回填经验（2026-08-20）

| 错误/风险 | 根因 | 改正措施 | 避免再犯 |
|---|---|---|---|
| 只读 localStorage 后误以为应用没有记忆数据 | Electron 项目数据实际通过 `window.electronAPI.storageList/storageRead` 持久化，localStorage 不是项目数据源 | 先从 Electron 持久化读取真实项目，再通过应用状态确认实体数量；不写入伪造样本 | 验证 Electron 数据必须区分 localStorage 与 `electronAPI` 文件存储 |
| 检索没有命中查询词时静默返回空背景 | 普通中文聊天或正文生成文本不一定包含实体名，严格命中会丢失当前项目的有限背景 | 有精准命中时筛选；无命中但存在记忆时回退带入最多 12 个实体，并仍受 2000 字上限约束 | 检索必须同时覆盖“精准命中”和“有限背景回退”，且记录实际注入文本 |
| 只验证聊天注入，漏验生成流水线注入 | ChatPanel 和 PipelinePanel 是两个独立请求路径，单一路径通过不能证明另一条路径 | 分别真实点击聊天发送和流水线正文生成，使用 CDP Network 捕获各自请求体 | 记忆回填验收至少覆盖聊天请求、流水线请求、数据来源、长度边界四项 |

本轮流水线真实证据：点击 `#btn-pipeline` → 第五层 → `#btn-pl-gen-body`，捕获 `POST https://openapi.cloud-ai.cn/v1/chat/completions`；请求含 `[相关记忆]`、实体 `角色/未命名角色/线索`，记忆段 `80` 字且不超过 `2000` 字。代码、构建、启动、真实 DOM、网络请求和数据边界必须分开记录，不能以模型最终响应代替请求注入证据。
## 记忆面板顶栏平铺标签栏改造 (2026-08-20)

| 编号 | 错误操作 | 原因 | 改正措施 | 避免再犯 |
|------|----------|------|----------|----------|
| E1 | 用rg/findstr搜索构建产物中的原始CSS class名，搜不到就认为没生效 | Vite scoped CSS编译时会给class加data-v-xxx属性前缀，直接搜原始class名搜不到 | 用CDP运行时检查DOM元素，不搜构建产物 | 构建产物中的class名可能被转换，验证必须通过CDP运行时DOM |
| E2 | 改了源文件不重新构建就启动应用 | Electron加载的是dist-renderer构建产物，不构建看不到变化 | 改源文件后必须npx vite build再杀进程重启 | 每次改代码后按顺序：改→构建→杀进程→重启→验证 |
| E3 | CDP验证时点击按钮后立即检查DOM，没等Vue异步渲染 | Vue的v-if组件需要异步渲染周期，立即检查会找不到元素 | CDP验证用awaitPromise+setTimeout等2.5秒再检查 | CDP验证必须等待Vue渲染完成，用Promise+setTimeout |

### 新增规则
- 记忆面板顶栏标签必须平铺，禁止用循环切换按钮
- 导出/导入类按钮收进下拉菜单，节省标签栏空间
- active标签高亮必须有底部2px指示线，不能只靠背景色
- 视图切换建议用v-show而非v-if保持已渲染视图在DOM中
- 临时脚本（_tmp_*.js）验证后必须删除，根目录保持干净
