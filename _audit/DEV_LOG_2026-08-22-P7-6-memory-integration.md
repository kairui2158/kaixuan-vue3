# P7-6 记忆整合全链路回归

日期：2026-08-22
状态：部分完成，未标记整合验收通过

## 本轮范围

核验正文保存与记忆入口、审核写入边界、四视图同源、JSON 导入导出入口、项目持久化/恢复、聊天与流水线读取记忆、动画/短剧接入。

## 对账结果

- [x] 经验文件与 P7-5 日志已读取，沿用“行为等价高于代码存在”和“未实测不标 PASS”。
- [x] 构建通过：`npm run build:vue`，输出 `175 modules transformed`，生成最新 `dist-renderer` bundle。
- [x] 源文件启动器基线通过：`start-electron.bat`，页面 `file:///D:/codex/novel-workshop-vue3/dist-renderer/index.html`，CDP `127.0.0.1:9227`，标题“神意助手”。
- [x] 项目管理真实 DOM 打开：按钮 `#btn-open-project` 可打开“项目管理”，显示“未命名”、加载、删除、新建项目和关闭入口。
- [x] 当前项目存储核对：`wa_lastProjectId=default`，`wa_project_default` 存在且可读；项目名为“未命名”，正文为空，记忆实体/关系/事件/世界观/伏笔/物品均为 0。
- [x] 记忆面板入口和四视图按钮源码已核对；四视图均从 `projectStore.memories` 接收数据，未发现视图自持久化业务副本。
- [x] JSON 菜单源码已核对：默认“导入 JSON（合并）”，另有“覆盖导入 JSON”；合并与覆盖均经过用户确认及 `recordMemoryChange`。
- [x] 主编辑器“提取记忆”入口存在，正文先保存再进入审核预览；流水线正文也有记忆抽取入口。
- [ ] 真实供应商成功抽取与审核确认/拒绝：当前可加载项目为空且没有可安全复用的测试项目，未核销。
- [ ] 原生 JSON 文件选择、导出文件存在且可读、合并/覆盖前后业务数据差异：未核销，未用脚本直写业务数据冒充用户操作。
- [ ] 关闭、杀 Electron、源文件启动器重启后打开同一项目并确认记忆恢复：当前项目无可恢复记忆，未核销。
- [ ] 聊天和流水线实际请求拿到同一份非空记忆上下文：源码入口存在，但没有非空记忆和真实请求证据，未核销。
- [ ] 动画/短剧读取记忆：源码未发现真实板块接入，明确标记“未接入”。

## 运行证据

只读 CDP 探针确认页面按钮、项目弹窗、存储键和项目 JSON。第二次递归探针因使用未限定组件作用域的 `.mem-tab-btn`，在项目弹窗遮罩状态下未命中，已记录为探针选择器问题，不作为业务通过证据。

## 根因与边界

本轮没有修改业务源码。当前无法把空项目和历史聊天内容拼成整合验收；页面显示的历史回归聊天不能证明记忆属于当前项目。真实抽取、原生文件交互和重启恢复必须用客户可控测试项目继续核销。

## 收尾

- [x] 本轮临时 CDP 探针已删除。
- [x] Electron 已杀掉并确认不留本轮运行进程。
- [x] 未写入项目、记忆、聊天或供应商业务数据。

## 下一阶段（仅供审核，不执行）

P7-7：使用客户明确提供或在 UI 中新建的隔离测试项目，完成真实正文抽取审核、原生 JSON 导入导出/合并覆盖、关闭重启恢复、聊天与流水线同源读取；动画/短剧接入另列为后续目标。每项都保留用户操作、DOM、store、文件和重启证据。
