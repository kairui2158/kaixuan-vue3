# 开发日志：3.10.2 封装（2026-08-31）

## 结论

已生成 Windows x64 NSIS 安装包：`dist/神意助手-Setup-3.10.2.exe`（97.7 MB，未签名）。

SHA256：`CD3A957EB0A1046A4EFF1C63551C7A18B6F837E624391981C180C901E4D63302`

## 本版修复

- `loadProject()` 不再根据大纲文本自动预建占位卷。
- 纯大纲项目保存后重新加载，卷纲层保持真实磁盘状态。
- `lockOutline()` 与 `createProject()` 的用户主动建卷入口保持不变。

## 验证

- `npx vite build` 通过。
- 源文件 Electron CDP 验证：大纲 69386 字符，`volumesLength` 为 0。
- `npm run build` 通过，Electron Builder 成功产出 3.10.2 安装包与 blockmap。

## 客户实操入口

1. 导入或粘贴纯文本大纲。
2. 保存退出应用。
3. 重新打开应用或重新加载项目。
4. 进入卷纲层：确认没有凭空出现占位卷。
5. 在大纲工作台主动确认并锁定大纲后，再确认正常的卷建立流程不受影响。

## 交付边界

- 安装包未签名。
- 本轮验证覆盖源文件运行态与安装包产物生成；未在安装包路径重复执行完整 CDP 业务回归。
