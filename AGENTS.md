# 小说工坊 Agent 指令文件

Codex 在每次新对话中自动加载本文件。以下是项目的核心规则，务必遵守。

## 语言与沟通

- 全程使用中文对话，技术术语可用英文但需解释
- 控制台输出使用 [OK]/[ERR]/[WARN] 纯文本标记，禁止使用 emoji 特殊符号
- 用户是编程新手，所有操作需用最直白详细的步骤说明

## 代码修改规则

- 最小改动: 只修改与当前任务直接相关的代码，禁止批量大修
- 精准修复: 遇到错误逐处修复，禁止批量正则替换
- 备份先行: 每次修改前用 Copy-Item 备份到 BACKUP/ 文件夹，根目录保持绝对干净
- 修改后验证: 每次改完立即 node --check 验证语法
- 零假数据: 禁止添加默认 API Key、示例供应商数据、硬编码模型名

## 技术栈约束

- 纯 HTML5 + CSS3 + JavaScript ES6+（class 语法），无任何前端框架
- 禁止引入 npm 依赖，保持单文件可运行
- 禁止使用 Python 脚本批量处理代码
- 所有源文件必须以无 BOM 的 UTF-8 编码保存

## Skill 分工

- 写代码时调用 vanilla-web / frontend-design skill
- 修复问题时调用 webapp-testing / playwright skill
- 记忆更新时调用 project-memory skill
- 禁止不调用对应 skill 直接手动操作

## 运行与网络

- 下载被中断重试最多 5 次，超过后换替代方案
- 超时超过 20 分钟才能考虑换取替代方案，20 分钟内禁止因超时放弃

## GitHub 备份

- 每完成一个功能点就提交推送到 GitHub 仓库 kaixuan (kairui2158/kaixuan)
- 连同开发日志 (DECISIONS.md, PROGRESS.md, CONTEXT.md) 和 memory/ 一起提交
- GitHub Token 通过 gh CLI 安全存储在 Windows 凭据管理器中，禁止写入任何项目文件

## 沙箱审批重试规则

- 遇到 503/429 审批系统过载时，持续重试，禁止1-2次就放弃
- 重试上限：8次，间隔递增（30s -> 60s -> 90s -> 120s -> 150s -> 180s -> 210s -> 240s）
- 总等待时间约15分钟后才考虑换替代方案
- 重试期间继续做不依赖审批的工作，不空等
- 提权操作统一攒到所有沙箱内工作完成后批量执行

## 编码安全规则（规则13）

- 禁止使用 PowerShell 的 Set-Content -Encoding UTF8 修改含中文的源文件
- 修改含中文的源文件时，必须使用 Node.js 的 fs 或 apply_patch
- 禁止使用 Out-File、Set-Content、Add-Content 等 PowerShell cmdlet 写入含中文的源文件

## 清理安全规则（规则14）

- 执行任何清理操作前，必须先用 Copy-Item 备份当前所有源文件到 BACKUP/
- 绝对不要修改 renderer.html 中的 meta charset UTF-8 标签
- 绝对不要修改任何与编码转换相关的代码
- 清理完成后，必须启动应用检查中文是否正常显示。如果出现乱码，立即回退备份

## 测试规则（规则13）

- 每次版本更新前，必须运行测试脚本验证
- 十层自检脚本: node test_full.js（代码质量+功能+性能+视觉+API，目标 0 FAIL）
- 深度模拟脚本: node test_deep_sim.js（单功能+组合场景+随机压力，目标 0 FAIL）
- 两个脚本都通过后才能提交推送
- 如果测试发现 FAIL，先修复再提交，禁止跳过测试直接推送
- 测试脚本本身也需维护，发现误报及时修正

## 纯文本限制（规则14）

- 当前模型（Deepseek）不支持非多模态模型，仅支持纯文本输入
- 禁止发送图片、截图或任何多模态内容，否则会触发超限问题导致会话崩溃
- 如需截图验证，必须使用 Playwright skill 自行截图后描述结果

## 完整规则

详细规则见 memory/rules.md（规则 1-23）。
技术栈详情见 STACK.md。
项目记忆见 memory/ 目录。

## 规则14: 验证三铁律（行为性验证）

- 外部裁判：改动完成后不能靠读代码判断通过。必须用 Playwright CDP 实际操作应用，截图 + JSON日志 + 时间戳三者缺一视为未验证
- 行为验证：不验证"元素存在"，验证"功能能用"。必须验证"操作→预期行为→实际结果"三元组
- 强制执行：每个阶段有验证清单，任何一项 FAIL 禁止进入下一阶段。用户可随时抽查，不符则全部重测

## 规则15: 错误实时记录 + CSS语法完整性

- 检测期间每遇到错误/FAIL，必须立即写入 lessons/ERROR_LOG.md，禁止事后补记
- 每次修改 style.css 后，必须运行花括号平衡检查，确保深度为 0，不为0禁止提交
- 如果 CDP 注入的规则生效但文件内同样规则不生效，立即检查 CSS 语法完整性，不要继续调整规则内容
 
 ## 规则19: CSS"先搜后改"强制门禁（反复违反教训#76）
 
 - **禁止CSS叠加**: 修改任何CSS选择器的属性时，必须先全文搜索该选择器的所有定义，合并到一处，删除其余
 - **新增前先搜**: 新增CSS规则前，先用全文搜索确认该选择器是否已存在。已存在则修改原定义，不允许新增第二处
 - **CSS修改三步法**: (1) 全文搜索选择器 (2) 找到所有定义 (3) 合并到最后一处、删除前面的
 - **封装前检查**: 每次封装前必须运行重复选择器检测脚本，非媒体查询真冲突必须为0才允许封装
 - **根因**: 教训#2(v2.0.0)和教训#76(v2.7.36)都记录了"只加不删"问题，但一直在违反。CSS层叠下后者覆盖前者，旧定义成为死代码，造成文件膨胀+调试困难+属性丢失风险

## 规则18: 429限流应对（含标准操作SOP）

### 429 的本质
- 429 是 API 硬限制，不是客户端状态问题
- 目标模式（防断网）解决不了 429 本身，只能帮 429 解除后从断点恢复
- 子 Agent 和主 Agent 共享同一份配额，子 Agent 触发 429 也会阻塞主 Agent

### 标准操作SOP（遇到429时按此顺序执行，不得跳步）

遇到 exceeded retry limit, last status: 429 Too Many Requests 时的正确做法：

1. 不放弃不简化 — 429是临时状态，禁止跳过步骤、禁止用简化方法绕过、禁止标记任务失败
2. 目标模式继续运行 — 429不阻塞目标模式，防断网机制照常工作，从断点恢复
3. 递增重试 — 按 30s->60s->90s->120s->150s->180s->210s->240s 间隔重试，共8次约15分钟
4. 重试期间做本地工作 — 读文件、CSS编辑、CDP验证、整理审计、清理技术债务，不空等
5. 子Agent触发429时 — 关闭旧子Agent释放配额，再重新spawn
6. 8次重试全部失败后 — 不终止任务，切换纯本地工作，每5分钟静默重试一次被限流操作
7. 429解除后 — 从断点继续，不从头重来
8. 超时持续等待 — 任何超时都持续等待，禁止跳过，禁止用简单方法
9. 递归不限深度 — 递归到最低层，禁止限制5层
10. 必须真实交互 — 截图+DOM操作验证，禁止只检查元素存在不验证功能正确

### 强制应对流程（遇到 429 时执行）
1. 不放弃，按递增间隔重试：30s -> 60s -> 90s -> 120s -> 150s -> 180s -> 210s -> 240s
2. 重试期间不空等，做不依赖新 API 调用的本地工作（读文件、跑已写好的脚本、整理审计结果）
3. 如果是子 Agent 触发的 429，关闭旧子 Agent 释放配额再试
4. 总等待约 15 分钟后才考虑换替代方案
5. 重试成功后从断点继续，不从头重来

### 禁止事项
- 禁止因 429 跳过当前步骤
- 禁止因 429 放弃任务
- 禁止用简化方法绕过需要 API 的验证步骤
- 禁止把 429 当作任务失败的借口

### 重试上限耗尽后（exceeded retry limit）
- 8 次重试全部失败后，目标模式继续运行不被阻塞，429 是临时状态不是终态
- 立即切换到不依赖被限流资源的本地工作（CSS 编辑、CDP 验证、整理审计），不空等
- 每 5 分钟静默重试一次被限流的操作，不阻塞主线工作流
- 429 解除后从断点继续，不从头重来
- 禁止因 429 把目标模式标记为 blocked 或终止目标模式
- 详细规则见 memory/rules.md 规则18

## 规则17: 工作前强制声明 + Agent透明报告

- 任何代码修改前必须先输出「工作前声明」（方法/Agent使用/经验引用/验证方案/错误记录位置/经验总结位置），未输出就写代码 = 违规重来
- 执行检测计划期间每次 spawn_agent 必须向用户透明报告：Agent类型、昵称、任务、文件范围
- 遵守规则13：禁止以线程限制为借口拒绝使用 Agent；必须尝试 GitHub 搜索可用 Agent
